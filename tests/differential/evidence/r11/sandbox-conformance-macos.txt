
> siralos@0.0.0 test:sandbox
> npm run build && node scripts/sandbox/run-conformance.mjs


> siralos@0.0.0 build
> tsc -b

SANDBOX CONFORMANCE: SKIPPED - 33 probes could not execute (backend: anthropic-runtime, profile: develop-offline).
  Private run directories are unavailable, so no sandboxed command can execute with a verified Siralos-owned run directory: Private run-directory creation is unavailable: Node offers no directory-relative (openat/mkdirat-style) or handle-relative primitive, so a same-user process can substitute a verified parent between identity verification and the pathname-based create, placing a new entry outside the intended verified root. Siralos never creates run directories at this stage; nothing was created.
The conformance suite did not run; a skipped suite is never treated as passed.
