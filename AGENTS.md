# AGENTS.md

## New session / coding-agent bootstrap

1. Read [docs/development/PROJECT_CONTEXT.md](docs/development/PROJECT_CONTEXT.md).
2. Read the scoped `AGENTS.md` for files being modified.
3. Follow linked architecture, ADR, security, and style documents as applicable.
   For future-facing product or architecture decisions, read
   [ADR 0036](docs/adr/0036-lean-product-composition-and-extension-model.md)
   (the lean product, composition, and extension model).
4. Verify current milestone status from repository evidence before claiming completion.

**Standing rule: self-loop on every prompt** — every prompt (human or follow-up, Wayfinder or direct implementation) must invoke the `self-loop` skill and follow its `references/verification-protocol.md` ledger (criterion → evidence → pass/fail/unknown → challenge lenses → repair), even when another skill says one ticket per session. Skill load is per-prompt; loop budget is one coherent pass + up to two repairs unless the prompt explicitly changes budget. Also see `docs/wayfinder/siralos-roadmap.md:Notes` for the Wayfinder-persistent form of this rule.

5. If `docs/wayfinder/siralos-roadmap.md` exists on the filesystem, also read its **Destination** and **Notes** (the Wayfinder map). It names the decision-ready route R7.5 → R12 → Stage 4 and the current frontier; Decisions so far are the index of closed tickets (refer by name, never bare id). The map is local-markdown fallback (label `wayfinder:map` on a hosted tracker) — read the file directly, not the `docs/wayfinder/tickets/` children, unless a ticket is claimed.

## Repository

- Rust workspace monorepo (Cargo) with minimal Node harness; live TypeScript tree **removed** (decision 40, freeze at `5da5cde`, v32 234/234, pinned at `tests/differential/evidence/typescript-freeze-v32/`).
- Stage 3R adds the Rust implementation: a Cargo workspace (`Cargo.toml`, `rust-toolchain.toml`, `rustfmt.toml`) with `crates/siralos-core` (domain-neutral host semantics), `crates/siralos-adapters` (infrastructure), and `crates/siralos-cli` (the `siralos` binary); `siralos-godot` now lives in standalone repo `https://github.com/CrimsX/siralos-godot` at `1bf2ca3` (was `crates/siralos-godot` at `5da5cde`–`5da2cab`, 41 files + host adapters at `1bf2ca3` self-contained, `siralos-godot → siralos-core` only via `path = "../siralos/crates/siralos-core"`), pinned in the monorepo as `siralos-godot = { path = "../siralos-godot" }` (3-member workspace, external path dep). Dependency direction: `cli → adapters → core`, `godot → core` (external); core must never depend on infrastructure or a domain. All Rust code follows [docs/development/RUST_STYLE.md](docs/development/RUST_STYLE.md) (authoritative; do not restate it). The TypeScript historical oracle is **archived** at `5da5cde` (freeze v32, 234/234, pinned) and retained only as digest-bound evidence; differential harness runs in **pinned mode** (historical replay requires worktree at freeze SHA); Rust is the sole source of truth per ADR 0032 (decision 40, honest trade-off C7).
- Treat [README.md](README.md) as the user-facing status, [ROADMAP.md](ROADMAP.md) as milestone status, [ARCHITECTURE.md](ARCHITECTURE.md) as dependency ownership, [SECURITY.md](SECURITY.md) as the security contract, and `docs/adr/` as the decision history. Do not duplicate those documents here. Use [docs/architecture/README.md](docs/architecture/README.md) as the architecture index; ADR metadata (id/status/domains/paths/supersedes) lives in each ADR's frontmatter.

## Current implementation

- Stage 3 is complete through milestone 11: task runtime, projections, workspace revisions and structural reads, project instructions and knowledge, references and research, self-reference and capability diagnostics, host-controlled planning, read-only Godot scene/resource intelligence, review context and impact intelligence, approved scene/resource mutation, and the unified `/develop` workflow (ADRs 0025–0027).
- Stage 3R R1 is complete: project renamed to Siralos, Rust engineering guide established, domain-neutral Rust workspace skeleton (see Repository above). Stage 3R R2 is complete: the Differential Behavioral Harness (ADR 0033) is the migration's audit remediation gate — it verifies a scenario- and corpus-digest-bound fixture set, supervises both runners symmetrically, semantically compares typed canonical outcomes, emits the migration audit, and gates remediation; its first subjects (state-dir resolution, product version identity) hold parity. Stage 3R R3 is complete: the domain-neutral host-owned task kernel in `siralos-core` (contracts, state, transitions, evidence, acceptance, completion gate, activity, progress) holds byte parity across 17 differential `task-contract` scenarios. Stage 3R R4 is complete: the generic workspace/project foundation — `siralos-core::workspace` (validated relative paths, bounds, revision handles/registry, prepared-effect models, checkpoint contracts/invariants/undo/reconciliation classification, Git disposition) and `siralos-adapters::workspace` (canonical root, containment-safe resolution, bounded complete exact reads, deterministic list/search, fail-closed mutation preparation, checkpoint storage inspection/reconciliation, typed unavailable Git) — holds differential parity across 23 `workspace-read`/`workspace-list`/`workspace-search`/`workspace-revision`/`workspace-prepare`/`checkpoint`/`git-inspection` scenarios. Stage 3R R5 is complete: the generic language-intelligence foundation — `siralos-core::language` (one-based positions/ranges with typed validation, bounded sanitized diagnostics with deterministic dedup/ordering and explicit truncation, generic symbol/definition/reference query models, the language-neutral structural-document representation with the deterministic advisory summary formatter, typed validation results that never conflate source-invalid with infrastructure failure, reference-extracted generic limits, R4 revisio... (line truncated to 2000 chars)
  Stage 3R R7.2 is complete: `siralos-core::tool` owns the generic Application Tool Loop (domain-neutral validated `CapabilityId`, allow/ask/deny permission evaluation, the immutable registration-ordered Tool Registry, the one generic Tool seam, the approved-visible-surface guard, the Tool Round with one-call/one-result and cancelled-tail pairing, the synchronous pull-based single-flight Application loop with exact round-budget normalization and the closed R7.2 event set) and `siralos-adapters::tool` owns the generic read-only `workspace.list`/`workspace.read`/`workspace.search` Tool adapters over the R4 primitives. The differential corpus gained the `tool-loop` subject with 16 required scenarios (corpus version 13, 120 scenario files); all required applicable scenarios match, including authorization (allow/deny/ask-plain), displayInput UTF-16, and Tool-result status matrices. R7.2 implements no projection, approval system, mutation/process/Godot Tools, async runtime, threads, or locks. Stage 3R R7.3 Projection parity is complete and evidence-backed: the Rust projection/application integration suite passes 13 tests and the differential corpus holds 11 required `context-projection` scenarios. Stage 3R R7.4 Configuration parity is complete and evidence-backed: `siralos-adapters::config` owns bounded parsing/validation and `siralos-cli::configuration` owns composition; corpus version 15 holds 133 scenario files and the Windows audit matches 128/128 applicable required scenarios, with four explicit platform skips and one accepted informational deviation. Stage 3R R7.5 `/context` and `/tools` rendering is **complete and evidence-backed** (51 focused Rust CLI tests (10 sanitize) + `sanitize.rs` port of `apps/cli/src/output/sanitize.ts` — stateful `push`+`flush` in `interactive.rs` `drain_events`; advisory `09` closed): the real no-argument session composes the existing projection and Tool authority seams, renders deterministic sanitized snapshots, and preserves the byte-equal R7.5 rubric — R7 is **Verified** at 61fbf997d781. Stage 3R R8 Optional Godot Stage-2 parity is **complete and evidence-backed**: six surfaces (discovery/profiling, recovery contracts, version-bound API knowledge, GDScript check-only diagnostics, bounded LSP, read-only scene/resource intelligence) are ported across `siralos-core::godot` (typed models, limits, bounded scene/resource parsers) and `siralos-adapters::godot` (fail-closed runners/services); corpus version 16 holds 155 scenario files with all five frozen `godot-*` differential subjects (discovery ×4, knowledge ×5, diagnostics ×4, lsp ×4, scene-resolve ×5) at required parity — 150/150 applicable required scenarios — and the fail-closed posture is mechanically preserved (zero spawn paths in any Godot module). R8 is **Verified** at c075b3cf5e52. Stage 3R R9 Optional Godot Stage-3 parity is **complete and evidence-backed**: three surfaces (review context & impact intelligence, prepare-only scene/resource mutation contracts, the deterministic unified `/develop` core) are ported across `siralos_core::godot::{impact, scene_mutation, development}` and `siralos-adapters::godot::scene_mutation` (apply typed `unavailable`); corpus version 17 holds 167 scenario files with all three frozen subjects (`godot-review-context` ×4, `godot-mutation-prepare` ×4, `godot-develop-plan` ×4) at required parity — 162/162 applicable required scenarios. R9 is **Verified** at 1623e800f8034. Stage 3R R10 — H1/H2/ICM/H3 runtime-readiness parity — is **complete and evidence-backed** as one Verified milestone with three ordered, entry-reviewed sub-slices: R10a H1 content identity + H2 determinism/replay (`siralos_core::identity` extended with typed digests/deltas/staleness plus `siralos_core::determinism` — explicit clock/random/ordering ports, reproducibility manifests, deterministic decisions/discovery/doctor; corpus version 18, 182 scenario files, 177/177 applicable required parity), R10b ICM (`siralos_core::context` — PhaseContract with narrowing-only authority and `siralos:PhaseContract:v1\0` digest binding over the single artifact-digest primitive, digest-bound dependency manifests, targeted content-addressed staleness, provenance refs and deterministic why-diagnostics; corpus version 19, 195 scenario files, 190/190), and R10c H3 runtime readiness (`siralos_core::runtime` — causal run/operation ids, manifest-bound budgets with typed artifact admission, the pure supervisor transition table over the closed 13-kind failure taxonomy with one terminal disposition plus an independent cleanup status, the deterministic fake-process fault driver under the controlled clock, and the fail-closed readiness doctor; corpus version 20, 210 scenario files, 205/205). No real process is ever launched; effect boundaries stay typed `unavailable`; R10 is **Verified** at a456afb71ab64c5504cd19e8eb7988d32d60a9dc.
- The cross-cutting executor briefing and context discipline (ADRs 0022–0023) is implemented: a versioned Execution Contract, milestone manifests with stable acceptance IDs (S3M8 has a real manifest), an evidence-backed AcceptanceEvaluator, the Executor Context Pack, the deterministic Executor Brief Compiler, `/brief` / `/milestone` inspection, task `WorkspaceScope` with verified/candidate files and budgets, current-step `ActiveWorkingSet` with inclusion reasons, new-file rationale/proliferation signals, and deterministic documentation selection (root + scoped AGENTS.md, architecture index, accepted ADRs; archive/superseded excluded). Briefs reference the execution contract by revision and never restate permanent rules; milestone acceptance is satisfied only by host-observed evidence, never executor claims; workspace scope and documentation selection are derived context that never grants capability.
- Working read-only surfaces include the deterministic fake provider, bounded workspace list/read/search, static Godot installation and project inspection, local-directory references, denied-by-default bounded research adapters, self-reference, capability diagnostics, and the interactive CLI.
- Task contracts, snapshots, plans, evidence, provider requests, tool definitions, and public result values must be detached from caller-owned mutable data. Task state is host-owned; model completion is only a request evaluated by host gates.
- Provider streams and tool loops are bounded, protocol-checked, cancellation-aware, transcript-paired, and sanitized at the terminal boundary. Provider output and external content are always untrusted data.
- Planning is host-routed and structurally read-only. Plan approval binds only to the exact plan and task revisions and never grants edit, command, checkpoint, sandbox, or research authority.
- Project instructions, project knowledge, evidence/history, references, and research are separate authority classes. None may be promoted implicitly, and none may override capability or sandbox policy.

## Fail-closed execution posture

The following surfaces intentionally report `unavailable` and perform no filesystem mutation or process launch:

- workspace create/edit/delete application and safe undo;
- new checkpoint creation and automatic checkpoint pruning;
- private run-directory creation or cleanup;
- `node-script` and `npm-script` command execution;
- Git inspection;
- Godot engine probes, API-dump generation, recovery project probes, GDScript check-only diagnostics, and GDScript LSP startup;
- executable caches, Godot knowledge caches, recovery mirrors, and repository-reference materialization.

These capabilities remain unavailable because Node does not provide the directory-relative create/replace/delete or identity-bound executable launch primitives needed to resist same-user pathname substitution. Static preparation contracts and truthful diagnostics may exist, but they must refuse before approval, checkpoint creation, mirror/cache creation, deletion, or spawn.

Do not weaken this posture with another pathname recheck, hashing window, private filename, monkey patch, comment, warning, or documentation claim. A capability becomes available only when its security property is mechanically enforceable and covered by adversarial tests.

Historical checkpoint data may be inspected, but unverifiable or unexpected content blocks capacity checks and is never repaired or deleted automatically. The logical checkpoint byte limit counts exact metadata and preimage bytes; preimages are handle-bound, bounded, content-verified, and stability-checked.

The Anthropic Sandbox Runtime backend is pinned. Linux/macOS availability requires the enforced host-read allowlist and live conformance. Windows setup and host-read capability are distinct; the backend must never be reported generally executable when enforcement is unavailable. A skipped live probe is never a pass.

## Workspace and security rules

- Canonicalize the launch workspace once and contain every model-facing path within it. Never follow workspace symlinks for traversal.
- Behavioral configuration (`AGENTS.md` at any depth and `.siralos/**`) is protected and cannot be changed through ordinary workspace mutation capability.
- Capability policy, one-time digest-bound approval, sandbox enforcement, checkpointing, and stale-state checks are independent gates. Success at one gate never implies another.
- Keep external references outside the workspace namespace. `@reference/<alias>` is not a filesystem path.
- Research is disabled by built-in profiles unless explicitly authorized. `ask` is refused where no approval protocol exists.
- Architecture checks are developer guardrails, not an OS security boundary.
- Never expose absolute workspace, cache, mirror, executable, or credential paths to providers or report-safe output.
- The terminal sanitizer is the single output boundary, the input queue is the single interactive-read owner, and the command catalog is the single command-vocabulary source.
- Self-reference and doctor collection are read-only and offline by default: no refresh, live probe, repair, permission broadening, mutation, checkpoint, or secret-bearing report. `ToolProjector` remains authoritative for model-visible tools and `SandboxBackend` for enforcement capability.
- Keep fixed Godot invocation tuples inside their architecture-owned runner modules. Project-independent probes never accept project arguments; recovery, check-only, and LSP-only flags remain structurally paired even while every runner is unavailable.

## Verification

- `npm run check` — format check (prettier, minimal), lint (eslint, minimal), doc links, project-context, identity, public-hygiene, Rust architecture check, and the full Rust gate (fmt, clippy with warnings denied, tests, pinned differential).
- `npm run check:identity` — no project-owned file may use the former identity (narrow documented exclusions only).
- `npm run check:rust` — Rust crate shape, dependency direction, binary identity, edition/toolchain/formatting policy, core domain neutrality, unsafe backstop.
- `npm run check:differential` — the differential behavioral harness (ADR 0033) in **pinned mode** (decision 40): runs the scenario corpus against the **pinned** TypeScript oracle (`tests/differential/evidence/typescript-freeze-v32/oracle.json`) and the Rust candidate, compares canonical outcome records, and emits the migration audit report (exit 0 = parity, 1 = deviation, 2 = harness error). Live oracle requires worktree at freeze SHA (`5da5cde`) and is never part of `check`.
- `npm run build:domain-conformance` — rebuilds the synthetic conformance domain components (Stage 3R R6) and refreshes the checked-in fixture bytes; run it whenever the production WIT or a conformance guest changes, and update the fixture digest constants in the same change.
- `npm run check:rust-format` / `npm run check:rust-clippy` / `npm run test:rust` — `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`.
- `npm run siralos` — build and launch the Rust CLI (`cargo run --locked --bin siralos -- --help` / `--version`).

Run checks relevant to each cohesive change before committing, then run `npm run check` before handoff. Use small Conventional Commit-style commits; do not put an entire multi-boundary task in one commit.

## Intended direction

- The lean product, composition, and extension model
  ([ADR 0036](docs/adr/0036-lean-product-composition-and-extension-model.md))
  governs future-facing work; read it (and
  [PROJECT_CONTEXT.md](docs/development/PROJECT_CONTEXT.md)) before
  product/architecture decisions. Multi-agent machinery, general Hooks,
  TaskGraph, workflow engines, marketplaces, and automatic acquisition
  are not committed.
- Siralos is a minimal, declarative AI coding harness with an inspectable execution environment, a domain-neutral core, and explicitly installed optional domain intelligence (Godot is the first and only optional domain).
- The TypeScript implementation was the behavioral reference (archived at `5da5cde`, pinned at `tests/differential/evidence/typescript-freeze-v32/`); later Stage 3R milestones ported subsystems to idiomatic Rust under behavioral parity, refactoring-during-port, and evidence-driven optimization rules (ADR 0032, `docs/development/RUST_STYLE.md`). Rust is now the sole source of truth (decision 40).
- Current: Stage 3R R12 is **Verified (retired)** at 4bef901 (executable 72e20be). R1–R13 all Verified. Stage 7 externalization is **complete** per decisions 60–65: Godot domain + host adapters moved to standalone `https://github.com/CrimsX/siralos-godot` at `1bf2ca3` (was `crates/siralos-godot` at `5da5cde`–`5da2cab`, 41 files + host adapters at `1bf2ca3` self-contained, `siralos-godot → siralos-core` only via `path = "../siralos/crates/siralos-core"`), pinned in the monorepo as `siralos-godot = { path = "../siralos-godot" }` (3-member workspace, external path dep, `87bfd35` 3 members, `1bf2ca3` 234 tests). **Stage 6 is Verified** at `e2c3540` (315/315 at `v52`/320, pinned v32 oracle untouched). Real Model/Provider is **Verified** per decisions 66–71 (roll-up 71): `ProfileRecord` provider/model/credential/endpoint (67 C1), `GenericProvider` all-purpose with `reqwest` blocking and provider-neutral placeholder defaults, `HostCredential` env-only (68), `siralos.toml` `[profile]` provider/model/credential/endpoint, replay recording via the determinism ports with typed availability and the recorded 68 §4 secret-hygiene sweep (70), `provider-generic` subject at `v53` `959915f875` (differential 316/316 applicable required, pinned v32 oracle untouched), and the provider-replay subject at v54 (differential 317/317 applicable required, 83 expectation records, pinned v32 oracle untouched), Real Provider follow-through **Verified** per decisions 72–75 (roll-up 76: the standing check:secrets guardrail gate plus the replay arc — recording (70), playback (73), composition seam (74), v54 corpus pinning (75)), and session replay-run composition (in-process record-then-replay, nothing persisted) is complete per decision 77 (corpus v55/323 files, audit 318/318 applicable required, 84 expectation records, pinned v32 oracle untouched), and cross-session replay — the bounded recordings store amending decision 73 under the mechanical hygiene contract — is complete per decision 78 (store B1; [profile] record-replay/replay session composition B2; replay-store subject at v56/324 files, audit 319/319 applicable required, 85 expectation records, pinned v32 oracle untouched — B3), and context management foundations (the Context Graph: reconstructable nodes/edges over the existing digest and staleness seams, no persisted store) are complete per decision 79 slice 1 (corpus v57/325 files, audit 320/320 applicable required, 86 expectation records, pinned v32 oracle untouched), and layered representations over the graph (L0-L4 additive store, L2 host-only enforced) are complete per decision 80 slice 2 (corpus v58/326 files, audit 321/321 applicable required, 87 expectation records, pinned v32 oracle untouched), and the deterministic tiered scheduler (HOT/WARM/COLD under synchronous ticks with the deterministic 4096-token budget) is complete per decision 81 slice 3 (corpus v59/327 files, audit 322/322 applicable required, 88 expectation records, pinned v32 oracle untouched), and the read-only context demand-paging tools (inspect/search/expand over immutable snapshots, never mutating scheduler state) are complete per decision 82 slice 4 (corpus v60/328 files, audit 323/323 applicable required, 89 expectation records, pinned v32 oracle untouched), and the benchmark decision gate measured NO-GO per decision 83 slice 5 (recall parity 14/14 held but aggregate token reduction 47.2% missed the pre-committed 50% bar; corpus v61/329 files, audit 324/324 applicable required, 90 expectation records, pinned v32 oracle untouched; the audit and hygiene-metrics seam is not justified by the measured evidence), and the program closure record (decision 84) disposed the deferred live-session registration as precondition-gated future work and closed the Context Management Foundations program, and the paged strategy v2 re-benchmark (decision 85) measured GO with the same pre-committed bar (58.5% token reduction at recall parity 14/14, dedup-dominated savings; corpus v62/329 files, audit 324/324 applicable required, 90 expectation records, pinned v32 oracle untouched), superseding the decision 83 NO-GO and un-gating the audit and hygiene-metrics seam as a candidate next slice, and the v3 heterogeneous re-gate (decision 86) measured NO-GO on honest fixtures (paged 3012 vs baseline 2286, recall 14/14, 9/9 sensitivity cells failing the margin; corpus v63/329 files, audit 324/324 applicable required, 90 expectation records, pinned v32 oracle untouched), overturning the decision 85 GO, returning the audit and hygiene-metrics seam to gated status, and exposing the baseline-definition and expansion-priority corrections the next methodology revision must address, and the corrected-baseline re-gate (decision 87) — externally signed off before the run — measured GO (paged 3012 vs DeepAll 7609, 60.4% reduction at recall parity, dedup share 0%, 9/9 sensitivity cells; corpus v64/329 files, audit 324/324 applicable required, 90 expectation records, pinned v32 oracle untouched), restoring a weak-but-honest efficiency claim that does not endorse the expansion policy pending the decision 88 escalation-policy pre-commit, and the escalation-policy test (decision 88) — externally signed off before the run with answer-aware depth-aware recall scoring — measured Retained (top-1 escalation 13/14 depth-aware recall vs V2's 14/14 at 1844 vs 3012 tokens; corpus v65/329 files, audit 324/324 applicable required, 90 expectation records, pinned v32 oracle untouched), retaining the ProgressiveV2 expansion policy as the measured cost-of-depth tradeoff and leaving runtime semantics unchanged pending the scheduler-semantics re-pin, and the scheduler-semantics re-pin (decision 89) adopted the four amendments — host-owned demand edge (TickInput events <= 64 canonical, touched nodes recency=now relevance+32 saturating), digest-counted 4096 budget, the 1024 pinned-HOT quota with stale-never-promote, and tick coalescing — re-pinning the decision 81 rules with the frozen clauses intact, the tools still read-only, the benchmark verdicts byte-identical, and live registration still precondition-gated, and the consumer slice (decision 90) completed the loop — HOT assembles L1 summaries by default with deeper levels reachable only through the read-only expand tool and the demand edge (the decision 88 curve finding rules out rank-only auto-escalation), hot nodes carry a bounded 1-hop neighbor map of ids+digests counted in the budget, and the end-to-end demand composition is proven as pure composition while live activation stays precondition-gated, and the metrics counters (decision 91) added pure in-memory observability — saturating u64 counters and a capped 64-record tick ring derived deterministically from tick inputs/outputs — with the tick pipeline unchanged, nothing persisted, the benchmark verdicts byte-identical, and live activation still precondition-gated, and the search-scoring re-rank (decision 92) amended the pinned decision 82 ordering with the closed scoring table (stopword-filtered terms, whole-word +10 summary / +4 id, substring +3/+1, tier +8 HOT/+4 WARM, kind +3 Knowledge/+1 Source, score descending with node_id-ascending tiebreak) and intentionally re-measured the benchmark under the unchanged decision 87/88 rules with the re-measured verdicts recorded, and the score-ordered retention (decision 93) resolved the re-gate without touching the benchmark — search score feeds only the assembly demotion order (rank-for-retention, not rank-for-depth), the benchmark record stays byte-identical to v69, and the decision 88 Retained outcome and 87/92 verdicts stand untouched, and the sensitivity-sweep rule (decision 94) recorded the externally-signed NO-REVISION ruling — the 9-cell estimator sweep stays gated, the primary-cell-only relaxation is rejected, and the real-tokenizer calibration is admitted only as informational evidence prerequisite for any future estimator-range argument, and the context system activation entry review (decision 95) was approved by the user (C1–C6) authorizing the activation arc — B1 bounded read-only workspace scan first, B2 node-taxonomy review as its own HITL slice, B3 default-off [profile.context] session wiring, B4 the decision 91 ring as the live audit trail, with the adversarial invariants gating every slice as pass/fail fixtures and the fail-closed posture untouched, and activation slice B1 (decision 96) delivered the bounded deterministic read-only workspace scan — pinned bounds (256 nodes, 65536 bytes per file), lexicographic ordering, protected and oversized paths skipped and counted, digest nodes over the existing identity primitive, typed unavailability, provisional graph binding awaiting the B2 taxonomy review, staleness deferred to session state, and activation slice B2 (decision 97) pinned the node taxonomy — Source (default), Decision (docs/adr/** and docs/wayfinder/decisions/**), Knowledge reserved with no producer — with classification at graph-binding time only, the B1 scan byte-unchanged, the decision 92 kind weights untouched, and L0-only binding pending the B3 summary-generation decision, and activation slice B3a (decision 98) delivered deterministic L1 summary generation — scanned nodes carry L0 digests plus L1 summaries from the R5 advisory summary formatter under existing bounds, composed into a host-side WorkspaceContext over the decision 80 store seam with the dedup property pinned and nothing persisted or wired, and activation slice B3b (decision 99) wired the subsystem into the session behind the default-off [profile.context_system] opt-in — the demand loop live end-to-end (tool round -> observations -> tick -> working set), byte-transparent when absent, fail-closed on build failure, and free of persistence and rendering, and activation slice B4 (decision 100) completed the arc — the decision 91 ring and counters render as the live audit trail inside /context for opted-in sessions (bounded counters block plus the last 8 tick records through the terminal sanitizer), byte-transparent when absent, and the context system activation roll-up (decision 101) verified the arc complete — B1-B4 as authorized, the adversarial invariants held, the OFF path byte-transparent at every layer, the benchmark record byte-identical from v69 through v75, the demand loop live behind the default-off opt-in with the bounded live audit surface, and the arc closed with deferred items recorded, and the provider-interface takeaways (decision 102) added measurement-only machinery from the Meta Model API docs review — the pre-committed cache-stability rule, provider usage capture into recordings, and the e1 informational comparison tool under the decision 94 rule with the gate untouched, and the Siralos TUI entry review (decision 103) was approved by the user (C1–C6, ratatui + crossterm in the CLI crate) authorizing the TUI arc — a frontend over the existing session seam composing the sanitizer, input-queue, and command-catalog seams unchanged, with slices T1 shell, T2 approvals, T3 the context pane, and T4 render-model pinning, and TUI slice T1 (decision 104) delivered the shell — ratatui + crossterm pinned in the CLI crate, the pure deterministic render model headlessly tested over TestBackend, the session seam composed with the sanitizer boundary intact, --tui opt-in with non-TTY fallback, and the stdio path byte-unchanged, the TUI became the default frontend on TTYs with a --stdio escape hatch (decision 105), and T2 delivered approval modals over the unchanged host gates with the dispatch-duplication consolidation started (decision 106), and T3 delivered the context pane — the decision 100 audit and tool activity as a live right-hand pane single-sourced from the ContextMetrics the /context segment uses, byte-transparent when the subsystem is off (decision 107), and T4 pinned the render model into the differential corpus (four TestBackend frame snapshots at corpus v76) and settled the dispatch debt — single shared definitions both loops call with any per-frontend residual recorded (decision 108), and the Siralos TUI roll-up (decision 109) verified the arc complete — T1-T4 as authorized, the consolidation debt settled to the permanent terminal-I/O residual, the render model pinned in the differential corpus, and the TUI the default frontend on TTYs with --stdio as the escape hatch, and the TUI usability pass (decisions 110-111) fixed the user-reported keystroke redraw starvation and command-vocabulary silence, adding the / command palette, UTC message stamps, the status provider/model line, the /provider + /model display commands, and the /evolve discovery command wiring the Stage 6 evolution surfaces into the session vocabulary, and the audit remediation pass (decisions 112-113) fixed the external-model audit's two real defects — context.inspect rejecting every nested-path node id and the double-applied scroll — plus the catalog/echo/status hygiene, the harness gate mirror, and the docs staleness sweep, with six HITL questions awaiting direction, and the audit HITL rulings (decision 114) resolved all six — approvals dormant as future-seam, the staleness policy and the sanitizer boundary documented, unknown-command honesty canonical in both frontends through one shared definition, the fourth atomic writer documented, and the session lock documented as authority-only — closing ticket 105, and the neighbor-expansion experiment (decision 115) measured a NULL result — the fixture corpus lacks hit→key graph edges so the candidate mechanism admitted zero candidates and the paraphrase boundary remains unmeasured, the gate re-measuring NO-GO byte-identical to the decision 92 record (corpus v78/357, audit 352/352 applicable required, 118 expectation records, pinned v32 oracle untouched), fresh full-gate exit 0. The Godot Runtime Adapter entry review is **PASS** per decision 41 (HITL 2026-08-28: C1–C6 approved, Stage 4.3 numbering kept, subjects `godot-runtime-launch` ×5 + `godot-runtime-evidence` ×4 at v34, nothing flips `unavailable`; authorized as next implementation slice). **Stage 4.3 is complete** at `5bedf57` (`siralos-godot::runtime_adapter` — `decide_godot_launch` over the generic decision order + Godot-scoped evidence with domain-separated godot digest; corpus v34/248 files, audit 243/243 applicable required; the 9 post-freeze scenarios are covered by digest-bound candidate-authored expectations at `tests/differential/evidence/post-freeze/expectations.json` per decision 40 C7 with additive `expectationCoverage` audit provenance; zero spawn paths preserved). The Stage 4.4 Visual Evidence entry review is **PASS** per decision 42 (HITL 2026-08-28: C1–C6 approved, sequence step 4, subject `visual-evidence` ×4 at v35, nothing flips `unavailable`; authorized as next implementation slice). **Stage 4.4 is complete** at `4a250d8` (`siralos-core::runtime::visual` — bounded digest-bound capture-evidence over the generic decision order; capture primitive absent → typed `UNAVAILABLE`; headless capture a typed pairing refusal; corpus v35/252 files, expectations 13 records, audit 247/247 applicable required; zero spawn paths preserved). The Stage 4.5 Controlled Interaction entry review is **PASS** per decision 43 (HITL 2026-08-28: C1–C6 approved, sequence step 5, run-interaction-rounds framing, subject `run-interaction` ×4 at v36, nothing flips `unavailable`; authorized as next implementation slice). **Stage 4.5 is complete** at `42ee5ab` (`siralos-core::runtime::interaction` — bounded interaction rounds over the generic decision order; interactive primitive absent → typed `UNAVAILABLE`; one-shot runs a typed pairing refusal; corpus v36/256 files, expectations 17 records, audit 251/251 applicable required; zero spawn paths preserved). The Stage 4.6 QA Workflows entry review is **PASS** per decision 44 (HITL 2026-08-28: C1–C6 approved, sequence step 6, bounded-workflow-steps framing, subject `qa-workflow` ×4 at v37 with the corpus-cap raise `256 → 384`, nothing flips `unavailable`; authorized as next implementation slice). **Stage 4.6 is complete** at `a83c2a4` (`siralos-core::runtime::qa` — bounded ordered QA workflow steps over the generic decision order; workflow execution primitive absent → typed `UNAVAILABLE`; zero-step workflows a typed pairing refusal; corpus v37/260 files, expectations 21 records, audit 255/255 applicable required; zero spawn paths preserved). The Stage 4.7 Profiling entry review is **PASS** per decision 45 (HITL 2026-08-28: C1–C6 approved, the final frozen-sequence arrow step 7, run-profiling-sessions framing, subject `run-profile` ×4 at v38, nothing flips `unavailable`; authorized as next implementation slice). **Stage 4.7 is complete** at `b206a4a` (`siralos-core::runtime::profiling` — bounded run-profiling sessions over the generic decision order; profiling primitive absent → typed `UNAVAILABLE`; zero-sample sessions a typed pairing refusal; corpus v38/264 files, expectations 25 records, audit 259/259 applicable required; zero spawn paths preserved). **The frozen 7-step Stage-4 sequence (decision 08) is fully consumed** — steps 1–7 all implemented, entry-reviewed, and evidence-backed; the next frontier is per the map's _Not yet specified_ section (Stage 5 composition is future-facing under ADR 0036, not committed). The Stage 4 Verified roll-up is **PASS** per decision 46 (HITL 2026-08-28: C1–C6 approved, seven realized steps as drafted, six verification criteria with fresh gate runs, pure closure record — no behavior change). **Stage 4 is Verified** at `9566eee` — fresh full-gate run (differential 259/259 applicable required at corpus v38/264 files, expectations 25 records, pinned v32 oracle untouched), spawn sweeps clean, core neutrality green, decisions 41–46 annotated; **the next frontier is per the map's _Not yet specified_ section** (Stage 5 composition is future-facing under ADR 0036, not committed). The Stage 5.1 Profiles entry review is **PASS** per decision 47 (HITL 2026-08-28: C1–C6 approved; Stage 5 — Composition graduated into the map with Profiles as the first slice — the composition unit per ADR 0036 §6, narrowing-only; subject `composition-profile` ×4 at v39; authorized as next implementation slice). **Stage 5.1 is complete** at `be030e3` (`siralos-core::composition` — the Profile record and `resolve_profile_overlay` with the mechanical narrowing-only property (Deny < Ask < Allow); widening → typed refusal; absent profile → typed default; `siralos-adapters::profile_config` — bounded TOML `[profile]` parsing; corpus v39/268 files, expectations 29 records, audit 263/263 applicable required; zero spawn paths preserved; no CLI command and no `siralos.lock` in this slice). The Stage 5.2 Profile Composition entry review is **PASS** per decision 48 (HITL 2026-08-28: C1–C6 approved; complete the ADR 0036 §6 resolution chain — effective run configuration over Host authority, subject `composition-effective` ×4 at v40; bad profiles not applied with truthful diagnostics). **Stage 5.2 is complete** at `4c562c8` (`siralos-core::composition` — `DeclaredProfile` + `compose_effective_policy` with the invariant re-checked at the composition boundary; `siralos-adapters::profile_config` — `load_workspace_profile` over the shared workspace `siralos.toml`; `siralos-cli` — the session composes the workspace profile at startup; corpus v40/272 files, expectations 33 records, audit 267/267 applicable required; zero spawn paths preserved; no `siralos.lock` in this slice). The Stage 5.3 Context Controls entry review is **PASS** per decision 49 (HITL 2026-08-28: C1–C6 approved; Live/Pinned/Frozen visibility over content identity on the R10b `siralos-core::context` seam, subject `context-controls` ×4 at v41; absent control = Live, Pinned-stale usable but labelled, Frozen-stale blocked). **Stage 5.3 is complete** at `ce3e7dc` (`siralos-core::context::controls` — `ContextPolicy` + `evaluate_context_policy` + digest-bound `ContextControlEvidence`; a control can only narrow what Siralos claims about content; corpus v41/276 files, expectations 37 records, audit 271/271 applicable required; zero spawn/fs paths; no adapters/CLI surface in this slice). The Stage 5.4 siralos.lock Resolution entry review is **PASS** per decision 50 (HITL 2026-08-28: C1–C6 approved; machine-generated resolved portable identities per ADR 0036 §8-9 over the established atomic-write pattern, subject `composition-lock` ×4 at v42). **Stage 5.4 is complete** at `0a6d592` (`siralos-core::composition::lock` — deterministic idempotent `create_workspace_lock`; `siralos-adapters::lockfile` — load/write/verify over the shared atomic-write pattern, load re-derives the lock digest, verify compares recomputed digests never file bytes; corpus v42/280 files, expectations 41 records, audit 275/275 applicable required). The Stage 5.5 Plugin Selection by Profiles entry review is **PASS** per decision 51 (HITL 2026-08-28: C1–C6 approved; narrowing-only domain activation over the decision 38/39 surfaces, subject `composition-plugin-selection` ×4 at v43). **Stage 5.5 is complete** at `5e1b3e0` (`ProfileRecord.plugins` + `select_profile_plugins` — the intersection property makes widening impossible by construction; digest-bound evidence; corpus v43/284 files, expectations 45 records, audit 279/279 applicable required; session application waits for its own slice). The Stage 5.6 Skills entry review is **PASS** per decision 52 (HITL 2026-08-28: C1–C6 approved; declarative digest-bound model guidance with no authority per ADR 0036 §22, subject `composition-skills` ×4 at v44). **Stage 5.6 is complete** at `fcf61c5` (`siralos-core::skills` — digest-bound evidence whose payload binds `authority = none`; opt-in intersection selection, order-independent resolution; `siralos-adapters::skills_loader` — bounded read-only catalog over `.siralos/skills/*.md`; corpus v44/288 files, expectations 49 records, audit 283/283 applicable required; zero spawn paths; no registry, no profile schema change). The Stage 5.7 Session Plugin Activation Gate entry review is **PASS** per decision 53 (HITL 2026-08-29: C1–C6 approved; the frozen 5.5 filter gates `/domains-activate`, completing decision 51's deferred consumption; subject `composition-plugin-activation` ×4 at v45). **Stage 5.7 is complete** at `926ac71` (`siralos-core::composition` — `decide_plugin_activation` with Host-authority precedence and digest-bound `PluginActivationEvidence`; `siralos-cli` — the session holds the applied profile's selection and `/domains-activate` refuses filtered ids before any side effect; corpus v45/292 files, expectations 53 records, audit 287/287 applicable required; zero spawn paths, gate transparent without an applied profile). The Stage 5.8 Session Context Controls entry review is **PASS** per decision 54 (HITL 2026-08-29: C1–C6 approved; the frozen 5.3 visibility seam is consumed by the applied profile and gates the session's `/context` claims; subject `composition-context-control` ×4 at v46). **Stage 5.8 is complete** at `6dc830e` (`siralos-core::composition` — `decide_context_control` over the frozen 5.3 evaluation with absent-control transparency and digest-bound `ContextControlDecisionEvidence`; `siralos-adapters::profile_config` — the additive `[profile.context]` key, malformed controls leave the profile unapplied; `siralos-cli` — `/context` labels Pinned-stale claims, refuses Frozen-stale ones, stays byte-transparent otherwise; corpus v46/296 files, expectations 57 records, audit 291/291 applicable required; zero spawn/fs-write paths). The Stage 5.9 Session Lock Verification entry review is **PASS** per decision 55 (HITL 2026-08-29: C1–C6 approved; the on-disk lock is verified at session startup against the recomputed current lock; the lock never gates authority; subject `composition-lock-verify` ×4 at v47; authorized as next implementation slice). **Stage 5.9 is complete** at `6e38804` (`siralos-core::composition` — `decide_lock_verification` over the `StoredLockDigest` states with digest-bound `LockVerificationEvidence`; `siralos-cli` — `verify_session_lock` recomputes the current lock from the applied profile's effective-policy identity and the installed plugin records and reports drift or untrusted content as a host-side startup diagnostic; corpus v47/300 files, expectations 61 records, audit 295/295 applicable required; zero spawn/fs-write paths; the lock never gates authority — unit- and session-proven). The Stage 5.10 Session Skill Consumption entry review is **PASS** per [decision 56](docs/wayfinder/decisions/56-stage5-10-session-skill-consumption-entry-review.md) (HITL 2026-08-29: C1–C6 approved; the frozen 5.6 skill seam is consumed by the applied profile via an additive `[profile.skills]` key; bound guidance surfaces in the system-instructions segment, guidance-only, never authority; subject `composition-skill-consumption` ×4 at v48; authorized as next implementation slice). **Stage 5.10 is complete** at `579f1e9` (`siralos-core::composition` — `compose_skill_consumption` over `SkillCatalogState` with digest-bound `SkillConsumptionEvidence` whose payload binds `authority = none` transitively and outcomes none/bound/unknown; `siralos-adapters::profile_config` — the additive `[profile.skills]` key, malformed values leave the profile unapplied; `siralos-cli` — bound guidance surfaces as the bounded `workspace-skills` system-instructions segment sorted by name, unknown names reported truthfully, absent selection/catalog byte-transparent; corpus v48/304 files, expectations 65 records, audit 299/299 applicable required; zero spawn/fs-write paths; the guidance-only property unit- and session-proven). **The next frontier is a Stage 5 Verified roll-up** (Stage 5.10 was the last unconsumed Stage 5 seam; all six — profiles, plugin selection, lock, context controls, lock verification, skills — are now consumed). The Stage 5 Verified roll-up is **PASS** per [decision 57](docs/wayfinder/decisions/57-stage5-verified-roll-up.md) (HITL 2026-08-29: C1–C6 approved, ten realized slices as drafted, six verification criteria with fresh gate runs, pure closure record — no behavior change). **Stage 5 is Verified** at `c2c30f0` — fresh full-gate run (differential 299/299 applicable required at corpus v48/304 files, expectations 65 records, pinned v32 oracle untouched), spawn sweep clean, core neutrality green, decisions 47–57 annotated; **the map's Not-yet-specified section has no open fog** — any next work starts with a new ticket + entry review per ADR 0036's lean model. The Stage 6 Evolution & Stabilization entry review is **PASS** per decision 58 (HITL 2026-08-30: C1-C6 approved; bounded `/evolve` with 6.1-6.4 slices, escalation Profile→Host, evidence over pure seams; authorized as next frontier). **Stage 6.1 is complete** at `a79f613` (`siralos-core::evolution` corpus + `evolve-corpus` ×4, v49/308, 303/303, 69 expectations), **Stage 6.2 is complete** at `0ba256f` (workflow + `evolve-workflow` ×4, v50/312, 307/307, 73 expectations), **Stage 6.3 is complete** at `ddb18a4` (proposal + `evolve-proposal` ×4, v51/316, 311/311, 77 expectations), **Stage 6.4 is complete** at `e2c3540` (release + `evolve-packaging` ×4, v52/320, 315/315, 81 expectations). The four-stage Stage-6 sequence is fully consumed. The Stage 6 Verified roll-up is **PASS** per decision 59 (HITL 2026-08-30: C1-C6 approved; four slices as drafted, six verification criteria with fresh gate runs, pure closure record - no behavior change). **Stage 6 is Verified** at `e2c3540` - fresh full-gate run (differential 315/315 applicable required at corpus v52/320, expectations 81 records, pinned v32 oracle untouched), spawn sweep clean, core neutrality green, decisions 58-59 annotated; **the map's
  Not-yet-specified section has no open fog** - any next work starts with a new ticket + entry review per ADR 0036’s lean model.
- Do not add persistence, multi-agent functionality, or `/evolve` execution outside their planned milestones — `/evolve` display-only discovery exists since decision 111 while execution remains host-gated/out-of-milestone; real provider integrations are **Verified** per decisions 66–71 (user-directed 2026-08-31) — the earlier AGENTS.md “no real provider” bullet is superseded.
