# Golden trace verification registry

Status: authoritative scenario registry.

The `GT-*` identifiers describe representative end-to-end behavior. A row is
`VERIFIED` only when executable evidence covers the complete scenario. Unit
pieces or an unavailable production effect are `PARTIAL`; later-stage
scenarios are `NOT DUE`. This registry does not store generated run output.

| ID     | Scenario                                                                                                                                                                         | Status   | Evidence or due milestone                                                                                                                                                                                                                                                       |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GT-001 | Read-only repository question; zero mutation.                                                                                                                                    | VERIFIED | Read-only workspace/tool-loop behavior suites and workspace-integrity checks.                                                                                                                                                                                                   |
| GT-002 | Single-file patch; verification succeeds; one committed delta.                                                                                                                   | PARTIAL  | Internal change-set tests exist; production mutation is fail-closed pending enforceable primitives (SECURITY.md).                                                                                                                                                               |
| GT-003 | Stale revision; delta rejected before write.                                                                                                                                     | VERIFIED | `workspace-revision` differential scenarios (`workspace-revision.bounded`/`isolation`/`registry`, `checkpoint` staleness) and `siralos-core::workspace::revision` / `siralos-core::context::staleness` unit tests (historical TypeScript behavior test, archived at `5da5cde`). |
| GT-004 | Multi-file transaction; second mutation fails; complete rollback.                                                                                                                | PARTIAL  | R4 transaction/checkpoint recovery contracts are tested internally; production mutation and checkpoint creation are `unavailable` (SECURITY.md).                                                                                                                                |
| GT-005 | Godot patch; static validation passes; headless validation passes.                                                                                                               | NOT DUE  | Godot headless execution is `unavailable` by design: the runtime adapter, visual, interaction, QA and profiling slices are complete (decisions 41-45) and still refuse before spawn. A boundary, not sequencing.                                                                |
| GT-006 | Godot patch; headless validation fails; rollback + diagnostic.                                                                                                                   | NOT DUE  | Same boundary as GT-005: no Godot execution path exists to fail and roll back.                                                                                                                                                                                                  |
| GT-007 | `project.godot` present but domain not installed; no activation.                                                                                                                 | VERIFIED | R6 `domain-lifecycle.no-implicit-activation` differential scenario and the core workspace-scan semantics: workspace contents are opaque, and no installation/enablement/activation/download/recommendation side effect exists.                                                  |
| GT-008 | Godot installed but disabled; no activation.                                                                                                                                     | VERIFIED | R6 `domain-lifecycle` differential scenarios: installed != enabled and no activation without an explicit enable.                                                                                                                                                                |
| GT-009 | Explicit install + enable + successful domain activation.                                                                                                                        | VERIFIED | R6 lifecycle plus `/domains-activate` (decision 39) and the profile's narrowing gate (decision 53).                                                                                                                                                                             |
| GT-010 | Repository prompt-injection string is treated as untrusted data.                                                                                                                 | VERIFIED | Instruction, projection, terminal sanitization, and adversarial behavior tests.                                                                                                                                                                                                 |
| GT-011 | Network tool request denied under offline policy.                                                                                                                                | PARTIAL  | Policy, unit and conformance tests exist; a skipped live sandbox probe is not a pass (SECURITY.md), so the offline-policy trace stays partial.                                                                                                                                  |
| GT-012 | Secret-consuming tool works without secret entering model context.                                                                                                               | NOT DUE  | No credential-consuming TOOL exists: the provider path is Verified (decisions 66-71) and credentials never enter context (HAR-037), but the scenario needs an out-of-band tool that is not committed.                                                                           |
| GT-013 | Oversized process output is deterministically truncated/referenced.                                                                                                              | PARTIAL  | Output bounds and truncation semantics are unit-tested (HAR-016); production command execution is `unavailable`, so the end-to-end trace stays partial.                                                                                                                         |
| GT-014 | Repeated failed proposal triggers bounded-loop termination.                                                                                                                      | VERIFIED | Provider/tool/planner/repair progress and stuck-detection behavior suites.                                                                                                                                                                                                      |
| GT-015 | Recorded replay runs offline and produces expected final state digest.                                                                                                           | VERIFIED | Replay store, record/replay providers and the bounded replay arc (decisions 70, 73-78); R10/R11 Verified.                                                                                                                                                                       |
| GT-016 | External concurrent edit creates revision conflict instead of overwrite.                                                                                                         | PARTIAL  | Revision and unexpected-change detection is verified (workspace-revision scenarios, HAR-012); the overwrite path it guards is fail-closed.                                                                                                                                      |
| GT-017 | Process cancellation leaves recoverable state.                                                                                                                                   | PARTIAL  | Cancellation and lifecycle contracts are adversarial-tested (HAR-025); production process execution is `unavailable`.                                                                                                                                                           |
| GT-018 | Resume persisted task after runtime restart.                                                                                                                                     | NOT DUE  | Durable persistence is deliberately not committed (ADR 0036); resumable tasks would need it.                                                                                                                                                                                    |
| GT-019 | Provider proposes a Tool call; Host gates authorize/deny it; exactly one paired result reaches the transcript; the provider recovers or the prompt terminates deterministically. | VERIFIED | R7.2 `tool-loop` differential scenarios plus the Core/adapter security suites: unknown/hidden/deny/ask-plain never execute, allow executes exactly once, invalid and cancelled calls pair exactly one result, and no automatic retry exists.                                    |

## Reconciliation (2026-09-12)

A read-only sweep of every Markdown file (ticket 131) found rows here waiting on
milestones that are now Verified, and reasons citing a TypeScript reference that no
longer exists (decision 40). Every status and evidence cell was reconciled against
the Verified record; the scenario text is normative and unchanged.

The distinction that matters: a trace needing a real EFFECT (mutation, checkpoint,
process, Godot execution) cannot be promoted past `PARTIAL`/`NOT DUE` by any amount
of work while that effect is `unavailable` by design (SECURITY.md, fail-closed
posture) -- it is a boundary, not a queue.

## Promotion rule

Promoting a trace requires a stable fixture, authoritative input identities,
typed expected observations/outcome, deterministic bounds, effect-boundary
checks where applicable, and CI execution on the platforms claimed. A skipped
probe or a fake-only path cannot promote a live effect scenario to `VERIFIED`.
