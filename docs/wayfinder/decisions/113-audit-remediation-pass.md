---
title: "The Audit Remediation Pass"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "105"
supersedes: []
---

# 113 — The Audit Remediation Pass

Ticket [105](../tickets/105-audit-remediation-pass.md) · entry review [112](112-audit-remediation-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The mechanical batch from the external-model project audit implemented: the context.inspect nested-path defect (HIGH — inspect rejected every real workspace path and its demand events never fired), the double-scroll defect, the single catalog, the echo/status sanitizer gaps, the compose ordering, the harness gate mirror, and the docs staleness sweep; the six HITL questions (approval wiring, snapshot staleness, unknown-command canonical behavior, the fourth atomic writer, provider-identity-in-lock, stderr scoping) are presented OPEN in decision 112 and await user direction.

## 2. Implemented R1–R9 — as delivered

| ID  | Implementation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Evidence / note                                                                                                                                                                                                             |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | **inspect slash fix (HIGH):** `parse_node_id_input` now allows `/` matching `parse_expand_input` bounds (non-empty, `<= 256` bytes, still rejecting NUL); `parse_expand_input` aligned to reject `\` and NUL as well; `\` remains rejected for path-traversal hygiene. Regression test scans a nested fixture (`a/b.txt` / `nested/a/b.txt`) then `context.inspect` by its real slash path succeeds; demand events fire on `Success`.                                                                                   | `crates/siralos-adapters/src/tool/context.rs:573–657` patched; tests `inspect_accepts_nested_path_slash`, `inspect_rejects_backslash_and_nul_but_allows_slash`, `inspect_slashed_id_end_to_end_via_nested_fixture`          |
| R2  | **double-scroll fix:** deleted the TUI loop inline `PageUp`/`PageDown` arms (`interactive.rs ~1940–1969` -> no second apply); kept `handle_key` (`tui.rs:1279–1286`) as the sole `+-10` applier; viewport clamp moved into `handle_key` via `viewport_height: u16` param (`max_scroll`); inline arms removed, single `+-10` per press, clamped.                                                                                                                                                                         | `crates/siralos-cli/src/tui.rs` `handle_key` now `fn handle_key(state, key, viewport_height)`; `interactive.rs` loop computes `viewport = h.saturating_sub(2)` and calls once; test `single_scroll_clamped_and_single_step` |
| R3  | **single catalog:** `tui::command_catalog` now delegates to `interactive::slash_command_catalog` (single source; owned `String` conversion keeps palette type stable) + cross-equality test. The cleaner direction is `tui` -> `interactive` (no leak, types map via `to_owned`).                                                                                                                                                                                                                                       | `crates/siralos-cli/src/tui.rs:330–337` delegates; tests `catalog_cross_equality_single_source` (tui) and `catalog_cross_equality_from_interactive` (interactive)                                                           |
| R4  | **echo sanitizer:** `sanitize_for_display` on the user echo before `push_line_stamped`; skip echo for empty input (matching stdio). The TUI submit path now sanitizes `input_line` into `"> {sanitized}"` with a UTC stamp, and the empty-input branch produces no echo.                                                                                                                                                                                                                                                | `crates/siralos-cli/src/interactive.rs:1948–1980` restructured; test `echo_is_sanitized_and_empty_skipped`                                                                                                                  |
| R5  | **status sanitizer:** `compose_status_line` sanitizes `provider`/`model` interpolations via `sanitize_for_display` before formatting the `"{p} / {m}"` prefix.                                                                                                                                                                                                                                                                                                                                                          | `crates/siralos-cli/src/tui.rs:581–600` patched; test `status_sanitizes_provider_and_model`                                                                                                                                 |
| R6  | **compose ordering:** `compose_session` now runs BEFORE `TerminalGuard::enter` in the TUI path (startup diagnostics visible before the alternate screen); verified that no compose step needs the terminal.                                                                                                                                                                                                                                                                                                             | `crates/siralos-cli/src/interactive.rs:1824–1840` reordered; test `compose_session_before_guard_no_terminal_needed` checks source ordering and that composition succeeds without a guard                                    |
| R7  | **harness gate mirror:** `context_session_record` (`harness.rs:10274–10281`) now mirrors the live seam (`interactive.rs:1032–1041`) — `host_rules` (`workspace.read` Allow) + `declare_profile` + `compose_effective_policy`, gating `enabled` on `applied_profile.is_some()`. A malformed/refused profile keeps `enabled=false` and the record `OFF` even if the raw `siralos.toml` claimed `enabled=true`. Corpus was `v77/357` after decision 111 and remains `v77/357` (no record output changed; no version bump). | `crates/siralos-cli/src/harness.rs:10266–10320` patched; test `refused_profile_leaves_subsystem_off_in_harness_record`                                                                                                      |
| R8  | **docs staleness sweep:** updated `README.md`, `docs/development/PROJECT_CONTEXT.md`, `AGENTS.md` (`/evolve` wording — clarified display-only discovery since decision 111 while execution stays host-gated/out-of-milestone), and `ARCHITECTURE.md` `/evolve` future claim; corpus counts remain `v77/357`, TUI-default entry, external `siralos-godot` at `1bf2ca3` self-contained (`FUTURE` claims clarified), usability-pass features retained. Only claims the current code contradicts were changed.              | `README.md`, `docs/development/PROJECT_CONTEXT.md`, `AGENTS.md:93`, `ARCHITECTURE.md:985` patched                                                                                                                           |
| R9  | **tripwire tests** are part of R1 (`a/b.txt` on inspect/search/expand) and R2/R3/R4/R5/R7 above.                                                                                                                                                                                                                                                                                                                                                                                                                        | `context.rs` 4 new tests + harness/tui/interactive tests above                                                                                                                                                              |

## 3. Criteria → evidence

| Criterion                           | Evidence                                                                                                                                                                                                                                                                                                                                                                                              | Status |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Inspect nested-path defect fixed    | `inspect_accepts_nested_path_slash` and `inspect_slashed_id_end_to_end_via_nested_fixture` pass: `a/b.txt` and `nested/a/b.txt` inspect succeed with `Success` and correct `output.id`; `inspect_rejects_backslash_and_nul_but_allows_slash` proves `\` and NUL still bounded-id, `/` is unknown-node not bounded-id; `expand_accepts_nested_path_slash` and `search_tripwire_with_slash_query` pass. | pass   |
| Double-scroll single `+-10` clamped | `single_scroll_clamped_and_single_step` passes: one `PageUp` = `+10`, one `PageDown` = `-10`, `max_scroll(viewport)` clamped, `handle_key` is the sole applier (inline arms deleted).                                                                                                                                                                                                                 | pass   |
| Single catalog                      | `catalog_cross_equality_single_source` and `catalog_cross_equality_from_interactive` pass: `tui::command_catalog` equals `slash_command_catalog` mapped to owned strings, order pinned 10 entries.                                                                                                                                                                                                    | pass   |
| Echo/status sanitizer gaps closed   | `echo_is_sanitized_and_empty_skipped` passes: poison `"\x1b[31m"`/`\0` does not appear raw, sanitized echo does, empty input produces no `"> "` line; `status_sanitizes_provider_and_model` passes: control chars stripped via `sanitize_for_display`.                                                                                                                                                | pass   |
| Compose ordering                    | `compose_session_before_guard_no_terminal_needed` passes: `compose_session` succeeds without a terminal and source ordering `compose_session` < `TerminalGuard::enter` holds.                                                                                                                                                                                                                         | pass   |
| Harness gate mirror                 | `refused_profile_leaves_subsystem_off_in_harness_record` passes: `DeclaredProfile::Invalid` gates `enabled=false` even when raw record claimed `true`; malformed `siralos.toml` (`plugins = "not-an-array"`) keeps the harness record `OFF`; live `interactive.rs:1032–1041` gate mirrored.                                                                                                           | pass   |
| Docs staleness sweep                | `AGENTS.md:93` `/evolve` execution wording clarified, `ARCHITECTURE.md:985` `/evolve` display-only note added, corpus `v77/357` retained, TUI-default and `1bf2ca3` self-contained claims verified, usability-pass features retained.                                                                                                                                                                 | pass   |
| Corpus outcome                      | No `context-session` scenario output changed under the new gate; `tests/differential/corpus/manifest.json` stays `v77` with `357` scenarios; `CORPUS_VERSION` remains `77` and strict loader asserts `357`.                                                                                                                                                                                           | pass   |
| Red lines held                      | Sanitizer is the single output boundary; stdio session/render behavior unchanged except where noted (R4/R5 inside the boundary, R2 scroll, R6 ordering); decisions `<= 111` untouched; no scheduler core, benchmark strategy, or `context-benchmark.json` changes.                                                                                                                                    | pass   |
| Formatting + hygiene                | `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace --all-targets --all-features`, `npm run check:differential` true exit, `npx prettier --write/--check`, `node scripts/check-doc-links.mjs`, `node scripts/check-secret-hygiene.mjs`, `npm run check:context`, `npx eslint .` all green.                                     | pass   |

## 4. Result

The remediation pass is complete: both audit defects are fixed with tripwire tests, the mechanical hygiene items are done, and the docs match the code; the six HITL questions await user direction as recorded.
