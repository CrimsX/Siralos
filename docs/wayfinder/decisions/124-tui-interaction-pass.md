---
title: "The TUI Interaction Pass"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "109"
supersedes: []
---

# 124 — The TUI Interaction Pass

Ticket [109](../tickets/109-tui-interaction-pass.md) · entry review [123](123-tui-interaction-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The third live-usage round implemented: the palette retention wiring fixed (the unit test tested the helper; the live handle_key never called it), arrow-key palette navigation with Enter/Tab selection, full command display, command history via Up/Down, a /models fetch from the provider (a new network surface with the freeze documented), and the right-edge layout gap fixed; the render frames re-pinned at corpus v82 if changed.

## 2. Implemented I1–I6 — as delivered

| ID  | Implementation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Evidence / note                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| I1  | PALETTE RETENTION (the real fix): `handle_key` calls `state.update_palette()` after every mutation of `state.input` (Char appended, Backspace, Tab completion via `complete_palette_prefix`, and any edit). The unit tests that tested `update_palette` directly remain; new tests drive `handle_key` itself (`/` -> `/p` -> `/pr` each step `Some` non-empty). The root cause (the decision 120 palette verification tested `update_palette` directly, but the live `handle_key` never called it after input edits) is fixed — the live palette recomputes on every keystroke.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `tui.rs` `handle_key` Char/Backspace/Tab each `update_palette`; test `palette_retention_while_typing` via `handle_key` + `handle_key_palette_retains_each_keystroke`                                                                                                                                                                                                                                                                 |
| I2  | ARROW-KEY PALETTE NAVIGATION: `TuiState` gains `palette_selected: Option<usize>`; Up/Down move through the filtered palette entries when the palette is `Some` (wrapping); the selected entry renders highlighted with reversed style (`REVERSED                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | BOLD`Yellow); Enter on a selected entry fills the input with the full command name (replacing the partial`/pr`with`/provider`) and clears the palette; Tab also completes to the selected entry. When the palette is `None`or the input does not start with`/`, Up/Down do command history (I4) instead.                                                                                                                             | `tui.rs` `palette_selected`, `handle_key` Up/Down palette vs history routing; `draw_with_pane`/`render_to_buffer_with_pane` reversed highlight; test `palette_arrow_navigation_and_enter_tab_selection` |
| I3  | FULL COMMAND DISPLAY: the palette shows all filtered entries (removed the at-most-8 bound); the palette popup grows to fit, bounded by the terminal height minus the input/status rows (`area.height - 3`); if more entries than fit, a scroll indicator appears (`+N more` / `↑/↓`). When the input is exactly `/`, all 11 commands display.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tui.rs` palette render removes 8-bound, `available_height = area.height - 3`, scroll indicator; test `palette_shows_all_11_on_slash_and_scrolls_when_overflow`                                                                                                                                                                                                                                                                      |
| I4  | COMMAND HISTORY: `TuiState` gains `prompt_history: Vec<String>` (submitted prompts, oldest first, bounded to 100) and `history_index: Option<usize>` plus `history_draft: Option<String>`; pressing Up when the palette is `None` recalls the previous prompt into the input (moving backward through the stack); Down moves forward; reaching past the newest restores the pre-navigation input; the history is per-session (in-memory, no persistence). Enter appends to the history (`push_history` bounded, dedup consecutive).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `tui.rs` `prompt_history`, `history_index`, `history_draft`, `push_history`; `handle_key` Up/Down history + Enter `push_history`; test `command_history_up_down_stack_and_restore`                                                                                                                                                                                                                                                   |
| I5  | RIGHT-EDGE GAP: the layout constraints in `draw_with_pane` and `render_to_buffer_with_pane` fixed: the off variant uses a single `Min(0)` full-width area for the transcript; the on variant uses transcript `Min(0)` + pane `Length(40)` with no gap; header/status rows unchanged. The transcript now fills the full terminal width when the pane is off and pane + transcript fill the full width with no gap when on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tui.rs` `draw_with_pane`/`render_to_buffer_with_pane` `Constraint::Min(0)` + `Length(40)`; test `off_render_at_80x24_has_no_empty_right_edge_strip`                                                                                                                                                                                                                                                                                 |
| I6  | /models FETCH: a new `/models` behavior: when the session has a configured provider (endpoint + credential), `/models` performs a blocking GET to the provider's `/models` endpoint (the OpenAI-compatible shape: `GET {endpoint}/models` with Bearer auth from the `HostCredential`) using the same bounded reqwest pattern as `GenericProvider` (1 MiB cap, sanitized, credential redacted); parses the OpenAI models response shape `{data: [{id: "..."}]}`; displays the fetched model list as transcript lines (host-generated, sanitized); the freeze during the fetch is documented (the synchronous architecture constraint — no threads, single read-owner). If the provider returns an error or the shape is unrecognized, an honest error line is displayed. If no provider is configured, `/models` reports that. The fetch is a new network surface — it uses the same credential, endpoint, and recording-hygiene rules as the existing provider path (no credential values in output, bounded responses). Implemented as a provider-surface fn `fetch_models(endpoint, credential) -> Result<Vec<String>, String>` alongside the existing completion call — not a tool; invoked from the `/models` command dispatch in both frontends. | `siralos-adapters/src/provider/generic.rs` `fetch_models` + `parse_models_shape` bounded 1 MiB, sanitized, redacted; `interactive.rs` `SlashCommand::Models`, `slash_command_catalog` 11, `dispatch_stdio_command`/`dispatch_tui_command` `/models` branch with blocking freeze note; tests `parse_models_shape_extracts_ids` + `fetch_models_bounded_get_redacts_credential_on_error` + `models_reports_honestly_when_unconfigured` |

## 3. Criteria -> evidence

| Criterion                                                                      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Verdict |
| ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| Palette retention fix verified in the live path (handle_key -> update_palette) | `handle_key` calls `state.update_palette()` after every `state.input` mutation (Char, Backspace, Tab); test `palette_retention_while_typing` drives `handle_key` for `/`, `p`, `r`, `/pr` each step `palette` `Some` non-empty; the helper-only verification gap is closed.                                                                                                                                                                                                                            | pass    |
| Arrow-navigation + selection behavior                                          | `palette_selected: Option<usize>` added; Up/Down move through filtered entries when palette `Some`, wrap; selected renders reversed; Enter fills input with full command name and clears palette; Tab completes to selected; when palette `None`, Up/Down route to history; test `palette_arrow_navigation_and_enter_tab_selection` (`/p` + Down to `/provider` + Enter -> input `/provider`, palette `None`).                                                                                         | pass    |
| Full display                                                                   | Removed 8-bound; popup grows to fit bounded by `area.height - 3`; scroll indicator appears when overflow; input exactly `/` shows all 11 commands; test `palette_shows_all_11_on_slash_and_scrolls_when_overflow` (80x24 shows 11, 80x10 shows indicator).                                                                                                                                                                                                                                             | pass    |
| History stack behavior                                                         | `prompt_history` bounded 100 oldest-first + `history_index` + `history_draft`; submit `a`, `b`, Up -> `b`, Up -> `a`, Down -> `b`, Down -> `""` restored; test `command_history_up_down_stack_and_restore` green; per-session in-memory, no persistence.                                                                                                                                                                                                                                               | pass    |
| /models fetch shape + the freeze note                                          | `fetch_models(endpoint, credential)` performs blocking GET `{endpoint}/models` Bearer, 1 MiB cap via `bounded_body_text`, sanitized, parses `{data: [{id}]}` via `parse_models_shape`; errors honest; endpoint+credential hygiene same as provider path (no credential values); freeze documented as synchronous architecture constraint (no threads, single read-owner); test `parse_models_shape_extracts_ids` via fixture + `fetch_models_bounded_get_redacts_credential_on_error` redaction proof. | pass    |
| Layout gap fix                                                                 | `draw_with_pane` off variant single `Min(0)` transcript full width; on variant transcript `Min(0)` + pane `Length(40)` fills full width with no gap; header/status unchanged; test `off_render_at_80x24_has_no_empty_right_edge_strip` verifies no empty right-edge column strip.                                                                                                                                                                                                                      | pass    |
| Red lines + stdio-unchanged proof                                              | Sanitizer single output boundary retained (every `/models` line via `sanitize_for_display`); input queue single read owner; approvals host-gated; no threads; no persistence; stdio frontend byte-unchanged except the additive shared `/models` command (catalog 11, dispatch, honesty line) — `interactive::tests` suite green; decisions ≤ 120 untouched.                                                                                                                                           | pass    |
| Render frames re-pinned at corpus v82                                          | `CORPUS_VERSION` 82 + `contract.mjs` 82 + `manifest.json` 82/359 + `expectations.json` re-pinned via `candidate2.json` tui-render records; `t4_frames_still_deterministic_with_history_and_palette` byte-equal.                                                                                                                                                                                                                                                                                        | pass    |
| Live-usage third round closed                                                  | The user-directed 2026-08-31 (session HITL) third live-usage round: palette retention wiring, arrow-key palette navigation with Enter/Tab, full command display, command history via Up/Down, /models fetch with freeze documented, and right-edge gap fix — all live.                                                                                                                                                                                                                                 | pass    |

## 4. Result

The interaction pass is complete: the palette retains and navigates with arrows, the full vocabulary displays, command history works via Up/Down, /models fetches the provider's model list, and the layout fills the terminal; corpus v82/359 re-pinned if the frames changed; HITL 2026-08-31.
