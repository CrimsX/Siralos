---
title: "The Provider Add-Form Clarity Fixes Entry Review"
label: "wayfinder:decision"
status: accepted
date: "2026-08-31"
ticket: "111"
supersedes: []
---

# 127 — The Provider Add-Form Clarity Fixes Entry Review

Ticket [111](../tickets/111-add-form-clarity-fixes.md) · entry review [103](103-siralos-tui-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The user is stuck on the Provider field because the form doesn't explain what each field expects. This pass makes the form self-explanatory: descriptive labels with examples, human-readable errors, and a field description per field.

## 2. Fixes D1–D4 — as authorized

| ID  | Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Evidence / note                                                                                                                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | DESCRIPTIVE FIELD LABELS: each form field renders with a descriptive label and an example: "provider name (e.g. openai, example-vendor):", "model (e.g. model-a, gpt-4o):", "credential env var (e.g. OPENAI_API_KEY):", "endpoint URL (optional, e.g. https://api.openai.com/v1):". The labels replace the current bare "provider:", "model:", etc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `tui.rs` `provider_add_form_lines` descriptive labels with examples; test `provider_add_form_descriptive_labels` checks "provider name" in rendered frame                                      |
| D2  | HUMAN-READABLE ERRORS: the validation errors are rewritten in plain English: "Provider name must be lowercase letters, numbers, hyphens, or underscores (e.g. openai, example-vendor)" instead of "A provider must match [a-z0-9_-]{1,64}"; "Model name must be printable and between 1 and 256 characters (e.g. model-a, gpt-4o)" instead of the current bound message; "Credential env var must be uppercase letters, numbers, and underscores (e.g. OPENAI_API_KEY) - set this variable with your API key before starting Siralos" instead of "A credential env name must match [A-Z0-9_]{1,64}"; "Endpoint must be a valid URL starting with https:// or http:// (e.g. https://api.openai.com/v1)" instead of "An endpoint must start with...". Apply the same rewriting to ALL validation errors in the form (provider, model, credential, endpoint). | `tui.rs` `validate_provider_name`, `validate_model_name`, `validate_credential_env_name`, `validate_endpoint_value` return human-readable strings; test `validation_errors_are_human_readable` |
| D3  | FIELD DESCRIPTIONS: each form field renders a dim description line BELOW the label explaining what the field is for: provider -> "the name you'll use to identify this provider", model -> "which model to use for completions", credential env -> "the environment variable that holds your API key (set it before starting Siralos)", endpoint -> "the API URL (leave empty for the default)".                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `tui.rs` `provider_add_form_lines` dim description lines below each label; test `field_descriptions_render`                                                                                    |
| D4  | THE PROVIDER FIELD EXPLAINED: the Provider field collects a SHORT IDENTIFIER NAME for the provider (like "openai", "example-vendor"), NOT a URL. The URL goes in the Endpoint field. This is made clear by the label + description + error message.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | D1 + D3 + D2 together make Provider vs Endpoint unambiguous; label "provider name (e.g. openai, example-vendor)" + description + human-readable error                                          |

## 3. Criteria -> evidence

| Criterion                                            | Evidence                                                                                                                                                                      | Verdict |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| Descriptive labels — each field shows example        | Rendered frame contains "provider name (e.g. openai, example-vendor)", "model (e.g. model-a, gpt-4o)", etc.; test `provider_add_form_descriptive_labels`                      | pass    |
| Human-readable errors — no cryptic regex             | Error strings are "Provider name must be lowercase letters, numbers, hyphens, or underscores (e.g. openai, example-vendor)" etc.; test `validation_errors_are_human_readable` | pass    |
| Field descriptions render below each label           | Rendered frame contains "the name you'll use to identify this provider" etc. as dim lines; test `field_descriptions_render`                                                   | pass    |
| Validation still works — same regex, better messages | Invalid provider/model/credential/endpoint still rejected (same regex/bounds); test `validation_still_rejects_invalid_input`                                                  | pass    |
| Provider vs Endpoint unambiguous                     | Provider label + description + error explain short identifier vs URL; manual inspection                                                                                       | pass    |
| Red lines honored                                    | Sanitizer single output boundary; no threads; no persistence; stdio frontend byte-unchanged; decisions <= 126 untouched                                                       | pass    |

## 4. Result

The clarity fixes are complete: every field explains what to type, errors are human-readable, and the user can complete the form without external documentation.
