---
title: "The TUI Usability Pass"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "104"
supersedes: []
---

# 111 — The TUI Usability Pass

Ticket [104](../tickets/104-tui-usability-pass.md) · entry review [110](110-tui-usability-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The approved usability pass implemented as drafted: the keystroke redraw starvation fixed (drain-then-draw-once), the command palette over the single vocabulary, unknown-command honesty, UTC-stamped messages, the status provider/model line, and the /provider + /model display commands plus the /evolve discovery command wiring the Stage 6 surfaces into the session vocabulary; modes recorded not-applicable; runtime switching and evolution execution out of scope.

## 2. Implemented I1–I7 — as delivered

| ID  | Implementation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Evidence / note                                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1  | U1 drain-then-draw-once: the TUI loop inner drain `while event::poll(Duration::from_millis(15))? { handle the event through the EXISTING paths (modal branch, handle_key, scroll) — collecting submits }, then if a submit was collected run the dispatch ONCE (the freeze stands), then one draw at the loop bottom. Keystroke display becomes immediate. The modal decision handling and the submit echo behave exactly as today (the T2/T4 tests pin them).                                                                                                                                                                                                                                                               | `interactive.rs` ~1687-1924 restructured; `poll` 100ms -> drain 15ms + bounded idle poll; tests `latency_drained_batch` + modal guards                      |
| I2  | U2 palette: `command_catalog() -> Vec<(String,String)>` ordered over the SAME `SlashCommand` vocabulary including the new U7/U8 variants; `TuiState.palette: Option<Vec<(String,String)>>` set when input starts with `/` (filtered by prefix, case-insensitive); `draw` renders the palette popup above the input line, bounded height (at most 8 visible + a +N more line); display-only.                                                                                                                                                                                                                                                                                                                                  | `tui.rs` `command_catalog`, `palette`, `draw` palette popup; tests palette filtered/hidden                                                                  |
| I3  | U3 unknown-command honesty: in the TUI submit path — if the line starts with `/` and `parse_slash_command` yields an unknown command, push the explicit line `unknown command - available: ...` listing the catalog names (host-generated static content) and SKIP the prompt fall-through. The stdio path is untouched.                                                                                                                                                                                                                                                                                                                                                                                                     | `interactive.rs` TUI branch unknown check; stdio `Prompt` unchanged; tests unknown line                                                                     |
| I4  | U4 timestamps: `TranscriptEntry { text: String, timestamp: Option<String> }` — `TuiState.transcript` becomes `Vec<TranscriptEntry>` (the ring bound stays); `push_line` keeps `None` for static host lines while `push_line_stamped` carries the timestamp; the live loop stamps user echoes and prompt responses with UTC `HH:MM:SS` via a small std-only civil-date helper (labeled `UTC`, e.g. `"2026-08-31 14:03:22 UTC"`); `draw` renders the dim stamp line below each entry when present; scroll operates over entries. `tui_render_record` builder carries FIXED fixture timestamps; 4 scenarios re-pinned at corpus v77 (count 357 unchanged; expectations 118 updated in place; context-benchmark BYTE-IDENTICAL). | `tui.rs` `TranscriptEntry`, `push_line_stamped`, `utc_timestamp_now`, `draw`; `harness.rs` fixture timestamps; `tests/differential/corpus/tui-render.*` v77 |
| I5  | U5 status provider/model: the status line composes the provider/model prefix from the composed profile (the TUI loop holds the applied profile via `compose_session` — thread provider/model strings into the `TuiState` status composition; absent -> `"no provider configured"`); the `/context` and `/tools` status texts unchanged otherwise.                                                                                                                                                                                                                                                                                                                                                                            | `interactive.rs` compose provider/model threading; `tui.rs` status composition; tests present/absent                                                        |
| I6  | U7 /provider and /model: `SlashCommand::Provider` and `SlashCommand::Model` variants; `parse_slash_command` recognizes `/provider` and `/model`; the shared dispatch: both writers print the same host-generated lines (provider: `<name>`, credential: `<present\|absent>`; model: `<model>`) from the composed profile; the catalog (I2) lists them; stdio tests that pinned `/provider` or `/model` as prompt-fallthrough updated (record the additive change).                                                                                                                                                                                                                                                           | `interactive.rs` `SlashCommand` + dispatch stdio+tui; `tui.rs` catalog; tests `/provider` `/model`                                                          |
| I7  | U8 /evolve discovery: `SlashCommand::Evolve`; `parse_slash_command` recognizes `/evolve`; the shared dispatch: both writers print the same host-generated lines listing the four bounded surfaces (corpus, workflow, proposal, packaging) and the host-gated execution note (escalation Profile->Host per Stage 6 design); the catalog lists it. No evolution execution is invoked.                                                                                                                                                                                                                                                                                                                                          | `interactive.rs` `SlashCommand::Evolve` + dispatch; `tui.rs` catalog; tests `/evolve` lists four surfaces                                                   |

## 3. Criteria → evidence

| #    | Criterion                                                   | Evidence                                                                                                                                                                                                                                                                                                                 |
| ---- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| (1)  | Keystroke redraw starvation fixed (U1/I1)                   | Loop drains `poll(15ms)` until empty then draws ONCE per batch; `latency_drained_batch_draws_once` verifies one draw per drained batch; `poll` 100ms idle remains; submit freeze documented T1 limitation unchanged; modal handling preserved (T2/T4).                                                                   |
| (2)  | Command palette over single vocabulary (U2/I2)              | `command_catalog()` enumerates the SAME `SlashCommand` variants including Provider/Model/Evolve; `palette_lists_catalog_filtered_by_prefix` verifies `/do` matches `domains*` and `/m` matches `model`; `palette_hidden_without_slash` verifies no popup; bounded height 8 + `+N more`.                                  |
| (3)  | Unknown-command honesty (U3/I3)                             | `unknown_command_line_content` verifies TUI line `unknown command - available: ...` listing catalog; stdio fall-through unchanged — the stdio test for unknown `/` still takes the `Prompt` path.                                                                                                                        |
| (4)  | UTC-stamped messages fixed fixtures (U4/I4)                 | `timestamps_render_below_messages` via `TestBackend` fixed fixture stamp `2026-08-31 12:00:00 UTC` renders dim stamp line; `civil_date_golden_values` asserts golden UTC values for sample epoch millis; live `utc_timestamp_now` uses std-only civil-from-days; scroll operates over `TranscriptEntry`.                 |
| (5)  | Status provider/model (U5/I5)                               | `status_provider_model_present` verifies status prefix `example-vendor / model-a` from composed profile; `status_no_provider_configured` verifies honest absent string; `/context` `/tools` texts unchanged.                                                                                                             |
| (6)  | /provider + /model + /evolve shared dispatch (U7/U8, I6/I7) | `provider_dispatch_lines`, `model_dispatch_lines`, `evolve_dispatch_lines` verify TUI + stdio writers print same host-generated lines; `evolve_lists_four_surfaces` asserts corpus, workflow, proposal, packaging; catalog lists Provider/Model/Evolve; execution not invoked; the stdio additive-command note recorded. |
| (7)  | Re-pinned tui-render scenarios at corpus v77                | Manifest `corpusVersion 77`, count 357, four tui-render scenarios re-pinned with FIXED timestamps; `harness.rs` `CORPUS_VERSION 77` + `contract.mjs` `CORPUS_VERSION 77`; `strict_loader 357`; `tui_render_record` carries fixture timestamps; `context-benchmark.json` BYTE-IDENTICAL to v76.                           |
| (8)  | Stage 6 wiring note                                         | `/evolve` lists exactly the four bounded surfaces from `crates/siralos-core/src/evolution.rs` (corpus, workflow, proposal, packaging, decisions 58-59) and states execution is host-gated (escalation Profile->Host per Stage 6 design); running evolution from the command is out of scope.                             |
| (9)  | All existing tests keep compiling                           | `cargo test --workspace --all-targets --all-features` green; `push_line`/`transcript` shape updated to `TranscriptEntry` while T1-T4 tests updated to the entry model; `handle_key`, approval modal, pane, sanitizer tests preserved.                                                                                    |
| (10) | Differential + hygiene + formatting green                   | `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `npm run check:differential` audit true exit, `npx prettier --write/--check`, `node scripts/check-doc-links.mjs`, `node scripts/check-secret-hygiene.mjs`, `npm run check:context`, `npx eslint .` all 0.             |

## 4. Result

The usability pass is complete: keystrokes display immediately, the palette makes the vocabulary discoverable, unknown commands are honest, messages carry UTC stamps, the status shows provider/model, and /provider + /model answer directly while /evolve surfaces the Stage 6 evolution features; the stdio session behavior is unchanged except the additive shared commands; corpus v77/357, audit 352/352 applicable required, 118 expectations, 4 explicit platform skips.
