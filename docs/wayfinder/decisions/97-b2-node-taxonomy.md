---
title: "Activation B2: The Node Taxonomy"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "68"
supersedes: []
---

# 97 — Activation B2: The Node Taxonomy

Governing plan 68 · entry review [95](95-context-activation-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL): C1–C6 approved as drafted — three kinds (Source default, Decision for docs/adr/** and docs/wayfinder/decisions/**, Knowledge reserved with no producer), classification at graph-binding time only, the decision 92 kind-weight table unchanged (Decision scores in the +0 bucket — no search-semantics change and no benchmark re-measurement), per-kind bounds deferred to the adversarial invariants, L0-only binding with L1 summary generation proposed for B3 as its own pre-committed decision, and no model-visible surface until B3 wires the default-off profile key.**

## 2. The taxonomy (T1–T6)

| Taxonomy                 | Detail                                                                                                                                                                                                                                                                                                                                          |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1 KINDS                 | Exactly three — `Source` (default: every scanned file), `Decision` (relative paths under `docs/adr/` or `docs/wayfinder/decisions/`, normalized separators, prefix match on path segments so `docs/adrbogus/x` does NOT match), `Knowledge` (RESERVED: taxonomy-only; the scan produces no Knowledge nodes and no producer exists in this arc). |
| T2 CLASSIFICATION SITE   | At graph-binding time only — a pure fn `classify_node(relative_path) -> NodeKind` (the graph's `ContextNodeKind`) applied inside the binding; the B1 scan output (`BoundedScan`/`ScanNode`) is UNCHANGED byte-for-byte.                                                                                                                         |
| T3 SEARCH WEIGHTS        | Untouched — Decision nodes score under the existing "all other kinds +0" rule of the decision 92 table (`Knowledge +3, Source +1, all other kinds +0` — Decision falls in the `+0` bucket; zero search-semantics change, no benchmark re-measurement); a test pins that a Decision node's decision 92 score contribution from kind is `0`.      |
| T4 PER-KIND BOUNDS       | None beyond the B1 global scan bounds (decision 95 adversarial invariants own flooding protection); the record states this.                                                                                                                                                                                                                     |
| T5 REPRESENTATIONS       | Binding stays at L0 digest only (no L1 summaries for scanned files in B2 — that is proposed for B3 as its own pre-committed decision).                                                                                                                                                                                                          |
| T6 MODEL-VISIBLE SURFACE | None — classification is host-side; no model-visible surface until B3 wires the default-off profile key.                                                                                                                                                                                                                                        |

## 3. Criteria → evidence

| Criterion                                                      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Status |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Node taxonomy pinned (Source/Decision/Knowledge-reserved)      | `crates/siralos-adapters/src/context_scan.rs` — `classify_node` (normalized `\\` → `/`, `docs/adr` / `docs/adr/**` and `docs/wayfinder/decisions` / `docs/wayfinder/decisions/**` with segment-boundary prefix match, default `Source`, never `Knowledge`), `bind_scan_to_graph` applies taxonomy at binding time only; per-kind bounds none (T4) and L0-only binding (T5) stated in record. Tests: `classify_decision_prefix_docs_adr` (docs/adr/x, nested, win separators), `classify_decision_prefix_wayfinder` (docs/wayfinder/decisions/x, nested), `classify_non_matching_near_misses_are_source` (docs/adrbogus/x, docs/adr.txt, decisionsbogus), `classify_default_source`, `binding_never_produces_knowledge` (assert no Knowledge nodes over any fixture). | pass   |
| Classification at graph-binding time only; scan byte-unchanged | `BoundedScan`/`ScanNode` unchanged byte-for-byte (T2); scan output kind-agnostic; evidence `scan_output_unchanged_byte_equal` (BoundedScan byte-equal before/after binding, clone equality, JSON canonical equality, bind does not mutate scan), `determinism_binding_byte_equal` (binding deterministic byte-equal), `determinism_run_twice_byte_equal` retained from B1.                                                                                                                                                                                                                                                                                                                                                                                           | pass   |
| Search-kind weights untouched (Decision +0)                    | Decision 92 table untouched (`Knowledge +3, Source +1, all other kinds +0`); test `kind_weight_decision_is_zero` pins Decision contributes `0` under the decision 92 kind table; `crates/siralos-adapters/src/tool/context.rs` scoring code not touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | pass   |
| Differential corpus pinning (v72/345)                          | New scenario `tests/differential/corpus/context-scan.scan-classification.json` (fixture workspace with `docs/adr/**`, `docs/wayfinder/decisions/**` plus ordinary files, near-miss fixtures, exercising the binding); harness `context_scan_record` via `with_fixture_workspace` + `scan_workspace` remains kind-agnostic; manifest `corpusVersion 72`, count `345`, digests refreshed via `regenerate-corpus-manifest.mjs` (sha `7fd4c2820badb08ddf010fe7bd8cea06d4bfb3749d4db997b8733d774c16b6e8`), `CORPUS_VERSION 72` in `crates/siralos-cli/src/harness.rs` + `tests/differential/shared/contract.mjs`, `strict_loader 345`; post-freeze expectations `106` records via OS-temp `canonicalRecordDocument` (surgical 105→106 diff, 1 record added)               | pass   |
| Differential audit all-applicable-required                     | Differential `npm run check:differential` parity held `340/340` applicable required scenarios; `4` explicit platform skips; `0` accepted informational deviations; pinned v32 oracle untouched; audit via `tests/differential/out/audit.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | pass   |
| Benchmark byte-identity                                        | `crates/siralos-adapters/src/tool/context_benchmark.rs` untouched; `crates/siralos-cli/src/harness.rs` `context_benchmark_record` byte-identical; `tests/differential/corpus/context-benchmark.json` sha unchanged `4f7a3374da0a5be6210416b9b763b3e204737b5af9c50e501086f34b2cf7a937`; differential `context-benchmark` record byte-identical to v71 (audit 340/340, compare `candidate.json` vs `oracle.json` via `canonicalizeJson`)                                                                                                                                                                                                                                                                                                                               | pass   |
| No persistence / no model-visible surface / read-only          | No persistence, no spawn, no model-visible surface (T6); `cargo test` + differential harness + `grep` for spawn/persistence remain clean; tools stay read-only; host-side classification only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | pass   |

## 4. Result

B2 is complete: the taxonomy is pinned (Source/Decision/Knowledge-reserved), classification lives at graph-binding time with the scan byte-unchanged, the decision 92 search-kind weights are untouched so no benchmark re-measurement occurs, and the binding stays L0-only pending the B3 summary-generation decision; the model sees nothing new.
