---
title: "The TUI UX Pass 2"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "107"
supersedes: []
---

# 120 — The TUI UX Pass 2

Ticket [107](../tickets/107-tui-ux-pass-2.md) · entry review [119](119-tui-ux2-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The second usability round implemented: the header deduplicated, an ASCII banner and greeting greet the session, the palette filter verified against typing, timestamps moved to the user's local timezone via the time crate (a recorded CLI-only dependency), the working indicator styled within the synchronous architecture (a live spinner is recorded as an open architecture question — it needs a UI thread), and /provider became a read-only picker with the add-flow split to ticket 108 pending the config-write authority decision.

## 2. Implemented I1–I6 — as delivered

| ID    | Implementation                                                                                                                                                                                                                                                                                                                                                                                                                                   | Evidence / note                                                                                                                                                                                                                                                                                                                                              |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| I1    | HEADER DEDUP: the header composes provider/model ONLY when both are present; absent -> the header shows only " Siralos " (reversed cyan). The "no provider configured" text lives only in the bottom status line via `compose_status_line`.                                                                                                                                                                                                      | `tui.rs` header bar `draw_with_pane`/`render_to_buffer_with_pane` + `header_text`; test `header_renders_provider_model_present_and_absent`                                                                                                                                                                                                                   |
| I2    | ASCII BANNER + GREETING: a hand-drawn "siralos" block (`SIRALOS_BANNER`, every line <= 80 cols) + the greeting line `SIRALOS_GREETING` pushed into the transcript at TUI session start (before first draw) via `push_line` (unstamped, static host strings). The stdio frontend UNCHANGED (banner is TUI-only).                                                                                                                                  | `tui.rs` `SIRALOS_BANNER`, `SIRALOS_GREETING`, `push_banner_and_greeting`; `interactive.rs` TUI startup; tests `banner_renders_within_80_and_greeting_present`                                                                                                                                                                                               |
| I3    | PALETTE FILTER VERIFIED: read the current palette update path (input starts with / -> palette = Some(catalog filtered by the typed prefix)); the filter updates on every keystroke — typing /p then /pr updates the filtered list each step; no drop mid-typing. Verified correct and recorded with `palette_filter_step_verified`.                                                                                                              | `tui.rs` `update_palette` + `handle_key`; test `palette_filter_step_verified`                                                                                                                                                                                                                                                                                |
| I4    | LOCAL-TIMEZONE STAMPS: added the time crate (0.3, features local-offset + formatting) to workspace.dependencies + siralos-cli ONLY; renamed `utc_timestamp_now` -> `local_timestamp_now` returning "YYYY-MM-DD HH:MM:SS +HH:MM" via `OffsetDateTime::now_local()` (fallback: `now_utc()` with UTC label when `now_local` is None); updated stamp format comments; tui-render fixtures keep FIXED stamps (the record builder uses fixed values).  | `Cargo.toml` `time = { version = "0.3", features = ["local-offset", "formatting"] }` workspace + CLI only; `tui.rs` `local_timestamp_now`, `utc_timestamp_from_millis` retained for fallback; `harness.rs` FIXTURE_TIMESTAMP fixed; test `local_timestamp_now_format_and_fallback`                                                                           |
| I5    | WORKING MARKER (honest constraint): the working status line is drawn immediately before the blocking dispatch (already the case — verified), and the working marker gains a distinctive style (yellow bold "working" in the status) so the frozen state is unmistakable. The live-spinner architecture question (a UI event-pump thread would be the enabling change) is recorded as open and NOT implemented.                                   | `tui.rs` `is_working_status` + status `Line` styling yellow bold; `interactive.rs` working dispatch verified; no threads                                                                                                                                                                                                                                     |
| I6    | PROVIDER PICKER (scoped read-only): /provider opens a popup (rounded border, title " providers ") listing each configured provider with its base-url host and model, up/down to move, Enter to select (echoing "provider: <name> selected" into the transcript as a host line), Esc to close without selecting; selection is DISPLAY + ECHO only — no config write, no session re-composition. If only one provider exists, the picker shows it. | `tui.rs` `ProviderPicker`, `ProviderEntry`, `open_provider_picker`, `handle_key` picker branch, `host_from_endpoint`, `provider_entries_from_session`; `interactive.rs` `/provider` TUI picker dispatch read-only; tests `provider_picker_renders_and_echo_and_esc`, `provider_picker_single_shows_and_selects`, `provider_picker_read_only_no_config_write` |
| Dep   | NEW DEPENDENCY: `time` 0.3 with `local-offset` + `formatting` added to workspace.dependencies pinned and to siralos-cli ONLY (recorded like ratatui was).                                                                                                                                                                                                                                                                                        | `Cargo.toml` workspace + `crates/siralos-cli/Cargo.toml` `time = { workspace = true }`; `Cargo.lock` updated                                                                                                                                                                                                                                                 |
| Spin  | LIVE SPINNER CONSTRAINT: synchronous session — during a blocking model round the loop cannot redraw, so a LIVE spinner requires a UI thread, which is an architecture decision (thread-free by design) presented as OPEN and NOT implemented.                                                                                                                                                                                                    | This decision §2 H5 + §4; roadmap notes                                                                                                                                                                                                                                                                                                                      |
| Scope | PICKER SCOPING: /provider is display-only this pass; the ADD-A-PROVIDER flow (writing siralos.toml) is ticket 108 pending a config-write authority decision. No config is written.                                                                                                                                                                                                                                                               | Ticket 107 Resolution + this decision                                                                                                                                                                                                                                                                                                                        |

## 3. Criteria → evidence

| Criterion                                                | Evidence                                                                                                                                                                                                                                   | Status |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| Header is deduplicated (no duplication when no provider) | `header_renders_provider_model_present_and_absent` asserts header row lacks "no provider configured" when absent, status still carries it via `compose_status_line`; `t4_frame_shell_basic_matches_harness_record` re-pinned at v81        | pass   |
| ASCII banner and greeting greet the session              | `SIRALOS_BANNER` (4 lines, each <=80) + `SIRALOS_GREETING` pushed via `push_banner_and_greeting` at TUI start; `banner_renders_within_80_and_greeting_present`, `greeting_line_present_in_transcript`; fixtures carry them at v81          | pass   |
| Palette filter verified against typing                   | `update_palette` filters live on every keystroke; `palette_filter_step_verified` types /p -> /pr -> /provider and asserts filtered length each step; no drop mid-typing                                                                    | pass   |
| Stamps are local-timezone with UTC fallback              | `local_timestamp_now` via `time::OffsetDateTime::now_local` formatting "YYYY-MM-DD HH:MM:SS +HH:MM", fallback to `utc_timestamp_from_millis` with UTC label; `local_timestamp_now_format_and_fallback` + `harness.rs` fixed fixture stamps | pass   |
| Working state is unmistakable (styled distinction)       | `is_working_status` detects "working" case-insensitive; `working_status_is_detected_and_styled` asserts yellow bold style in buffer; dispatch sets `base = "working"` before blocking call                                                 | pass   |
| /provider is a read-only picker echoing the selection    | `provider_picker_renders_and_echo_and_esc` renders " providers " popup with entries, Enter echoes "provider: <name> selected", Esc closes without second echo; `provider_picker_single_shows_and_selects`; no config write                 | pass   |
| Stdio frontend byte-unchanged                            | `stdio_path_byte_unchanged_banner_tui_only` ensures stdio transcript stays empty while TUI carries banner; `stdio_unknown_command_honesty_line_not_prompt` etc green; differential `command-catalog` etc still 352/352                     | pass   |
| Time crate is CLI-only (dependency direction)            | `Cargo.toml` workspace `time` + `crates/siralos-cli/Cargo.toml` `time = { workspace = true }`; `crates/siralos-core/Cargo.toml` and `crates/siralos-adapters/Cargo.toml` contain no `time`                                                 | pass   |
| Frames are re-pinned at corpus v81 with determinism      | `harness.rs` `CORPUS_VERSION 81` + `contract.mjs` 81 + `manifest.json` 81/357 + `expectations.json` 118 re-pinned via `candidate2.json` tui-render records; `t4_frames_still_deterministic_with_header` byte-equal                         | pass   |
| Live spinner constraint recorded open                    | This decision §2 I5 + §4 + roadmap notes: "a live spinner requires a UI thread (open architecture question)"; no threads added                                                                                                             | pass   |

## 4. Result

The UX pass is complete: the header is deduplicated, sessions open with the banner and greeting, the palette filter is verified, stamps are local-timezone, the working state is unmistakable, and /provider picks among configured providers read-only; the add-flow and the live spinner await their own decisions; the frames re-pinned at corpus v81/357; HITL 2026-08-31.
