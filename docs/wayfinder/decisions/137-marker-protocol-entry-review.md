---
title: "The Verbatim Credential and the Protocol Picker Entry Review"
label: "wayfinder:decision"
status: "accepted"
date: "2026-08-31"
ticket: "116"
supersedes: []
---

# The Verbatim Credential and the Protocol Picker Entry Review

Ticket [116](../tickets/116-api-key-marker-protocol-picker.md) · entry review [103](103-siralos-tui-entry-review.md) · [Map](../siralos-roadmap.md)

> **User-directed 2026-08-31 (session HITL, amended in place before commit).** The api key field stores what the user types VERBATIM — the form is an interface, not a validator: input starting with env: is stored as the env-var form, any other non-empty input is stored as the literal key (key:<value>), empty means no credential. The user has explicitly overridden the env-only-only posture for direct storage (the config is gitignored); env-only remains available via the env: prefix; display surfaces redact key: values; the secret-hygiene gate still guards repo files. Additionally: the model picker gains a sliding viewport (the selection is always visible), and provider errors render honestly and bounded (the effective URL + status + a truncated body, no raw HTML dumps). The api protocol remains a selectable picker over openai-completions (POST chat/completions, messages[], Bearer), openai-responses (POST responses, input/instructions, output Items), anthropic-messages (POST v1/messages, top-level system, required max_tokens, x-api-key + anthropic-version), with the legacy aliases accepted in the parse; the request-shaping use remains future work.

## 2. The Fixes

| Fix                        | The change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | The evidence                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V1 VERBATIM CREDENTIAL     | The ApiKey field stores the user's input VERBATIM — NO validation, NO teaching error, NO marker special-case. Empty -> `credential_env = None` (no credential); input starting with `env:` -> stored as-is (the env-var form, still supported); ANY other non-empty input -> stored as `key:<input>` (the literal key). `write_profile_config` writes the credential string as given (`env:X` stays `env:X`; `key:X` written as `key:X`). The `GenericProvider` credential resolution gains the `key:` scheme: `env:NAME` -> resolve the env var at use time; `key:VALUE` -> the literal value as the Bearer token; a bare value (legacy configs) -> treated as an env-var name (compat). Resolution NEVER logs or echoes the value. The `/provider` display and the status line REDACT: a `key:` credential renders as `key:***` (never the value); an `env:` credential renders as `env:NAME`. No code path may include the key value in transcript lines, errors, or the replay store beyond the config write itself. The AGENTS.md line "credentials are env-only" is superseded by this user-directed amendment. | `tui.rs` verbatim ApiKey handlers (two), `interactive.rs` `write_profile_config` verbatim write + `HostCredential::from_credential_str`, `generic.rs` key: resolution, redacted `render_provider_line`; tests `verbatim_credential_public_stores_key_public`, `verbatim_credential_env_form_stored_as_is`, `verbatim_credential_empty_is_none`, `credential_resolution_*`, `provider_line_credential_present_absent` redaction, `verbatim_storage_*` |
| V2 PICKER VIEWPORT         | The model picker (and the protocol picker) render a bounded WINDOW of items (8 visible rows) that slides with the selection — the selected item is ALWAYS visible; a position indicator "selected+1/total" renders at the window top; Up/Down move the selection, the window follows. The picker no longer overflows the modal/terminal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `tui.rs` `provider_add_form_lines` viewport (8 rows, sliding window, indicator); tests `model_picker_viewport_selected_always_visible`, `protocol_picker_viewport_position_indicator`, `picker_viewport_slides_with_selection`                                                                                                                                                                                                                       |
| V3 BOUNDED ERRORS          | The provider error path (the "Response failed: op error ..." composition) truncates the body to 240 chars, strips HTML tags crudely (a bounded regex-free scan: cut at the first '<' — an HTML body's useful content for a human is the status line), and PREPENDS the effective request URL and status: e.g. "response failed: 404 at https://host/v1/chat/completions - <first 240 chars of the body with html cut>". The fetch_models errors get the same treatment. The credential value never appears in an error.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `generic.rs` `truncated_sanitized_body` + `call_generic` and `fetch_models` bounded error composition; tests `error_truncation_html_cut_and_bounded`, `error_truncation_10kb_html_body_is_bounded`, `fetch_models_error_is_bounded_and_has_url_status`                                                                                                                                                                                               |
| C2 THE PROTOCOL CLOSED SET | The `Protocol` enum in `siralos-core::composition` becomes three variants: `OpenAiCompletions`, `OpenAiResponses`, `AnthropicMessages` (default `OpenAiCompletions`). `profile_config` parses the three new string values; the legacy values are accepted as aliases: `openai-compatible` -> `OpenAiCompletions`, `anthropic` -> `AnthropicMessages` (documented in the parse); any other value -> the profile UNAPPLIED with a truthful diagnostic listing the three valid values. `write_profile_config`: the protocol validation accepts the three new values; written only when not the default (`openai-completions` omitted).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `composition.rs` `Protocol::parse` + `as_str`; `profile_config.rs` parse error lists three values; tests `profile_parse_protocol_matrix`, `write_includes_new_keys`                                                                                                                                                                                                                                                                                  |
| C3 THE PROTOCOL PICKER     | The ApiProtocol field becomes a SELECTABLE picker in the form (the ModelPicker pattern): on advancing into ApiProtocol, open a 3-item picker (`openai-completions`, `openai-responses`, `anthropic-messages`) with Up/Down navigation (wrapping) and Enter to select (the selection stored, advance to Model); Esc falls back to free-text entry (validated against the closed set with the human-readable error listing the three values). The picker renders inside the form modal like the model picker, with the selected entry highlighted. The pre-selected item: `openai-completions` (the default) — or the previously stored protocol if the user went Back and re-entered.                                                                                                                                                                                                                                                                                                                                                                                                                                  | `tui.rs` `ProtocolPicker`, `open_protocol_picker`, `handle_key` Up/Down/Enter/Esc + render; tests `protocol_picker_opens_with_three_items_default_zero`, `protocol_picker_up_down_wrap`, `protocol_picker_enter_selects`, `protocol_picker_esc_fallback_to_free_text`, `protocol_picker_preselects_stored_value_on_back`                                                                                                                             |

## 3. Criteria → Evidence

| Criterion                                                             | Evidence                                                                                                                                                                                                                      | Verdict |
| --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| Verbatim credential (public -> key:public, env: stays, empty -> None) | `verbatim_credential_public_stores_key_public` (public -> key:public), `verbatim_credential_env_form_stored_as_is`, `verbatim_credential_empty_is_none`, `verbatim_storage_*` + `write_profile_config` round-trip             | pass    |
| Resolution (key: -> Bearer, env: -> env lookup, bare -> compat)       | `credential_resolution_key_prefix_is_literal`, `credential_resolution_env_prefix_resolves_env_var`, `credential_resolution_bare_is_env_compat`                                                                                | pass    |
| Redaction (key:***, env:NAME)                                         | `provider_line_credential_present_absent` (key:*** not leak), `redaction_status_line_no_leak`, `verbatim_storage_*` check no raw value in display                                                                             | pass    |
| Viewport (45 items, selected always visible, indicator)               | `model_picker_viewport_selected_always_visible` (45 items 0..44), `protocol_picker_viewport_position_indicator`, `picker_viewport_slides_with_selection` (bounded 8)                                                          | pass    |
| Bounded errors (URL + status + <=240, no HTML dump, no cred)          | `error_truncation_html_cut_and_bounded` (10KB HTML -> <=240, no <), `error_truncation_10kb_html_body_is_bounded`, `fetch_models_error_is_bounded_and_has_url_status` + `fetch_models_bounded_get_redacts_credential_on_error` | pass    |
| Protocol picker flow                                                  | `protocol_picker_opens_with_three_items_default_zero`, `protocol_picker_up_down_wrap`, `protocol_picker_enter_selects`, `protocol_picker_esc_fallback_to_free_text`, `protocol_picker_preselects_stored_value_on_back`        | pass    |
| Determinism & stdio                                                   | `determinism` (same state + size -> identical buffer); `stdio_path_byte_unchanged_banner_tui_only` still byte-identical; full gate green                                                                                      | pass    |

## 4. Result

The verbatim credential, picker viewport, bounded errors, and protocol picker are complete: public works verbatim, and the protocol is picked from the three real wire protocols.
