---
title: "The TUI Polish Pass"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "106"
supersedes: []
---

# 118 — The TUI Polish Pass

Ticket [106](../tickets/106-tui-polish-pass.md) · entry review [117](117-tui-polish-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The polish pass implemented: the input latency fixed (the drain's inner wait was gating every redraw — now a zero-timeout drain draws immediately after the last queued key), and the visual pass adapted from the MiMo-Code reference — rounded borders, role-colored transcript, a session header with provider/model, a styled command palette, and a context-usage status readout; the render frames re-pinned at corpus v78; the stdio frontend byte-unchanged.

## 2. Implemented P1–P6 — as delivered

| ID  | Implementation                                                                                                                                                                                                                                                                                                                                                      | Evidence / note                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1  | LATENCY (priority one): the inner drain uses `poll(Duration::ZERO)` (only already-queued events; NEVER waits), and after the drain the draw runs immediately; the outer loop keeps ONE bounded idle poll (raise 15ms → 50ms) so the pane/status redraw still happens when idle. A keystroke is handled and drawn with no intermediate wait; submit path unchanged.  | `interactive.rs` drain restructured to `TUI_IDLE_POLL` 50ms + `TUI_DRAIN_POLL` ZERO + immediate draw; tests `tui_drain_poll_is_zero_and_idle_is_50ms` |
| P2  | HEADER BAR: a top header row (1 line): `" Siralos "` left, `"provider / model"` right (the same composed profile source the status line uses; absent → `"no provider configured"`), styled reversed/accent; transcript/input/status layout shrinks by 1 row. OFF-independent.                                                                                       | `tui.rs` header bar in `draw_with_pane`/`render_to_buffer_with_pane` 80×24; tests `header_renders_provider_model_present_and_absent`                  |
| P3  | ROLE COLORS: transcript lines styled by role — user echo lines (`"> ..."`) in Cyan with dim timestamps, assistant/system lines default, host notices (`unknown command`, `Approved.`/`Denied.`) in Yellow. Styles are part of deterministic TestBackend buffer.                                                                                                     | `tui.rs` `style_for_transcript_line`; tests `role_colors_user_vs_system`                                                                              |
| P4  | ROUNDED BORDERS + PALETTE STYLING: the context pane and the palette popup use `BorderType::Rounded`; palette gets bordered title-styled popup (`" commands "`) with matching prefix highlighted Yellow Bold; approval modal keeps backdrop (restyled rounded).                                                                                                      | `tui.rs` `BorderType::Rounded` for pane/palette/modal; tests `rounded_borders_on_pane_and_palette` + `palette_bounded_and_prefix_highlight`           |
| P5  | CONTEXT-USAGE STATUS: when the context subsystem is opted in AND built, the status line gains `"ctx <assembled>/<4096>"` (assembled unique-digest total from the same `ContextMetrics` the pane uses — single source `context_assembled_total`) appended to the composed status; absent → unchanged.                                                                | `tui.rs` `append_context_usage`/`compose_status_line_with_context`; `interactive.rs` wiring; tests `status_usage_readout_present_and_absent`          |
| P6  | RENDER RE-PIN: the `tui-render` scenarios re-pin at corpus v78 (header changes layout rows; styles ride frames; record builder 80×24 with fixed fixture timestamps). Manifest `corpusVersion` 78, count 357 unchanged, digests refresh; 4 expectation records updated in place via throwaway OS-temp `canonicalRecordDocument`; `context-benchmark` BYTE-IDENTICAL. | `contract.mjs`/`harness.rs` v78 (bumped to 80 from 79 baseline — see deviation), `manifest.json` 80, `expectations.json` 4 records re-pinned          |

## 3. Criteria → evidence

| Criterion                                                      | Evidence                                                                                                                                                                                                                                                                                                               | Status |
| -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Latency structure proof (P1)                                   | `tui.rs` `TUI_DRAIN_POLL = ZERO`, `TUI_IDLE_POLL = 50ms`; `interactive.rs` outer poll 50ms + inner ZERO drain + immediate draw; test `tui_drain_poll_is_zero_and_idle_is_50ms` pins the constants.                                                                                                                     | pass   |
| Header / role colors / rounded borders / palette scope (P2–P4) | Header 1 line with `Siralos` + provider/model, reversed Cyan/Black; role colors Cyan user / Yellow notices vs White system via `style_for_transcript_line`; pane/palette/modal `BorderType::Rounded`; palette prefix Yellow Bold; tests `header_renders_*`, `role_colors_*`, `rounded_borders_*`, `palette_bounded_*`. | pass   |
| Context-usage status (P5)                                      | `append_context_usage` appends `ctx <assembled>/4096` from `context_assembled_total` (last `TickRecord.assembled_unique_total`) only when metrics Some; `compose_status_line_with_context` wires it in `interactive.rs` for working/ready/status; tests `status_usage_readout_present_and_absent`.                     | pass   |
| Render re-pin (P6)                                             | `tui-render` 4 scenarios re-pinned 80×24 with FIXED fixture timestamps; `contract.mjs`/`harness.rs` 80, `manifest.json` 80/357, digests refreshed via `regenerate-corpus-manifest.mjs`; `expectations.json` 4 records updated in place via throwaway `canonicalRecordDocument`; `context-benchmark` BYTE-IDENTICAL.    | pass   |
| Red lines held                                                 | Sanitizer single output boundary; input queue single read owner; approvals host-gated; no threads; no persistence; stdio frontend byte-unchanged (existing tests); OFF byte-transparency for pane; decisions ≤116 untouched.                                                                                           | pass   |
| Determinism & stdio-unchanged proof                            | `draw_determinism_same_state_identical_buffer` + `t4_frames_still_deterministic_with_header` + `t4_frame_*` snapshots byte-equal; `context_pane_tests` OFF byte-identical; stdio path tests green.                                                                                                                     | pass   |

## 4. Result

The polish pass is complete: keystrokes render without the drain-wait gate, the TUI carries the rounded/role-colored/header layout with the styled palette and the context-usage readout, the frames are re-pinned at corpus v78, and the stdio frontend is byte-unchanged.
