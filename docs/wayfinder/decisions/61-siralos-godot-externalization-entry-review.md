---
title: "siralos-godot Externalization Entry Review — Freeze the Standalone Repo Contract"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "61"
supersedes: []
---

# Decision 61 — siralos-godot Externalization Entry Review

**Ticket:** [61 — siralos-godot Externalization Entry Review](../tickets/61-siralos-godot-externalization-entry-review.md) · label `wayfinder:grilling` HITL
**Map:** [Siralos Roadmap](../siralos-roadmap.md)
**Blocked by:** [60 — Research](../decisions/60-siralos-godot-externalization-research.md)

> **PASS — HITL 2026-08-31: C1–C6 approved** (scope verbatim, repo public `github.com/CrimsX/siralos-godot`, pin `git rev + Cargo.lock + cargo deny`, evidence via additive expectations, ordering 60→61→bootstrap→pin). Authorizes repo-bootstrap + verbatim move as next implementation slice.

## Question

Stage 6 is Verified at `e2c3540` (differential 315/315 at v52/320, 81 expectations, pinned v32 oracle untouched). `crates/siralos-godot` holds the 6+3 R8/R9 surfaces plus Stage 4.3 `runtime_adapter` in-repo behind `siralos:domain-abi@1.0.0` per decisions 34/37. The lean contract froze `github.com/CrimsX/siralos-godot` as **FUTURE / NOT DUE** until `siralos.toml → siralos.lock` + `cargo deny` was demonstrated — now both Verified (Stage 5 `c2c30f0`, 5.4 `0a6d592`). How is the externalization frozen so an executor can implement it without rediscovering scope, authority, parity, or distribution boundaries?

## Contract (C1–C6, approved 2026-08-31)

| #   | Clause                     | Frozen contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Scope — what moves         | **Move `crates/siralos-godot` verbatim** (41 files listed in decision 60 §1: `src/godot/mod.rs`, `api.rs`, `capabilities.rs`, `compatibility.rs`, `diagnostics.rs`, `digest.rs`, `engine_profile.rs`, `events.rs`, `gdscript.rs`, `inspector.rs`, `installations.rs`, `knowledge.rs`, `limits.rs`, `lsp.rs`, `probe.rs`, `probes.rs`, `project.rs`, `selection.rs`, `version.rs`, `development/*` 4, `impact/*` 3, `scene/*` 10, `scene_mutation/*` 2, `runtime_adapter.rs` + `src/lib.rs` + `Cargo.toml`). `siralos-godot → siralos-core` only, `forbid(unsafe_code)` (`src/lib.rs:8`), `edition.workspace = true (2024)`, `publish = false`. No new surface, no `available` flip (`scene_mutation::apply` etc. stay `UNAVAILABLE` per decisions 41–45), zero new `std::process`/`Command::new().spawn()` paths. `siralos-core::language` / `siralos-core::domain` / `wit/domain-abi.wit` stay in monorepo. Refactoring under parity per ADR 0032. |
| C2  | Standalone repo shape      | New repo `github.com/CrimsX/siralos-godot` — own `Cargo.toml` workspace (`resolver = "3"`), same `rust-toolchain.toml`/`rustfmt.toml` (`channel` pinned, `max_width = 79`), `README.md`/`CONTRIBUTING.md`, `LICENSE` same as Siralos (currently no published license — record explicitly as `no-license` with owner `CrimsX`), `ARCHITECTURE.md` pointer, `wit/domain-abi.wit` documented as canonical interface (remains in monorepo until ADR 0036 §32 unification). Repo `publish = false` initially; crates.io publish is NOT this slice.                                                                                                                                                                                                                                                                                                                                                                                                       |
| C3  | Distribution & pinning     | Monorepo consumes external crate via **cargo git dependency pinned to exact commit SHA**: `siralos-godot = { git = "https://github.com/CrimsX/siralos-godot", rev = "<40-hex-sha>" }` plus `Cargo.lock` update + `cargo deny` (`advisories`/`licenses`/`bans`/`sources`) — never `branch = "main"` nor committed `path =` override. Local dev may use `[patch.crates-io]`/`[replace]` `path =` override only with evidence, never committed. `siralos.toml [plugins.godot] { path, digest }` + `siralos.lock` digest via `DomainHost::install` (lstat + isFile + maxFileSha256Bytes + is_path_within + SHA-256 before `Enabled→Active` per decisions 38/39) stays authority gate. `git submodule` and registry publish explicitly rejected for this slice (lean bias ADR 0036 §35-37).                                                                                                                                                              |
| C4  | Parity & evidence          | Existing `godot-*` corpus subjects stay at required parity (10 subjects 315/315 at v52 retained). Monorepo harness continues to emit audit at v52+ next bump — external crate behavior covered via **digest-bound additive expectation records** in `tests/differential/evidence/post-freeze/expectations.json` per decision 40 C7 (81 records at `e2c3540`, additive `expectationCoverage` provenance), not by re-running TypeScript oracle. No corpus bump in this entry-review ticket; subsequent move slice bumps `CORPUS_VERSION` in `crates/siralos-cli/src/harness.rs:12` + all `contract.mjs` sites + strict-loader asserts together with manifest version per map Notes (must run `cargo test -p siralos-cli --lib --all-features`).                                                                                                                                                                                                       |
| C5  | Monorepo linkage & shim    | During move, `crates/siralos-godot` in monorepo becomes **thin re-export shim** (or is removed after verification) that forwards to git-pinned crate — `scripts/check-rust-architecture.mjs:29-34` `EXPECTED_CRATES`/`64-67` `ALLOWED_DEPENDENCIES` updated without widening `FORBIDDEN_CORE_SYMBOL_PATTERN:37-38` (`siralos-core → siralos-godot` stays forbidden, `siralos-godot → siralos-core` stays only edge). Shim removed only after `cargo test --workspace --all-features` + `npm run check:differential` 315/315+ retained and `npm run check` green.                                                                                                                                                                                                                                                                                                                                                                                    |
| C6  | Lean guardrails & ordering | No marketplace, no auto-acquisition, no plugin dependency graph, no general Hooks/TaskGraph/Fleet (ADR 0036 §51). Ordered slices after PASS: (1) 60 research (done) → (2) 61 entry review (this) → (3) repo bootstrap + verbatim move (next, authorized) → (4) monorepo pin + shim removal + doc/CI sweep (frozen but not authorized until 3 Verified). Each slice entry-reviewed, one per session, budget one coherent pass + up to two repairs. `npm run check` (fmt/clippy/test/check:rust/arch/differential) stays green; any new spawn path is a failure.                                                                                                                                                                                                                                                                                                                                                                                      |

## HITL answers (2026-08-31, recorded verbatim)

- **Q1 Scope** — approved: _"Approve verbatim"_ — move exactly `crates/siralos-godot` verbatim as drafted, no reshuffle, no new surface, refactoring under parity only.
- **Q2 Repo identity** — approved: _"Approve public"_ — `github.com/CrimsX/siralos-godot` public, same toolchain, `LICENSE` same as Siralos (unpublished, record explicitly), `publish=false`, WIT canonical stays in monorepo.
- **Q3 Pinning** — approved: _"Approve git rev"_ — exact SHA pin via `cargo git rev + Cargo.lock + cargo deny`, never `branch = "main"` or committed `path =`; `git submodule`/registry rejected for this slice per lean bias.
- **Q4 Evidence** — approved: _"Approve expectations"_ — additive expectation-coverage per decision 40 C7 (81 records at v52), no corpus bump in this decision, thin shim during migration, zero spawn preserved.
- **Q5 Ordering** — approved: _"Approve ordering"_ — 4-step ordered slice sequence 60→61→bootstrap→pin, one per session.

**Entry review is PASS — externalization contract frozen, repo-bootstrap + verbatim move authorized as next implementation slice** against this frozen C1–C6 and the 5 HITL answers above. Monorepo pin slice remains frozen but not authorized until bootstrap Verified.

## Self-loop verification

| Criterion                                        | Evidence                                                                                                          | Verdict |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------- | ------- |
| Research consumed                                | decision 60 fact sheet (41-file inventory, boundary via `check-rust-architecture.mjs:29-67`, 10 subjects 315/315) | pass    |
| C1–C6 frozen without new behavior                | table above, decision 34/37 verbatim scope, zero spawn                                                            | pass    |
| Pinning is deterministic & supply-chain-bound    | `git rev` + `Cargo.lock` + `cargo deny`, reject `branch`/`path`                                                   | pass    |
| Evidence stays via additive expectations (40 C7) | `tests/differential/evidence/post-freeze/expectations.json` 81 records                                            | pass    |
| Human decided 5 material cuts                    | HITL answers 2026-08-31 verbatim                                                                                  | pass    |

## Implementation record

_Authorized at 2026-08-31 — no code in this decision. Next slice repo-bootstrap + verbatim move is authorized as next implementation slice against frozen C1–C6._
