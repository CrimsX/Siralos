---
title: "TUI T4: Render-Model Pinning"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "103"
supersedes: []
---

# 108 — TUI T4: Render-Model Pinning

Ticket [103](../tickets/103-siralos-tui.md) · entry review [103](103-siralos-tui-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The final TUI slice: the render model is pinned into the differential corpus (four TestBackend frame snapshots as candidate-authored expectations) and the dispatch debt from the T2/T3 ledgers is settled — the slash-command dispatch and key-edit handling are single shared definitions; the session-composition residual, if legitimately per-frontend, is recorded with its why. This closes ticket 103 pending its roll-up.

## 2. Design (A1–A3 + B1–B4 + the final consolidation ledger) — as implemented

| ID  | Design                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Evidence / note                                                                                                                                                                   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | NEW SUBJECT tui-render: `harness.rs` gains `SUBJECT_TUI_RENDER` + `tui_render_record(scenario_id, input)` — the record builder constructs a `TuiState` from the scenario input (`transcript_lines`, `input` text, `status` text, `scroll_offset`, `pending_approval` lines or none, pane enabled + metrics snapshot `{counters, ring lines, tool activity lines}` or none), renders over a FIXED TestBackend viewport (80x24) via the production `draw`/`draw_with_pane` path, and serializes the rendered frame buffer canonically (one line per buffer row, exact characters, trailing spaces trimmed per row). The builder is DETERMINISTIC — same input -> byte-equal record.                                                                                                                                                                                                                                                                                                   | `harness.rs` `tui_render_record`, `TUI_RENDER_WIDTH`/`TUI_RENDER_HEIGHT`, `validate_tui_render_input`, `SUBJECT_TUI_RENDER` dispatch                                              |
| A2  | SCENARIOS (4): `tui-render.shell-basic` (transcript + input + status, no pane, no modal); `tui-render.transcript-scroll` (many lines with a scroll offset); `tui-render.approval-modal` (pending approval modal over a dimmed transcript); `tui-render.context-pane` (pane enabled with a metrics snapshot: counters, ring lines, tool activity). Corpus v76: manifest corpusVersion 76, count 357, digests refresh; strict_loader 357; `contract.mjs` CORPUS_VERSION 76; `protocol.mjs` validator for the tui-render record shape (frame lines array + scenario echo); the 4 expectation records via the throwaway OS-temp node canonicalRecordDocument script (delete after; diff surgical: tui-render records added only; context-benchmark BYTE-IDENTICAL to v75).                                                                                                                                                                                                              | `tests/differential/corpus/tui-render.*.json` (4 files); `ALLOWED_SUBJECTS`/`withInput`/`validateCompletedResult` for `tui-render`; expectations 114 -> 118 records               |
| A3  | IMPORTANT: the render path exercised is the PRODUCTION draw code (no render re-implementation inside the harness) — the harness only constructs `TuiState` inputs and serializes the produced buffer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `tui_render_record` calls `tui::draw`/`draw_with_pane` over `TestBackend`; no widget/layout duplication in `harness.rs`                                                           |
| B1  | SLASH-COMMAND DISPATCH: the match shared by `handle_tui_line` and the stdio loop is extracted into ONE parser both loops call — `parse_slash_command` (the `SlashCommand` vocabulary: Context/Tools/Domains/Exit/DomainsAdd/DomainsEnable/DomainsActivate/Prompt, including the bare/prefixed `/domains-*` pairs) plus the shared render helpers `render_context_segment`/`render_tools_segment` and the two thin per-frontend writers `dispatch_stdio_command`/`dispatch_tui_command` (one match on the parsed command, two sinks: `writer` vs `TuiSink`). Test-pinned same-function (the T2/T3 fn-pointer pattern). The two dispatches had NO genuinely different arms — the old `handle_tui_line` match and the stdio match were arm-for-arm identical (same `/context`+audit gate, same `/tools`, same `/domains-*`, same prompt path) — so the residual per arm is ZERO; the only per-frontend difference is the sink (`write_all`+`map_err(Io)` vs `let _ = sink.write_all`). | `interactive.rs` `SlashCommand`, `parse_slash_command`, `dispatch_stdio_command`, `dispatch_tui_command`; `t4_dispatch_same_parse_both_loops_call`; old `handle_tui_line` deleted |
| B2  | KEY-EDIT HANDLING: the inline key-edit handling in the live loop routes through the same `handle_key` the T2 modal work provided — the loop calls `crate::tui::handle_key` for ALL non-modal keys (Enter submits; Char/Backspace/PageUp/PageDown edit in place); the inline `match key.code` duplicate (Enter/Backspace/Char/PageUp/PageDown arms) is DELETED. Residual: the TUI loop keeps only what `handle_key` cannot own — the PageUp viewport lookup (`terminal.size`, live terminal state) and the Ctrl+C exit — recorded below.                                                                                                                                                                                                                                                                                                                                                                                                                                             | Live loop calls `handle_key`; `t4_key_routing_calls_the_shared_handle_key`; no `input.pop`/`input.push`/`scroll_offset` inline code remains in the loop                           |
| B3  | SESSION COMPOSITION: extraction was attempted AND SUCCEEDED — the two compositions were genuinely the same shape (the normalized diff of the two blocks was ZERO: 244 non-comment lines each, token-identical), so `compose_session` is the single shared helper both loops call, returning the `SessionComposition` bundle (workspace root, tool-definitions snapshot, policy, application, hosts, manifests, profile/context wiring, replay-store flush inputs). The honest residual is PERMANENT per-frontend composition of the terminal I/O only: the stdio loop owns `reader`/`writer` generics; the TUI loop owns `TerminalGuard`/`Terminal`/`TuiState`/`TuiSink` — recorded with the why (each frontend's I/O handles cannot cross the boundary; `unsafe_code = "forbid"` bars pointer tricks, so the bundle carries the immutable definitions snapshot instead of the borrowed registry). No forced unification beyond this.                                               | `interactive.rs` `compose_session`, `SessionComposition`; `t4_compose_session_is_the_single_shared_definition`; final ledger in `tui.rs` composition note                         |
| B4  | EVERY consolidation keeps the stdio path byte-unchanged (existing tests prove it) and the TUI behavior identical (the T4 frame snapshots prove it — the snapshots were pinned AFTER the consolidation so the pinned frames are the consolidated behavior).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `cargo test -p siralos-cli --lib --all-features` 114 pass (all pre-existing stdio tests green); T4 frame tests run against the consolidated code                                  |

### Final consolidation ledger (shared vs residual, with the why)

| Shared (single definition both loops call)                                                                                                                                                                                                                                                                                                                                                                                                     | Residual (per-frontend, with the why)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `compose_session` + `SessionComposition` (whole composition block) · `parse_slash_command` + `SlashCommand` (dispatch vocabulary) · `render_context_segment` + `render_tools_segment` (segment renders) · `dispatch_stdio_command` + `dispatch_tui_command` (thin writers, one parsed match each) · `handle_key` (key edits) · `flush_record_replay` (exit flush) · T2/T3 carry-over: approval helpers, `context_audit_session`, pane builders | stdio owns `reader`/`writer` generics (the stdio frontend IS blocking line I/O — cannot be shared with an event-poll pump); TUI owns `TerminalGuard`/`Terminal`/`TuiState`/`TuiSink` (terminal state cannot cross into stdio); Ctrl+C-exit + PageUp `terminal.size` lookup + modal verdict lines stay in the TUI loop (they need live terminal state); `/tools` renders from the immutable definitions snapshot (the registry is borrowed by the live application and `Box<dyn Tool>` is not `Clone`, and `unsafe_code = "forbid"` — the snapshot is byte-equal forever because the registry is immutable after construction) |

## 3. Criteria → evidence

| Criterion                                                                       | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Status |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| Pinned frame format (canonical frame buffer serialization)                      | `{ scenario: {id, transcript_lines, input, status, scroll_offset, pending_approval, pane_present}, viewport: {80, 24}, frame: [24 row strings] }` — one line per buffer row, exact characters, trailing spaces trimmed per row; `protocol.mjs` `validateTuiRenderResult` pins the shape (24 rows, ≤80 chars, fixed 80x24 viewport, scenario echo)                                                                                                                                                            | pass   |
| 4 scenarios (shell, scroll, approval modal, context pane)                       | `tui-render.shell-basic` (3 transcript lines + `> help me` + `ready`, no pane/modal) · `tui-render.transcript-scroll` (40 lines, offset 8 → rows `line 10`…`line 31`, ends hidden) · `tui-render.approval-modal` (modal `Approval required (y/n, Esc deny)` + 2 request lines over dimmed transcript, status `awaiting approval`) · `tui-render.context-pane` (40-col pane: 12 counters + 3 ring + 3 activity lines beside the transcript) · corpus v76/357 · expectations 114 → 118 (4 added, surgical)     | pass   |
| Final consolidation ledger (shared vs residual with the why)                    | Table in §2: 6 shared definitions + T2/T3 carry-over vs the terminal-I/O residual with per-row why; `tui.rs` composition note + `interactive.rs` loop docs record the same ledger                                                                                                                                                                                                                                                                                                                            | pass   |
| Test names (4 frame snapshots + dispatch proof + key proof + composition proof) | `t4_frame_shell_basic_matches_harness_record` · `t4_frame_transcript_scroll_matches_harness_record` · `t4_frame_approval_modal_matches_harness_record` · `t4_frame_context_pane_matches_harness_record` (unit TestBackend mirrors of the 4 scenarios + harness `tui-render` records) · `t4_dispatch_same_parse_both_loops_call` (same-function proof) · `t4_key_routing_calls_the_shared_handle_key` (loop-calls-it proof) · `t4_compose_session_is_the_single_shared_definition` (shared-composition proof) | pass   |
| Corpus v76/357 + strict_loader 357                                              | `CORPUS_VERSION` 76 in `harness.rs` + `contract.mjs`; `load_corpus` count 357; `strict_loader_accepts_the_checked_in_digest_bound_corpus` asserts 357                                                                                                                                                                                                                                                                                                                                                        | pass   |
| stdio-unchanged proof (existing tests pin the byte path)                        | All pre-existing stdio tests green after the consolidation (`context_before_prompt_is_truthful…`, `prompt_then_context…`, `domains_*`, `profile_*`, `context_system_*`, 114 cli pass incl. 7 new); the `/tools` render uses the immutable definitions snapshot (byte-equal to `registry.definitions()` — registry immutable after construction)                                                                                                                                                              | pass   |
| Benchmark byte-identity (context-benchmark record unchanged v75 → v76)          | The v76 expectations diff touches tui-render records ONLY; the `context-benchmark` record bytes are identical to v75 (verified by diffing the record before/after the corpus bump)                                                                                                                                                                                                                                                                                                                           | pass   |
| Annotations (map + 3 status docs)                                               | `siralos-roadmap.md` T4 index bullet after the T3 line; `AGENTS.md`/`README.md`/`docs/development/PROJECT_CONTEXT.md` status fragments after the decision 107 anchor                                                                                                                                                                                                                                                                                                                                         | pass   |
| All existing tests keep compiling                                               | Full `cargo test --workspace --all-targets --all-features` green (four crate counts quoted in the report); no signature breaks outside the crate (`parse_slash_command`/`dispatch_*`/`compose_session` are private; `draw` unchanged)                                                                                                                                                                                                                                                                        | pass   |

## 4. Result

T4 is complete: the TUI render model is corpus-pinned and the dispatch debt is settled (ledger: shared — compose_session, parse_slash_command + render/dispatch helpers, handle_key, flush_record_replay plus the T2/T3 carry-over; residual — per-frontend terminal I/O only, recorded with its why); ticket 103 is complete pending its roll-up record.
