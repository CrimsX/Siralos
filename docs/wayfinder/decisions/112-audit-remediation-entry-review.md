---
title: "The Audit Remediation Pass Entry Review"
label: "wayfinder:decision"
status: accepted
date: 2026-08-31
ticket: "105"
supersedes: []
---

# 112 — The Audit Remediation Pass Entry Review

Ticket [105](../tickets/105-audit-remediation-pass.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The mechanical remediation batch from the external-model project audit is authorized (the context.inspect nested-path defect and the double-scroll defect as priority); the six HITL questions the audit raised are presented OPEN below and are explicitly NOT implemented in this pass — they need user direction.

## 2. Authorized batch R1–R9 and OPEN HITL questions

### Authorized mechanical batch (R1–R9)

| ID  | Remediation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Disposition |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| R1  | **inspect slash fix (HIGH):** `parse_node_id_input` allows `/` matching `parse_expand_input` bounds (non-empty, `<= 256` bytes, still rejecting NUL); keep rejecting `\` for path-traversal hygiene and record it. Regression test: scan nested fixture then inspect by real path id succeeds; tripwire tests on all three tools with `a/b.txt`.                                                                                                                                                                                            | authorized  |
| R2  | **double-scroll fix:** delete the TUI loop inline `PageUp`/`PageDown` arms (`interactive.rs` `~1940–1969` -> `_ => {}`); keep `handle_key` (`tui.rs:1279–1286`) as sole `+-10` applier; viewport clamp moves into `handle_key` (pass viewport height) or a post-call clamp — pin with tests (`single +-10`, clamped).                                                                                                                                                                                                                       | authorized  |
| R3  | **single catalog:** one catalog delegates to the other (`interactive.rs:slash_command_catalog` delegates to `tui::command_catalog` or vice versa — pick cleaner direction) + cross-equality test.                                                                                                                                                                                                                                                                                                                                           | authorized  |
| R4  | **echo sanitizer:** `sanitize_for_display` on the user echo before `push_line_stamped`; skip echo for empty input (matching stdio).                                                                                                                                                                                                                                                                                                                                                                                                         | authorized  |
| R5  | **status sanitizer:** `compose_status_line` sanitizes provider/model interpolations.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | authorized  |
| R6  | **compose ordering:** `compose_session` runs BEFORE `TerminalGuard::enter` in the TUI path (startup diagnostics visible; verify no compose step needs the terminal).                                                                                                                                                                                                                                                                                                                                                                        | authorized  |
| R7  | **harness gate mirror:** `context_session_record` (`harness.rs:10274–10281`) mirrors the live seam (`interactive.rs:1032–1041`) — `declare_profile` + `compose_effective_policy` over fixture host rules, gating `enabled` on `applied_profile.is_some()`; update affected `context-session` records in place via throwaway OS-temp node `canonicalRecordDocument` script (delete after) if outputs change; corpus version bump ONLY if records change (already `v77` after decision 111; if changed becomes `v78`, count `357` unchanged). | authorized  |
| R8  | **docs staleness sweep:** update `README.md` (`~297–298` frozen at `v52`), `docs/development/PROJECT_CONTEXT.md` (`~13` stale corpus version), `AGENTS.md` `/evolve` wording (clarify display-only discovery since decision 111 while execution stays host-gated/out-of-milestone), and any `ARCHITECTURE.md` `/evolve`-future claim: corpus counts -> `v77`/`357`, TUI-default entry, external godot at `1bf2ca3` self-contained (`FUTURE` claims), usability-pass features. Only claims current code contradicts.                         | authorized  |
| R9  | **tripwire tests** are part of R1 (nested-path `a/b.txt` on inspect/search/expand).                                                                                                                                                                                                                                                                                                                                                                                                                                                         | authorized  |

### OPEN HITL questions (NOT implemented — awaiting user direction)

| ID  | Finding                                                                                                                                                                          | Options                                                                | Status                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ----------------------- |
| Q1  | **Approval wiring intent:** T2 modal machinery and stdio approval shims are live-unwired — nothing ever sets `pending_approval`.                                                 | Wire host approval request into the session, or record as future-seam? | awaiting user direction |
| Q2  | **Tool-snapshot staleness policy:** search tier bonuses and inspect tiers freeze at build time.                                                                                  | Accept+document / re-register per prompt / split the seam?             | awaiting user direction |
| Q3  | **Unknown-command canonical behavior:** TUI-only honesty line vs stdio prompt fallthrough — which is canonical?                                                                  | Choose TUI honesty as canonical or keep stdio fallthrough?             | awaiting user direction |
| Q4  | **Fourth live atomic writer:** `domain/manifest.rs` `write_record_document` outside the three approved modules — accept+document, or restrict?                                   | Accept+document, or restrict to the three approved modules?            | awaiting user direction |
| Q5  | **Provider-identity-in-lock:** provider/model/endpoint bypass the core composition digest so the session lock never drifts on routing changes — bind or document as intentional? | Bind into digest, or document as intentional routing-exempt?           | awaiting user direction |
| Q6  | **Stderr sanitizer scoping:** stderr diagnostics bypass the sanitizer — document boundary as model/tool-output sinks only?                                                       | Document as model/tool-output sinks only, or extend to stderr?         | awaiting user direction |

## 3. Criteria → evidence

| Criterion                    | Evidence                                                                                                                                                                                                                                                                                                                                                 | Status |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Inspect defect root-caused   | `crates/siralos-adapters/src/tool/context.rs:parse_node_id_input ~573–595` rejects `/` while `crates/siralos-adapters/src/tool/context_scan.rs:303–313` scan node ids ARE slash paths; derivation only fires on `Success` so demand events never fire for rejected inspects; tests used slash-free fixture ids, hiding the defect. Fix authorized as R1. | pass   |
| Double-scroll root-caused    | `crates/siralos-cli/src/tui.rs:1279–1286` `handle_key` applies `+-10` AND `crates/siralos-cli/src/interactive.rs:1940–1969` loop arms apply `+-10` a second time. Fix authorized as R2 (delete loop arms, keep `handle_key` as sole applier with clamp).                                                                                                 | pass   |
| Harness gate gap root-caused | `crates/siralos-cli/src/harness.rs:10274–10281` `context_session_record` `enabled` misses the applied-profile gate the live session enforces at `crates/siralos-cli/src/interactive.rs:1032–1041` (`effective.applied_profile.is_some()`). Fix authorized as R7 mirror.                                                                                  | pass   |
| Red lines held               | Sanitizer is the single output boundary; stdio session/render behavior unchanged except where noted (R4/R5 sanitizer gaps inside the boundary, R6 ordering, R2 scroll); decisions `<= 111` untouched; mechanical batch only.                                                                                                                             | pass   |
| HITL questions scoped OUT    | Q1–Q6 presented OPEN above, explicitly NOT implemented in this pass; they need user direction per session HITL.                                                                                                                                                                                                                                          | pass   |
| Ticket + decision linkage    | Ticket 105 `supersedes []`, decision 112 `ticket "105" supersedes []`, both dated `2026-08-31`; ticket body records the five audit chunks with file:line evidence and defers the six HITL questions to this entry review.                                                                                                                                | pass   |

## 4. Result

Entry review PASS: the mechanical batch R1–R9 is authorized; the six HITL questions await user direction and are explicitly out of this pass's scope.
