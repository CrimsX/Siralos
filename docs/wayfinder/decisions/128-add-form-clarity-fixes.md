---
title: "The Provider Add-Form Clarity Fixes"
label: "wayfinder:decision"
status: accepted
date: "2026-08-31"
ticket: "111"
supersedes: []
---

# 128 — The Provider Add-Form Clarity Fixes

Ticket [111](../tickets/111-add-form-clarity-fixes.md) · entry review [127](127-add-form-clarity-entry-review.md) · Map.

> **User-directed 2026-08-31 (session HITL).** The add-form clarity fixes implemented: descriptive field labels with examples, human-readable error messages, and per-field descriptions — the form is now self-explanatory and the user can complete it without external documentation.

## 2. Implemented I1–I3 — as delivered

| ID  | Implementation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Evidence / note                                                                                                                                                                                                    |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| I1  | DESCRIPTIVE LABELS + DESCRIPTIONS: updated `provider_add_form_lines` to render the descriptive labels (D1) — "provider name (e.g. openai, example-vendor):", "model (e.g. model-a, gpt-4o):", "credential env var (e.g. OPENAI_API_KEY):", "endpoint URL (optional, e.g. https://api.openai.com/v1):" — and a dim description line below each label (D3): provider "the name you'll use to identify this provider", model "which model to use for completions", credential env "the environment variable that holds your API key (set it before starting Siralos)", endpoint "the API URL (leave empty for the default)". `ProviderAddField::label` and `description` updated; the form modal grows from 65% to 75% height to accommodate the extra lines.                      | `tui.rs` `ProviderAddField::label`, `description`, `provider_add_form_lines`, `draw_with_pane`/`render_to_buffer_with_pane` modal 65×75; tests `provider_add_form_descriptive_labels`, `field_descriptions_render` |
| I2  | HUMAN-READABLE ERRORS: rewrote ALL validation error messages in `validate_provider_name`, `validate_model_name`, `validate_credential_env_name`, `validate_endpoint_value` to the human-readable strings (D2): provider "Provider name must be lowercase letters, numbers, hyphens, or underscores (e.g. openai, example-vendor)", model "Model name must be printable and between 1 and 256 characters (e.g. model-a, gpt-4o)", credential "Credential env var must be uppercase letters, numbers, and underscores (e.g. OPENAI_API_KEY) - set this variable with your API key before starting Siralos", endpoint "Endpoint must be a valid URL starting with https:// or http:// (e.g. https://api.openai.com/v1)". The VALIDATION RULES are unchanged — only the error text. | `tui.rs` validation fns; test `validation_errors_are_human_readable` asserts literal new strings, no regex                                                                                                         |
| I3  | TESTS UPDATED: updated the tests that asserted the old cryptic error strings to assert the new human-readable strings; added coverage for labels, descriptions, and the unchanged validation logic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `tui.rs` `validation_errors_are_human_readable`, `validation_still_rejects_invalid_input`, `provider_add_form_descriptive_labels`, `field_descriptions_render`                                                     |

## 3. Criteria -> evidence

| Criterion                                                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Verdict |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| Descriptive labels render with examples                  | `render_to_buffer` frame contains "provider name (e.g. openai, example-vendor)", "model (e.g. model-a, gpt-4o)", etc.; `ProviderAddField::label` returns descriptive strings; test `provider_add_form_descriptive_labels` green                                                                                                                                                                                                                                                                                                                                                                                                           | pass    |
| Dim description lines render below each label            | `render_to_buffer` frame contains "the name you'll use to identify this provider", "which model to use for completions", etc. as dim lines; test `field_descriptions_render` green                                                                                                                                                                                                                                                                                                                                                                                                                                                            | pass    |
| Human-readable errors — literal new strings, no regex    | `validate_provider_name` -> "Provider name must be lowercase letters, numbers, hyphens, or underscores (e.g. openai, example-vendor)"; `validate_model_name` -> "Model name must be printable and between 1 and 256 characters (e.g. model-a, gpt-4o)"; `validate_credential_env_name` -> "Credential env var must be uppercase letters, numbers, and underscores (e.g. OPENAI_API_KEY) - set this variable with your API key before starting Siralos"; `validate_endpoint_value` -> "Endpoint must be a valid URL starting with https:// or http:// (e.g. https://api.openai.com/v1)"; test `validation_errors_are_human_readable` green | pass    |
| Validation still rejects invalid input (rules unchanged) | Same regex/bounds still enforced; typing a URL in Provider fails with human-readable error, valid identifiers pass; test `validation_still_rejects_invalid_input` green                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | pass    |
| Modal grows to accommodate descriptions                  | `draw_with_pane` and `render_to_buffer_with_pane` use `centered_rect(65, 75, ...)` for the add-form modal (was 65×65)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | pass    |
| Red lines + stdio-unchanged proof                        | Sanitizer single output boundary retained; no threads; no persistence; stdio frontend byte-unchanged except additive form clarity; decisions <= 126 untouched; `cargo test` green                                                                                                                                                                                                                                                                                                                                                                                                                                                             | pass    |

## 4. Result

The clarity fixes are complete: every field explains what to type, errors are human-readable, and the user can complete the form without external documentation.
