# Wayfinder Map — Siralos Roadmap (Stage 3R R7 → R12 → Stage 4 Entry)

## Destination

A verified, decision-ready route from the current R7.5 CLI candidate (pending independent review) through **R7 Verified** to the **R12 retirement/retention decision**, making Stage 4–6 scope and entry conditions unambiguous so any executor can take the next ticket without rediscovering authority, parity, or lean-product boundaries.

The map is done when no decision remains before an executor could: (1) promote R7.5 through independent review, (2) sequence R8–R11 with frozen differential subjects, and (3) state the exact R12 evidence that opens Stage 4. The destination is a decision/thin spec, not the implementation itself.

## Notes

- Domain: Siralos — Stage 3R migration (TypeScript oracle → Rust candidate), lean product model ADR 0036.
- Current position: R1–R13 Verified (72e20be), **R12 Verified (retired)** at 4bef901 (audit v31 e24f4bb, 231/231) — Rust is now the sole source of truth. Godot source move is **complete** (the 6+3 R8/R9 surfaces now live in crates/siralos-godot; siralos-core is domain-neutral again — refactoring under parity, 231/231 v31 retained, all gates green). Add Plugin UI slice is **complete** at `7fe9b66` (decision 38) + repair `0e78b41` + oracle-resolution `4b84265`. Enable/Activate Host-gated UI is **complete** at `978ac07` (decision 39: `/domains-enable` + `/domains-activate`, empty vs declared authority, memory-only until Activate, typed stay-installed; catalog 51 both sides). Stage 4.1 corpus **v32 reconciliation is **complete** at the differential commit (`runtime-execution` + `runtime-evidence` subjects landed, `CORPUS_VERSION` 32, audit 234/234 applicable required, 4 skips, 0 informational). TypeScript archive removal is **complete** per [decision 40](decisions/40-typescript-archive-removal.md) (freeze v32 234/234 pinned at `tests/differential/evidence/typescript-freeze-v32/`; removal commit `e6c49f1` — corpus v33 `01ba53a…`, live `apps/` + `packages/` removed, differential runs pinned with opt-in freeze-SHA replay, `npm run check` is now Rust + pinned differential; review repairs `72c6dff` + `ddbe148`). External siralos-godot repo stays **FUTURE**. The **Godot Runtime Adapter entry review** is **PASS** ([decision 41](decisions/41-godot-runtime-adapter-entry-review.md), HITL 2026-08-28: C1–C6 approved; numbering **4.3** kept — 4.2 recorded as folded into 4.1; subjects `godot-runtime-launch` ×5 + `godot-runtime-evidence` ×4 at v34; nothing flips `unavailable`). **Stage 4.3 is complete** at `5bedf57` (`decide_godot_launch` + Godot-scoped evidence in `crates/siralos-godot::runtime_adapter`; subjects `godot-runtime-launch` ×5 + `godot-runtime-evidence` ×4 at corpus v34/248 files; audit 243/243 applicable required — the 9 post-freeze scenarios covered by digest-bound expectations at `tests/differential/evidence/post-freeze/expectations.json` per decision 40 C7, provenance disclosed in the audit; zero spawn preserved). External siralos-godot repo stays **FUTURE** (gated on Stage 5 portable locking per decision 34). The **Stage 4.4 Visual Evidence entry review** is **PASS** ([decision 42](decisions/42-stage4-4-visual-evidence-entry-review.md), HITL 2026-08-28: C1–C6 approved; sequence step 4 of decision 08; nothing flips `unavailable`). **Stage 4.4 is complete** at `4a250d8` (`siralos-core::runtime::visual` — bounded digest-bound capture-evidence over the generic decision order, capture primitive absent → typed `UNAVAILABLE`, headless capture a typed pairing refusal; subject `visual-evidence` ×4 at corpus v35/252 files, expectations 13 records, audit 247/247 applicable required; zero spawn preserved). The **Stage 4.5 Controlled Interaction entry review** is **PASS** ([decision 43](decisions/43-stage4-5-controlled-interaction-entry-review.md), HITL 2026-08-28: C1–C6 approved; sequence step 5 of decision 08 — bounded interaction rounds over the generic runtime boundary, subject `run-interaction` ×4 at v36; interactive primitive identity-bound and absent → typed `UNAVAILABLE`; nothing flips `unavailable`); **authorized as next implementation slice**. **Stage 4.5 is complete** at `42ee5ab` (bounded interaction rounds in `siralos-core::runtime::interaction` over the generic decision order; interactive primitive identity-bound and absent → typed `UNAVAILABLE`; one-shot runs a typed pairing refusal; subject `run-interaction` ×4 at corpus v36/256 files; expectations 17 records; audit 251/251 applicable required; zero spawn paths preserved). The **Stage 4.6 QA Workflows entry review** is **PASS** ([decision 44](decisions/44-stage4-6-qa-workflows-entry-review.md), HITL 2026-08-28: C1–C6 approved; sequence step 6 of decision 08 — bounded QA workflow contracts over the generic runtime boundary, subject `qa-workflow` ×4 at v37 with the deliberate candidate-side corpus-cap raise `256 → 384` (pinned v32 oracle untouched); workflow execution primitive identity-bound and absent → typed `UNAVAILABLE`; nothing flips `unavailable`); **authorized as next implementation slice**. **Stage 4.6 is complete** at `a83c2a4` (bounded ordered QA workflow steps in `siralos-core::runtime::qa` over the generic decision order; workflow execution primitive identity-bound and absent → typed `UNAVAILABLE`; zero-step workflows a typed pairing refusal; subject `qa-workflow` ×4 at corpus v37/260 files with the deliberate candidate-side corpus-cap raise `256 → 384` (pinned v32 oracle untouched); expectations 21 records; audit 255/255 applicable required; zero spawn paths preserved). The **Stage 4.7 Profiling entry review** is **PASS** ([decision 45](decisions/45-stage4-7-profiling-entry-review.md), HITL 2026-08-28: C1–C6 approved; the final frozen-sequence arrow, step 7 of decision 08 — bounded run-profiling sessions over the generic runtime boundary, subject `run-profile` ×4 at v38; profiling primitive identity-bound and absent → typed `UNAVAILABLE`; the R8 Godot engine-profiler stays untouched; nothing flips `unavailable`); **authorized as next implementation slice**. **Stage 4.7 is complete** at `b206a4a` (bounded run-profiling sessions in `siralos-core::runtime::profiling` over the generic decision order; profiling primitive identity-bound and absent → typed `UNAVAILABLE`; zero-sample sessions a typed pairing refusal; subject `run-profile` ×4 at corpus v38/264 files; expectations 25 records; audit 259/259 applicable required; zero spawn paths preserved). **The frozen 7-step Stage-4 sequence (decision 08) is fully consumed** — steps 1–7 all implemented, entry-reviewed, and evidence-backed; the next frontier is per the map's _Not yet specified_ section (Stage 5 composition is future-facing under ADR 0036, not committed). The **Stage 4 Verified roll-up** is **PASS** ([decision 46](decisions/46-stage4-verified-roll-up.md), HITL 2026-08-28: C1–C6 approved; consolidate the seven realized slices into one milestone-Verified state — pure closure record, no behavior change). **Stage 4 is Verified** at `9566eee` — fresh full-gate run (differential 259/259 applicable required at corpus v38/264 files, expectations 25 records, pinned v32 oracle untouched), spawn sweeps clean, core neutrality green, decisions 41–46 annotated; **the next frontier is per the map's _Not yet specified_ section** (Stage 5 composition is future-facing under ADR 0036, not committed). The **Stage 5.1 Profiles entry review** is **PASS** ([decision 47](decisions/47-stage5-1-profiles-entry-review.md), HITL 2026-08-28: C1–C6 approved; Stage 5 — Composition graduated into the map with Profiles as the first slice — the composition unit per ADR 0036 §6, a named declarative AI working configuration, narrowing-only; subject `composition-profile` ×4 at v39; nothing flips `unavailable`); **authorized as next implementation slice**. **Stage 5.1 is complete** at `be030e3` (`siralos-core::composition` — the Profile record with bounded name/overlay and `resolve_profile_overlay`, the mechanical narrowing-only property: an overlay entry is legal iff not broader than the Host rule (Deny < Ask < Allow), widening → typed refusal, absent profile → typed default; `siralos-adapters::profile_config` — bounded TOML `[profile]` document parsing; corpus v39/268 files; expectations 29 records; audit 263/263 applicable required; zero spawn paths preserved; no CLI command and no `siralos.lock` in this slice per C1). The **Stage 5.2 Profile Composition entry review** is **OPEN** ([ticket 48](tickets/48-stage5-2-profile-composition-entry-review.md), HITL pending; complete the ADR 0036 §6 resolution chain — effective run configuration over Host authority, subject `composition-effective` ×4 at v40). **Stage 5.2 is complete** at `4c562c8` (`siralos-core::composition` — `DeclaredProfile` + `compose_effective_policy`: the narrowing-only invariant is re-checked at the composition boundary, an overlaid capability gets min-rank(Host, requested), non-overlaid capabilities keep their Host rule, refused/invalid profiles are never applied (truthful diagnostic, C3); `siralos-adapters::profile_config` — `load_workspace_profile` over the shared workspace `siralos.toml` (lstat-verified regular file, byte-bounded, `[profile]`-subtree-only so `[plugins]` coexists); `siralos-cli` — the interactive session composes the workspace profile at startup with a host-side diagnostic when a declared profile is not applied; corpus v40/272 files, expectations 33 records, audit 267/267 applicable required; zero spawn paths preserved; no `siralos.lock` in this slice per C1). The **Stage 5.3 Context Controls entry review** is **OPEN** ([ticket 49](tickets/49-stage5-3-context-controls-entry-review.md), HITL pending; Live/Pinned/Frozen visibility over content identity on the R10b `siralos-core::context` seam, subject `context-controls` ×4 at v41). **Stage 5.3 is complete** at `ce3e7dc` (`siralos-core::context::controls` — `ContextPolicy` (`Live` asserts nothing and binds nothing; `Pinned` binds a content digest and reports staleness truthfully while remaining usable; `Frozen` binds and refuses stale use with a typed `Blocked`) + `evaluate_context_policy` over the artifact-digest primitive + digest-bound `ContextControlEvidence`; the mechanical property: a control can only narrow what Siralos claims about content — never claim a freshness it has not verified; corpus v41/276 files, expectations 37 records, audit 271/271 applicable required; zero spawn/fs paths in the module; no adapters/CLI surface in this slice per C1). **Stage 5.4 is complete** at `0a6d592` (`siralos-core::composition::lock` — `LockPluginIdentity`/`WorkspaceLock` + deterministic idempotent `create_workspace_lock` over the artifact-digest primitive; `siralos-adapters::lockfile` — load/write/verify over the shared atomic-write pattern (lstat-verified regular file + rename, symlink replaced not followed); load re-derives the lock digest so a hand-edited lock is typed invalid; verify compares recomputed digests, never file bytes alone; corpus v42/280 files, expectations 41 records, audit 275/275 applicable required). **Stage 5 is Verified at \c2c30f0\ (10 slices, differential 299/299 at v48/304, 65 expectations, pinned v32 oracle untouched) and **Stage 6 is Verified at \e2c3540\ (4 slices, differential 315/315 at v52/320, 81 expectations) -- externalization precondition (siralos.lock + deny) met. **Stage 7 siralos-godot externalization is **complete — plugin fully self-contained** (60 research + 61 C1-C6 PASS + 62 cutover plan PASS + 63 bootstrap + 64 Option A + 65 adapters moved into plugin)** — ../siralos-godot at 1bf2ca3 (domain + adapters + workspace/config/paths, 234 tests, deps = siralos-core + serde_json + dirs), monorepo at 87bfd35 (3 members, adapters -> core only, external path dep, harness consumes plugin adapters, core 505 + adapters 155 + cli 70, differential 315/315 parity held; push 1bf2ca3 to GitHub when ready). **Next frontier: Real Model/Provider** — 66 research PASS, 67 C1–C6 PASS, 68 credentials & registry PASS → implementation frontier is `ProfileRecord` provider/model fields + `siralos.toml` parsing (authorized per 67 C6).
- Skills every session should consult: **self-loop** (+ `.agents/skills/self-loop/references/verification-protocol.md`), `docs/development/RUST_STYLE.md`, `docs/development/R7_BEHAVIOR_EXTRACTION.md` §14, `ARCHITECTURE.md` § Rust candidate, `AGENTS.md` workspace/security rules. **Standing rule: self-loop on every prompt** — every future prompt (human or follow-up) must invoke the `self-loop` skill and its `references/verification-protocol.md` ledger (criterion → evidence → pass/fail/unknown → challenge → repair), even when Wayfinder says one ticket per session. Skill load is per-prompt; loop budget is one coherent pass + up to two repairs unless the prompt explicitly changes budget.
- Standing preferences: Wayfinder **Plan, don't do** — tickets resolve decisions, not deliverables, unless Notes explicitly carries execution into the map. One ticket per session (research may parallel). Reference closed tickets by **name**, never bare id. Lean discipline: profiles/context/skills/plugins are declarative composition — do not pull multi-agent machinery, general Hooks, TaskGraph, workflow engines, marketplaces, or automatic acquisition forward (ADR 0036).
- Tracker: **local-markdown** fallback (no issue-tracker binding configured — run `/setup-matt-pocock-skills` if a hosted tracker is desired). The map lives at `docs/wayfinder/siralos-roadmap.md`; tickets live under `docs/wayfinder/tickets/`. This is the canonical artifact (label `wayfinder:map` in hosted trackers).
- Corpus-mechanics reminder (recurring repair at v40 and v41): every corpus-version bump must move the strict-loader count assert in `crates/siralos-cli/src/harness.rs` together with the manifest version — the assert only runs under `--all-features` (`npm run check`'s test step), not plain `cargo test --workspace`, so run `cargo test -p siralos-cli --lib --all-features` before every slice commit.

## Decisions so far

<!-- one line per closed ticket: gist + link; map never restates the ticket body -->

- [R7.5 Review Rubric — When Is the /context + /tools Candidate PASS?](decisions/01-r7-5-review-rubric.md) — 5-section PASS rubric frozen (byte-equal strings, 16 CLI tests + 11 differential scenarios, sanitizer advisory, flag scope, executable checklist); `cargo fmt/clippy/test` + `check:architecture/rust` observed PASS in DSH partial sandbox.
- [R7 Verified Promotion — What Closes R7 Active?](decisions/02-r7-verified-promotion.md) — DoD frozen for R7A+7.1-7.5 (corpus v12/104→v13/120→v15/133, gates fmt/clippy/test/check:rust/arch/differential 128/128), 7-surface atomic promotion table, R8 needs entry-review (not immediate), lean Stage 4 guardrail, executable promotion checklist.
- [Godot Boundaries Research — What siralos-core Isolation Must Protect for R8/R9](decisions/03-godot-boundaries.md) — fact sheet with 4 enforcement tables (domain neutrality via FORBIDDEN_CORE_SYMBOL_PATTERN, R4 hardening, adapter-vs-core ownership, ADR 0036 lean quotes) at file:line; `cargo fmt` + `check:rust/arch` observed PASS.
- [R8 vs R9 Cut — Which Godot Parity Ships in Which Slice?](decisions/04-r8-r9-cut.md) — frozen two-row table: 6 R8 surfaces (discovery, recovery, knowledge, check-only, bounded LSP, read-only scenes) + 3 R9 surfaces (prepared mutation, review/impact, unified workflow), each with fail-closed posture + Not-in-R8/R9 list (no placeholder domains/marketplaces/auto-install); `fmt`/`check:rust/arch` PASS, no code written.
- [R10 Scope — H1/H2/ICM + H3 Runtime-Readiness Parity Slice Shape](decisions/05-r10-scope.md) — one Verified milestone with 3 ordered entry-reviewed sub-slices (R10a H1+H2, R10b ICM, R10c H3) justified by `H1→H2→ICM→H3` hard dependencies; 14 proposed differential subjects with core seams (`siralos-core::identity/determinism/context/runtime`); R11 boundary explicit (effect-boundary/run-directory/security/recovery/cross-platform stay R11); `fmt`/`check:rust/arch` PASS.
- [R11 Gate — Full Differential, Effect-Boundary, Security, Recovery, Cross-Platform Closure Criteria](decisions/06-r11-gate.md) — entry on R7+R8/R9+R10a-c Verified + harness schema 3; 4 pillars (effect-boundary typed `unavailable`, security truthful `test:sandbox`, typed recovery 7 dims, Tier-1 cross-platform digest-bound audit) + RUST_STYLE measurement discipline + PASS corpus/7-surface artefacts; `fmt`/`check:rust/arch` PASS.
- [R12 Disposition — Retirement vs Retention Evidence Template](decisions/07-r12-disposition.md) — 8-row shared evidence table + 5-field retention block vs retirement audit-retention split, 8-surface atomic disposition table (7 docs + stage4-entry-gate), 8 guardrails (R11 owner, predecessors Verified, audit-bound, Stage 4 four-thing gate, no early removal, typed retention trigger, retained audits, grilling HITL); `fmt`/`check:rust/arch` PASS.
- [Stage 4 Entry — Generic Controlled Runtime vs Godot Adapter Layering](decisions/08-stage4-entry-sequence.md) — 7-step Stage-4 sequence frozen generic-first (RUST_MIGRATION 719-732), generic Host vs Godot Plugin table, 4-arrow entry gate (R1-R11 + Stage 1-3 audit + R12 + stage4-entry-gate.md 17 criteria), two lean sentences (Host not a plugin + composition model), Stages 4-6 confirmed not guaranteed; `fmt`/`check:rust/arch` PASS.
- [R8 Entry Review — Freeze R8 Contract, Subjects, Measurement](decisions/10-r8-entry-review.md) — **PASS — R8 contract frozen; authorized as next implementation slice** (6 surfaces, 13-row co-located table, 5 differential subjects, measurement per `RUST_STYLE.md:568-589`); `fmt`/`check:rust/arch` PASS.
- [R8 Verified Promotion — What Closes R8 Active?](decisions/11-r8-verified-promotion.md) — **R8 Verified at worktree `c075b3c`** (all gates observed PASS incl. differential 150/150 at corpus v16/155 files; fail-closed grep sweep zero spawn paths; atomic 9-surface advancement); R9 requires its own entry review.
- [R9 Entry Review — Freeze the Godot Stage-3 Contract](decisions/12-r9-entry-review.md) — **PASS — R9 contract frozen; authorized as next implementation slice** (3 surfaces: review/impact intelligence, prepare-only mutation contracts, deterministic `/develop` core; differential subjects `godot-review-context`/`godot-mutation-prepare`/`godot-develop-plan`; apply/checkpoints stay typed `unavailable`).
- [R9 Verified Promotion — What Closes R9 Active?](decisions/13-r9-verified-promotion.md) — **R9 Verified at worktree `1623e80`** (all gates observed PASS incl. differential 162/162 at corpus v17/167 files; fail-closed sweep zero spawn paths; adapter ownership row materialized; atomic surface advancement); R10 requires its own entry review.
- [R10 Entry Review — Freeze the Runtime-Readiness Milestone Contract](decisions/14-r10-entry-review.md) — **PASS — R10 milestone contract frozen; R10a authorized next** (13 subjects graduated to frozen names across three ordered sub-slices `H1→H2→ICM→H3`; corpus mechanics v18/v19/v20 pinned to slice reconciliation commits).
- [R10b Entry Review — Freeze the ICM Contract](decisions/15-r10b-entry-review.md) — **PASS — R10b contract frozen (two wired subjects: `icm.phase-contract`, `icm.dependency-manifests`; projection/briefing explicit scope boundaries)**; narrowing-only authority invariant + `siralos:PhaseContract:v1\0` digest binding frozen before code.
- [R10c Entry Review — Freeze the H3 Runtime-Readiness Contract](decisions/16-r10c-entry-review.md) — **PASS — R10c contract frozen; authorized as next implementation slice** (four subjects: identity, budgets, lifecycle, doctor; harness-owned fault injection under controlled clock; real spawn/sandbox stay R11).
- [R10 Verified Promotion — What Closes R10 Active?](decisions/17-r10-verified-promotion.md) — **R10 Verified at executable worktree `a456afb`** (one atomic promotion over three landed sub-slices at corpus v20/210 files; full gate observed PASS incl. differential 205/205; seven-surface advancement; R11 entry state inherited with 4 platform skips / 0 informational deviations; authorizes nothing downstream).
- [R11 Entry Review — Freeze the Full-Parity Closure Contract](decisions/18-r11-entry-review.md) — **PASS — R11 contract frozen; authorized as next implementation slice** (two new subjects: `workspace-apply` refusal-before-binding + protected paths, `recovery-taxonomy` over the seven typed failure dimensions; sandbox live conformance truthful-skip; Tier-1 Linux/macOS audit artefacts promotion-blocking; no fail-closed flip).
- [R11.3 Tier-1 Repair Register](tickets/19-r11-tier1-repair-register.md) — **CLOSED** — six cross-platform findings registered, five repaired in code without weakening fail-closed posture, one (#5 `SSH_AUTH_SOCK`) resolved by HITL option (a); all confirmed by the green three-platform dispatch at `eea0029` with artefacts retained in EVIDENCE.md.
- [R11 Verified Promotion — What Closes R11 Active?](decisions/20-r11-verified-promotion.md) — **R11 Verified at worktree `eea0029`** (three-platform digest-bound audits 217/217 applicable required parity at corpus v23/222 files; truthful loud sandbox skips; atomic surface advancement; R12 disposition requires its own HITL grilling).
- [Remaining-Surface Ports Entry Review — Freeze the Continuation Contract (R13)](decisions/21-r13-remaining-surface-parity-entry-review.md) — **PASS — R13 contract frozen; R13.1 authorized** (five ordered slices covering the unported TypeScript inventory, nine frozen subjects, corpus bumps v24+ at reconciliation commits, decision-07 predecessor list amended to R1–R11 + R13, R13.5 full CLI parity); landings tracked in the [R13 Execution Register](tickets/22-r13-execution-register.md), which gates the deferred [R12 Disposition Execution](tickets/20-r12-disposition.md).
- [R13.1 Entry Review — Host Introspection & Authority Scenarios](decisions/22-r13-1-entry-review.md) — **PASS — scenario contract frozen; implementation authorized** (~20 fixtures at corpus v24: `security-permissions` ×10 incl. one-time approval binding + behavioral-config classification, `command-catalog` ×5, `capability-doctor` ×5 over harness-injected fakes; no unavailable effect operational).
- [R13.2 Entry Review — Workspace Guidance Scenarios](decisions/23-r13-2-entry-review.md) — **PASS — scenario contract frozen; implementation authorized** (14 cases at corpus v25: `instructions-resolution` ×7 over resolver precedence/scope/conflicts/normalization/identity/rendering, `knowledge-revisions` ×7 over proposal/no-churn evolution/policy-shape rejection/secrets/provenance gating/scoring+trace/pin-retire; injected clock + ports only; seeding deferred to R13.5).
- [R13.3 Entry Review — External Knowledge Boundaries Scenarios](decisions/24-r13-3-entry-review.md) — **PASS — scenario contract frozen; implementation authorized** (20 cases at corpus v26: `reference-identity` ×10 over strict declaration parsing/origin normalization/pins/pre-resolver mutable-ref refusal/containment/duplicate audit/outcome matrix/fail-closed refresh/binding FIFO/materializer posture, `research-policy` ×10 over gate-first denial/validation bounds/source matching/task binding/stale discard/timeout-cancelled/normalization disclosure/provenance fallback/evidence ring/view rendering; access port + tools deferred to R13.5; no network; injected clock).
- [R13.4 Entry Review — Planning & Briefing Scenarios](decisions/26-r13-4-entry-review.md) — **PASS — scenario contract frozen; implementation authorized** (20 cases at corpus v27: `planning-runtime` ×10 over model identity/immutability, strict validation, policy depth, flow phases, set-lifecycle sequencing/replacement rules, contract-advance staleness, approval binding, revision cap, detach, invalidate-reason trail, `executor-brief` ×10 over execution-contract identity, manifest acceptance ids, evidence-only acceptance, brief determinism, working set, scope classification, documentation selection, new-file discipline, bounded rendering, context-pack refs; synthetic fixtures only; briefing-service + real manifests deferred to R13.5).

- [R13.5 Entry Review - CLI Product Composition Contract](decisions/27-r13-5-entry-review.md) - **PASS - contract frozen as four ordered sub-slices (R13.5a-d); R13.5a authorized** (scripted-stdin sessions; deferred seams inside existing subjects; all four real manifests as fixture constants).
- [R13.5a Entry Review — Slash-Dispatch Core Fixtures](decisions/28-r13-5a-entry-review.md) — **PASS — R13.5a scenario set frozen; implementation authorized** (~6 case groups at corpus v28: input-parsing, session-lifecycle, help-and-commands, status-view, unknown-command, prompt-turn; oracle probe `cli-session-oracle.mjs` landed at `afb5f19` over the real composition with scripted queue/fake IO/sanitizer).
- [R13.5b Entry Review — Briefing-Service and Real Manifests](decisions/29-r13-5b-entry-review.md) — **PASS — R13.5b scenario set frozen; implementation authorized** (~8 groups at corpus v29: briefing-service memoization, S3M8×15, S3M9×13, S3M10×13, S3M11×18, milestone selection, dynamic-context invalidation, fingerprint stability).
- [R13.5c Entry Review — Deferred Seams (Knowledge Seeding + Reference/Research Access)](decisions/30-r13-5c-entry-review.md) — **PASS — R13.5c scenario set frozen; implementation authorized** (~10 groups at corpus v30: knowledge seeding ×3, reference access ×4, research access ×4 over existing subjects).
- [R13.5d Entry Review — Full CLI-Session Closure](decisions/31-r13-5d-entry-review.md) — **PASS — R13.5d scenario set frozen; implementation authorized** (~7 groups at corpus v31: godot ×9, gdscript ×7, develop ×5, system ×8, queue ownership, sanitizer boundary, ordering determinism).
- [R13 Verified Promotion — What Closes R13 Active?](decisions/32-r13-verified-promotion.md) — **R13 Verified at worktree 72e20be** (five slices, corpus v31, 236 files, 231/231 local audit, 4 skips, 0 informational; seven-surface atomic advancement; R12 disposition next).
- [R12 Disposition Execution — Retirement Verdict](decisions/33-r12-disposition-execution.md) — **R12 Verified (retired)** at 4bef901 (audit v31 e24f4bb, 231/231, 236 files; TypeScript archived, Rust sole source of truth; stage4-entry-gate 17/17 PASS).
- [Stage 4.1 Generic Runtime + Godot Plugin Extraction Contract](decisions/34-stage4-1-generic-runtime-and-godot-plugin-extraction.md) — **PASS — Stage 4.1 authorized next; Godot extraction frozen as in-repo crate crates/siralos-godot (6+3 surfaces) with empty-state Add Plugin UI (manifest-bound, siralos.toml, typed fail-closed; external siralos-godot repo FUTURE).
- [Stage 4.1 Entry Review — Freeze the Generic Controlled Runtime Contract](decisions/35-stage4-1-entry-review.md) — **PASS — Stage 4.1 contract frozen; authorized as next implementation slice** (runtime-execution ×6, runtime-evidence ×4 at v32, process.execute only, unavailable when primitive absent).
- [Stage 4.1 Verified Promotion — Generic Controlled Runtime](decisions/36-stage4-1-verified-promotion.md) — **Stage 4.1 Verified at 9383ab8** (execution ×6 dispositions + evidence bounded 1 MiB, process.execute → UNAVAILABLE fail-closed, 0 spawn, check:differential 231/231 retained; authorizes crates/siralos-godot extraction next).
- [Godot Crate Extraction Entry Review — Freeze the Source Move](decisions/37-godot-crate-extraction-entry-review.md) — **PASS — Godot crate extraction frozen; authorized as next implementation slice** (23 siralos-core/src/godot/** + 10 siralos-adapters/src/godot/** files into crates/siralos-godot; no new scenario, check:rust exemption _removed_).
- [Add Plugin UI Entry Review — Freeze the Empty-State Domains Slice](decisions/38-add-plugin-ui-entry-review.md) — **PASS — Add Plugin UI slice frozen; authorized as next implementation arrow** (HITL 2026-08-27: `domain-manifest.toml` at folder root, workspace-root `siralos.toml` `[plugins.<id>]`, View + Add only, optional component verified at add). **Implemented at `7fe9b66`** (`/domains` empty-state + `/domains-add` picker with `DomainHost::install` boundary, workspace `siralos.toml` record, catalog 49 entries both sides; differential 231/231 v31 retained; repair `0e78b41`).
- [Enable/Activate Host-Gated UI Entry Review](decisions/39-enable-activate-entry-review.md) - **PASS - Enable/Activate slice frozen; authorized as next implementation arrow** (HITL 2026-08-27: `/domains-enable` + `/domains-activate` separate commands, empty vs declared authority, memory-only until Activate, typed stay-installed failures). **Implemented at `978ac07`** (Host-gated in-memory hosts map, typed stay-installed failures). **Stage 4.1 v32 reconciliation landed** (`runtime-execution` + `runtime-evidence` subjects at `CORPUS_VERSION` 32, audit 234/234 applicable required).
- [TypeScript Archive Removal — Oracle Freeze and Complete Tree Removal](decisions/40-typescript-archive-removal.md) — **PASS — Freeze contract C1–C8 approved** (HITL 2026-08-28: Q1 Port to Rust, Q2 pinned+opt-in, Q3 retain minimal, Q4 keep schemas+fixtures; corpus v32 frozen at `5da5cde`/`c8b70a95`; implementation slice authorized next, no deletion in this decision). **Implemented at `e6c49f1`** (corpus v33 `01ba53a…`, pinned differential with opt-in freeze-SHA replay; review repairs `72c6dff`, `ddbe148`).
- [Godot Runtime Adapter Entry Review — First Specialization of the Generic Runtime Boundary](decisions/41-godot-runtime-adapter-entry-review.md) — **PASS — Stage 4.3 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; numbering 4.3 kept with 4.2 folded into 4.1; subjects `godot-runtime-launch` ×5 + `godot-runtime-evidence` ×4 at v34; nothing flips `unavailable`, zero spawn preserved). **Implemented at `5bedf57`** (corpus v34/248 files, audit 243/243 applicable required incl. 9 expectation-covered post-freeze scenarios; post-freeze expectations mechanism per decision 40 C7 with additive audit provenance).
- [Stage 4.4 Visual Evidence Entry Review — Visual-Run Evidence Lifecycle over the Generic Runtime Boundary](decisions/42-stage4-4-visual-evidence-entry-review.md) — **PASS — Stage 4.4 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; sequence step 4; subject `visual-evidence` ×4 at v35; capture primitive identity-bound and absent → typed `UNAVAILABLE`; no display probing). **Implemented at `4a250d8`** (corpus v35/252 files, expectations 13 records, audit 247/247 applicable required; zero spawn preserved).
- [Stage 4.5 Controlled Interaction Entry Review — Bounded Interaction Rounds over the Generic Runtime Boundary](decisions/43-stage4-5-controlled-interaction-entry-review.md) — **PASS — Stage 4.5 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; sequence step 5; run-interaction-rounds framing, approval-protocol alternative rejected as already landed; subject `run-interaction` ×4 at v36; interactive primitive identity-bound and absent → typed `UNAVAILABLE`; nothing flips `unavailable`). **Implemented at `42ee5ab`** (corpus v36/256 files, expectations 17 records, audit 251/251 applicable required; zero spawn preserved).
- [Stage 4.6 QA Workflows Entry Review — Bounded QA Workflow Contracts over the Generic Runtime Boundary](decisions/44-stage4-6-qa-workflows-entry-review.md) — **PASS — Stage 4.6 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; sequence step 6; bounded-workflow-steps framing, task-kernel-acceptance alternative rejected as already landed; subject `qa-workflow` ×4 at v37 with the corpus-cap raise `256 → 384`; workflow execution primitive identity-bound and absent → typed `UNAVAILABLE`; nothing flips `unavailable`). **Implemented at `a83c2a4`** (corpus v37/260 files, expectations 21 records, audit 255/255 applicable required; zero spawn preserved).
- [Stage 4.7 Profiling Entry Review — Bounded Run-Profiling Sessions over the Generic Runtime Boundary](decisions/45-stage4-7-profiling-entry-review.md) — **PASS — Stage 4.7 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; the final frozen-sequence arrow, step 7; run-profiling-sessions framing, Godot-profiler alternative rejected as an already-landed R8 domain surface; subject `run-profile` ×4 at v38; profiling primitive identity-bound and absent → typed `UNAVAILABLE`; nothing flips `unavailable`). **Implemented at `b206a4a`** (corpus v38/264 files, expectations 25 records, audit 259/259 applicable required; zero spawn preserved; completes the frozen 7-step sequence).
- [Stage 4 Verified Roll-Up — One Milestone-Verified State for the Seven Realized Slices](decisions/46-stage4-verified-roll-up.md) — **PASS — Stage 4 Verified roll-up contract frozen; evidence gathering authorized** (HITL 2026-08-28: C1–C6 approved; seven realized steps as drafted; six verification criteria with fresh gate runs; pure closure record, no behavior change). **Stage 4 is Verified at `9566eee`** (fresh full-gate run: differential 259/259 applicable required at v38/264, expectations 25 records, pinned v32 oracle untouched; spawn sweeps clean; core neutrality green; decisions 41–46 annotated).
- [Stage 5.1 Profiles Entry Review — Named Declarative Working Configurations over a Narrowing-Only Boundary](decisions/47-stage5-1-profiles-entry-review.md) — **PASS — Stage 5.1 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; model + overlay, no CLI/lockfile; subject `composition-profile` ×4 at v39; narrowing-only overlays, zero-config valid, pure declarative data). **Implemented at `be030e3`** (corpus v39/268 files, expectations 29 records, audit 263/263 applicable required; zero spawn preserved; the first Stage 5 slice).
- [Stage 5.2 Profile Composition Entry Review — Effective Run Configuration over Host Authority](decisions/48-stage5-2-profile-composition-entry-review.md) — **PASS — Stage 5.2 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; compose seam + workspace loader + CLI consumption, subject `composition-effective` ×4 at v40, bad profiles not applied with truthful diagnostics). **Implemented at `4c562c8`** (corpus v40/272 files, expectations 33 records, audit 267/267 applicable required; the authority invariant is re-checked at the composition boundary; zero spawn preserved).
- [Stage 5.3 Context Controls Entry Review — Live/Pinned/Frozen Visibility over Content Identity](decisions/49-stage5-3-context-controls-entry-review.md) — **PASS — Stage 5.3 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; core-only controls module, subject `context-controls` ×4 at v41, absent control = Live, Pinned-stale usable but labelled, Frozen-stale blocked). **Implemented at `ce3e7dc`** (corpus v41/276 files, expectations 37 records, audit 271/271 applicable required; zero spawn/fs paths; consumption surfaces wait for their own slices).
- [Stage 5.4 siralos.lock Resolution Entry Review — Machine-Generated Portable Identities](decisions/50-stage5-4-lock-resolution-entry-review.md) — **PASS — Stage 5.4 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; completes the ADR 0036 §8-9 pair — deterministic, idempotent `siralos.lock` over the established atomic-write pattern, subject `composition-lock` ×4 at v42). **Implemented at `0a6d592`** (corpus v42/280 files, expectations 41 records, audit 275/275 applicable required; load re-derives the lock digest, verify compares recomputed digests never file bytes). **Stage 5.5 is complete** at `5e1b3e0` (`siralos-core::composition` — `ProfileRecord.plugins` + `select_profile_plugins` with the mechanical intersection property (activated = enabled ∩ selected; the intersection can only shrink, so a profile can never enable, install, or activate anything) + digest-bound `PluginSelectionEvidence`; ids outside the Host-enabled set are diagnosed and stay inactive; `siralos-adapters::profile_config` — bounded unique `plugins` parsing; corpus v43/284 files, expectations 45 records, audit 279/279 applicable required; session application waits for its own slice).
- [Stage 5.5 Plugin Selection by Profiles Entry Review — Narrowing-Only Domain Activation](decisions/51-stage5-5-plugin-selection-entry-review.md) — **PASS — Stage 5.5 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; the Plugins clause of ADR 0036 §6 — the profile filters Host-enabled plugins and can never enable, install, or activate anything; subject `composition-plugin-selection` ×4 at v43). **Implemented at `5e1b3e0`** (corpus v43/284 files, expectations 45 records, audit 279/279 applicable required; the intersection property unit-proven).
- [Stage 5.6 Skills Entry Review — Declarative Digest-Bound Model Guidance](decisions/52-stage5-6-skills-entry-review.md) — **PASS — Stage 5.6 contract frozen; authorized as next implementation slice** (HITL 2026-08-28: C1–C6 approved; the ADR 0036 §22 Skill — reusable declarative guidance with **no authority** ("Skill != Capability"); subject `composition-skills` ×4 at v44). **Implemented at `fcf61c5`** (corpus v44/288 files, expectations 49 records, audit 283/283 applicable required; order-independence unit- and corpus-proven).
- [Stage 5.7 Session Plugin Activation Gate Entry Review — Consumption of the Frozen Filter](decisions/53-stage5-7-plugin-activation-gate-entry-review.md) — **PASS — Stage 5.7 contract frozen; authorized as next implementation slice** (HITL 2026-08-29: C1–C6 approved; Host authority first, the profile filter refuses before side effects, gate transparent without an applied profile; subject `composition-plugin-activation` ×4 at v45). **Implemented at `926ac71`** (corpus v45/292 files, expectations 53 records, audit 287/287 applicable required; authority-precedence unit- and corpus-proven).
- [Stage 5.8 Session Context Controls Entry Review — Consumption of the Frozen Visibility Seam](decisions/54-stage5-8-session-context-controls-entry-review.md) — **PASS — Stage 5.8 contract frozen; authorized as next implementation slice** (HITL 2026-08-29: C1–C6 approved; the applied profile's context control gates the session's `/context` claims — Pinned-stale labelled, Frozen-stale refuses; additive `[profile.context]` key; subject `composition-context-control` ×4 at v46). **Implemented at `6dc830e`** (corpus v46/296 files, expectations 57 records, audit 291/291 applicable required; narrowing-only transparency unit- and corpus-proven).
- [Stage 5.9 Session Lock Verification Entry Review — Consumption of the Frozen Lock Seam](decisions/55-stage5-9-session-lock-verification-entry-review.md) — **PASS — Stage 5.9 contract frozen; authorized as next implementation slice** (HITL 2026-08-29: C1–C6 approved; the on-disk lock is verified at session startup against the recomputed current lock — missing/current transparent, stale/invalid truthful diagnostics; the lock never gates authority; subject `composition-lock-verify` ×4 at v47). **Implemented at `6e38804`** (corpus v47/300 files, expectations 61 records, audit 295/295 applicable required; never-gates property unit- and session-proven).
- [Stage 5.10 Session Skill Consumption Entry Review — Consumption of the Frozen Skill Seam](decisions/56-stage5-10-session-skill-consumption-entry-review.md) — **PASS — Stage 5.10 contract frozen; authorized as next implementation slice** (HITL 2026-08-29: C1–C6 approved; the frozen 5.6 skill seam is consumed by the applied profile via an additive `[profile.skills]` key — bound guidance surfaces in the system-instructions segment, guidance-only, never authority; subject `composition-skill-consumption` ×4 at v48). **Implemented at `579f1e9`** (corpus v48/304 files, expectations 65 records, audit 299/299 applicable required; the guidance-only property unit- and session-proven; absent selection/catalog stays byte-transparent, R7.5 preserved).
- [Stage 5 Verified Roll-Up — One Milestone-Verified State for the Ten Realized Slices](decisions/57-stage5-verified-roll-up.md) — **PASS — Stage 5 roll-up contract frozen; evidence gathering authorized** (HITL 2026-08-29: C1–C6 approved; ten realized slices as drafted; six criteria with fresh gate runs; single marker + atomic docs; pure closure record — no behavior change). **Stage 5 is Verified at `c2c30f0`** (fresh full-gate run: differential 299/299 applicable required at v48/304, expectations 65 records, pinned v32 oracle untouched; spawn sweep clean; core neutrality green; decisions 47–57 annotated). The Stage 5.6 Skills entry review is **PASS** per decision 52 (HITL 2026-08-28: C1–C6 approved; declarative digest-bound model guidance with no authority per ADR 0036 §22, subject `composition-skills` ×4 at v44). **Stage 5.6 is complete** at `fcf61c5` (`siralos-core::skills` — `SkillDefinition`/`SkillCatalog`/`resolve_profile_skills` with digest-bound evidence whose payload binds `authority = none`; opt-in intersection selection, unknown names diagnosed never bound, order-independent resolution; `siralos-adapters::skills_loader` — bounded read-only catalog over `.siralos/skills/*.md`; corpus v44/288 files, expectations 49 records, audit 283/283 applicable required; zero spawn paths; no registry, no profile schema change). The Stage 5.7 Session Plugin Activation Gate entry review is **PASS** per decision 53 (HITL 2026-08-29: C1–C6 approved; the frozen 5.5 filter gates `/domains-activate`, completing decision 51's deferred consumption; subject `composition-plugin-activation` ×4 at v45). **Stage 5.7 is complete** at `926ac71` (`siralos-core::composition` — `decide_plugin_activation` with Host-authority precedence (a profile can never enable) and digest-bound `PluginActivationEvidence`; `siralos-cli` — the session holds the applied profile's selection and `/domains-activate` refuses filtered ids before any install/enable/activate side effect; corpus v45/292 files, expectations 53 records, audit 287/287 applicable required; zero spawn paths, gate transparent without an applied profile). The Stage 5.8 Session Context Controls entry review is **PASS** per [decision 54](decisions/54-stage5-8-session-context-controls-entry-review.md) (HITL 2026-08-29: C1–C6 approved; the frozen 5.3 visibility seam is consumed by the applied profile and gates the session's `/context` claims; subject `composition-context-control` ×4 at v46). **Stage 5.8 is complete** at `6dc830e` (`siralos-core::composition` — `decide_context_control` over the frozen 5.3 evaluation with absent-control transparency and digest-bound `ContextControlDecisionEvidence`; `siralos-adapters::profile_config` — the additive `[profile.context]` key through the 5.3 constructor, malformed controls leave the profile unapplied; `siralos-cli` — the session holds the applied control and `/context` labels Pinned-stale claims, refuses Frozen-stale ones, and stays byte-transparent otherwise; corpus v46/296 files, expectations 57 records, audit 291/291 applicable required; zero spawn/fs-write paths). The Stage 5.9 Session Lock Verification entry review is **PASS** per [decision 55](decisions/55-stage5-9-session-lock-verification-entry-review.md) (HITL 2026-08-29: C1–C6 approved; the on-disk lock is verified at session startup against the recomputed current lock; the lock never gates authority; subject `composition-lock-verify` ×4 at v47; authorized as next implementation slice). **Stage 5.9 is complete** at `6e38804` (`siralos-core::composition` — `decide_lock_verification` over the `StoredLockDigest` states with digest-bound `LockVerificationEvidence`; `siralos-cli` — `verify_session_lock` recomputes the current lock from the applied profile's effective-policy identity and the installed plugin records and reports drift or untrusted content as a host-side startup diagnostic; corpus v47/300 files, expectations 61 records, audit 295/295 applicable required; zero spawn/fs-write paths; the lock never gates authority — unit- and session-proven). The Stage 5.10 Session Skill Consumption entry review is **PASS** per [decision 56](decisions/56-stage5-10-session-skill-consumption-entry-review.md) (HITL 2026-08-29: C1–C6 approved; the frozen 5.6 skill seam is consumed by the applied profile via an additive `[profile.skills]` key; bound guidance surfaces in the system-instructions segment, guidance-only, never authority; subject `composition-skill-consumption` ×4 at v48; authorized as next implementation slice).
- [Stage 6 Evolution & Stabilization Entry Review — Freeze the Bounded Evaluation Workflow Contract](decisions/58-stage6-evolution-entry-review.md) — **PASS — Stage 6 contract frozen; authorized as next frontier** (HITL 2026-08-30: C1–C6 approved; bounded `/evolve` with 6.1–6.4 slices (6.1 Evaluation Corpus be030e3-style, 6.2 Workflow Core, 6.3 Proposals, 6.4 Packaging), escalation Profile→Context→Skill→Plugin→Host with deletion preference, evidence over pure seams per decision 40 C7, corpus cap 384, zero spawn).
- [Stage 6 Verified Roll-Up - One Milestone-Verified State for the Four Realized Slices](decisions/59-stage6-verified-roll-up.md) - **PASS - Stage 6 roll-up contract frozen; evidence gathering authorized** (HITL 2026-08-30: C1-C6 approved; four slices as drafted; six criteria with fresh gate runs; pure closure record - no behavior change). **Stage 6 is Verified at `e2c3540`** (fresh full-gate run: differential 315/315 applicable required at v52/320, expectations 81 records, pinned v32 oracle untouched; spawn sweep clean; core neutrality green; decisions 58-59 annotated).
- [siralos-godot Externalization Research — Inventory, Shape, and Harness Implications](decisions/60-siralos-godot-externalization-research.md) — fact sheet with 5 tables (41-file crate inventory `crates/siralos-godot/Cargo.toml:1-14` + `src/lib.rs:1-13`, boundary via `check-rust-architecture.mjs:29-67`, 10 godot differential subjects 315/315 at v52/320, distribution via `siralos.toml`/`siralos.lock`, gate retention); research PASS, unblocks entry review.
- [siralos-godot Externalization Entry Review — Freeze the Standalone Repo Contract](decisions/61-siralos-godot-externalization-entry-review.md) — **PASS — C1–C6 frozen (scope verbatim, repo `github.com/CrimsX/siralos-godot` public, pin `git rev + Cargo.lock + deny`, evidence additive expectations per decision 40 C7, shim, ordering 60→61→bootstrap→pin); HITL 2026-08-31 Q1 Approve verbatim / Q2 Approve public / Q3 Approve git rev / Q4 Approve expectations / Q5 Approve ordering — authorizes repo-bootstrap + verbatim move next.
- [siralos-godot Distribution & Migration Plan — Pin, Shim, and Cutover](decisions/62-siralos-godot-distribution-and-migration-plan.md) — cutover plan frozen (pin `git rev + Cargo.lock + deny` per §1, shim A→B→C per §2, bootstrap checklist per §3, 7-surface doc sweep per §4, rollback per §5) — **PASS; planning 60→61→62 complete.**
- [siralos-godot Repo Bootstrap + Verbatim Move](tickets/63-siralos-godot-repo-bootstrap-and-verbatim-move.md) — **Verified at `d098926150cb73d29269584c32d107ee45f3cc09`** (external `../siralos-godot` 41 files verbatim, `rust-toolchain.toml`/`rustfmt.toml`, `cargo test --workspace --all-features` **77 passed** via `path = "../siralos/crates/siralos-core"` local dev) — slice 3 complete.
- [siralos-godot Monorepo Pin + Shim Removal + Doc Sweep](decisions/64-siralos-godot-monorepo-pin-and-shim-removal.md) — **Option A (shim retained)** — HITL 2026-08-31: external `../siralos-godot` at `d098926` 77 tests is **ready to move** to `https://github.com/CrimsX/siralos-godot`, monorepo retains `crates/siralos-godot` shim (members 4, `EXPECTED_CRATES` 4) to avoid circular `Siralos → siralos-godot → Siralos` (`path = "../siralos/..."` vs `git rev`); no `git rev` pin in this slice, 7-surface doc sweep deferred. **Stage 7 planning + bootstrap is Verified — ready to move, manual `git push` when owner deems ready.**
- [siralos-godot Standalone — Move Host Adapters into Plugin](decisions/65-siralos-godot-standalone-adapters-move.md) — **plugin fully self-contained at external `1bf2ca3`** (41 domain files + `src/adapters/godot` host adapters + `workspace`/`config.rs`/`paths.rs` helpers moved in with a case-sensitive ordered import rewrite after the first case-insensitive attempt corrupted `KnowledgeSupportState`; deps = siralos-core + serde_json + dirs only, no wasmtime; **234 tests**, clippy `-D warnings` clean); monorepo `87bfd35` deletes `adapters/src/godot`, adapters → core only, harness consumes `siralos_godot::adapters::godot::*` via external path dep. Verified: workspace all-targets clean, core 505 + adapters 155 + cli 70, **differential 315/315 parity held**, arch + doc links + strict-loader PASS. **Stage 7 complete — no open frontier.** The plugin repo has been pushed to GitHub and is managed independently in its own repository.
- [Real Model/Provider Research — Inventory and Secure Credential Requirements](decisions/66-real-model-provider-research.md) — fact sheet with 5 tables (deterministic-fake at `crates/siralos-cli/src/configuration.rs:18`, `ProfileRecord:60` no provider/model, `siralos.toml`/`config.json`/`siralos.lock`, determinism/replay, ADR 0036 §10 env: pattern); research PASS, unblocks entry review.
- [Real Model/Provider Entry Review — Freeze the Provider Contract](decisions/67-real-model-provider-entry-review.md) — **PASS — C1–C6 frozen (provider/model in ProfileRecord, env: only, bounded Host-observed, ToolRegistry gate, 4-step ordering); HITL 2026-08-31 Q1 Add to ProfileRecord / Q2 Env only / Q3 Bounded Host-observed / Q4 Host gate / Q5 Approve ordering — authorizes ProfileRecord fields + siralos.toml parsing next.**
- [Real Provider Credentials and Registry — Env Resolution and Host-Mediated HTTP Adapters](decisions/68-real-provider-credentials-and-registry.md) — credential `env:` only (never in portable config), registry `provider` → `ModelProvider` with typed `UnknownProvider`, HTTP adapter with `determinism` clock + replay, secret hygiene 3-surface audit — **PASS; ProfileRecord fields + siralos.toml parsing is now fully planned and authorized per decision 67 C6; HTTP registry remains frozen but not authorized until that slice is Verified.** **Real Provider is now fully planned — implementation frontier is ProfileRecord provider/model fields + siralos.toml parsing (authorized per 67 C6).**
- [Real Provider Implementation Progress — Record of Landed Slices and Remaining Frontier](decisions/69-real-provider-implementation-progress.md) — **ProfileRecord fields + `siralos.toml` parsing, env-only `HostCredential`, registry + all-purpose `GenericProvider`, bounded HTTP adapters, hermetic `provider-generic` subject at `v53` (316/316 applicable required, pinned v32 oracle untouched) are landed and full-gate green; remaining: determinism/replay recording slice (68 §3), secret-hygiene audit (68 §4), Verified roll-up.**
- [Real Provider Replay Recording and Secret-Hygiene Audit — Determinism Ports for Provider Responses](decisions/70-real-provider-replay-recording.md) — **ProviderResponseIdentity + ReplayRecorder determinism ports landed; openai/anthropic/generic record every observed response with typed Recorded/Unavailable availability (default zero behavior change); 68 §4 sweep recorded — no secret in portable surfaces; full gate exit 0 (316/316, pinned oracle untouched). Remaining: Verified roll-up only.**
- [Real Provider Verified Roll-Up — Closure Record for the Real Model/Provider Range](decisions/71-real-provider-verified-roll-up.md) — **fresh full gate exit 0 (316/316 applicable required at v53/321, 82 expectation records, pinned v32 oracle untouched), spawn sweep clean, secret-hygiene audit recorded, decisions 66–71 annotated, status flips in README/ROADMAP/PROJECT_CONTEXT/AGENTS — Real Model/Provider is Verified.**
- [Mechanical Secret-Hygiene Gate — Enforcing the 68 §4 Sweep](decisions/72-mechanical-secret-hygiene-gate.md) — **the decision 68 §4 sweep is now a standing gate (check:secrets, chained into npm run check): zero tolerance on portable surfaces, credential-shape patterns repo-wide, documented fake fixtures allowed; never echoes matched secret text.**
- [Recorded-Response Replay Provider — Serving Determinism-Port Recordings](decisions/73-recorded-response-replay-provider.md) — **the "for replay" half of 68 §3: RetainingReplayRecorder retains sanitized bodies in memory only; RecordedReplayProvider serves recordings as event streams (insertion order, typed exhausted failure, cancellation-first); shared body-to-events parser extracted verbatim from the generic adapter; corpus untouched. Follow-through: session replay-run composition.**
- [Replay Composition Seam — From Retaining Recorder to Replay Provider](decisions/74-replay-composition-seam.md) — **one pure composition function over the existing seams (recorder snapshot -> RecordedReplayProvider, copy semantics); round-trip test pins record-then-replay with typed exhausted failure; no session/CLI wiring or corpus change.**
- [Provider-Replay Differential Subject — Pinning the Replay Contract at Corpus v54](decisions/75-provider-replay-differential-subject.md) — **hermetic subject pinning the full replay contract (ordered playback, typed exhausted failure, copy semantics) at v54/322 files; audit 317/317 applicable required, expectations 83 records via canonicalRecordDocument, pinned v32 oracle untouched.**
- [Real Provider Verified Roll-Up II — the Guardrail Gate and the Replay Arc](decisions/76-real-provider-followthrough-verified-rollup.md) — **pure closure record over 72–75: the 68 §4 sweep as the standing check:secrets gate; recording → playback → composition → v54 corpus pinning; audit 317/317, 83 expectation records, pinned v32 oracle untouched; fresh full gate exit 0 at 04e89f8. Real Provider range (66–75) fully Verified; session replay-run composition remains unticketed.**
- [Session Replay-Run Composition — In-Process Record-Then-Replay](decisions/77-session-replay-run-composition.md) — **SessionReplayComposer binds the frozen seams (record phase via the retaining recorder, replay via the 74 seam, digest-bound evidence); nothing persisted (Option A, HITL 2026-08-31); pinned by the session-replay subject at v55/323 files, audit 318/318, expectations 84 records; cross-session replay explicitly deferred.**
- [Cross-Session Replay — the Bounded Recordings Store](decisions/78-cross-session-replay-entry-review.md) — **Amends decision 73's never-persisted invariant with the mechanical hygiene contract (sanitization-before-persist via the hand-rolled credential-shape scanner, 64-recording/2 MiB bounds with oldest-first eviction, digest-verified bounded load over the lockfile atomic-write pattern, untrusted-input posture, never auto-repaired); the store (B1), session composition over the additive [profile] record-replay/replay keys with fail-closed no-live-fallback semantics (B2), and the replay-store subject at v56/324 files, audit 319/319, expectations 85 records (B3); Option B, HITL 2026-08-31.**
- [Context Management Foundations — the Context Graph (Entry Review + Slice 1)](decisions/79-context-management-foundations.md) — **Adopts the verified context-management thesis (maximize useful context within capacity; memory management over compaction) with a frozen interpretation table (synchronous deterministic scheduler, deterministic L2 representations, no persisted store in v1, lexical/structural routing only, conversation excluded, deterministic budget, benchmark decision gate); slice 1 lands ContextNode/ContextEdge/ContextGraph with validation, canonical digests, and targeted staleness over the existing seams; pinned by the context-graph subject at v57/325 files, audit 320/320, expectations 86 records; HITL 2026-08-31.**
- [Context Representations — L0-L4 Layers (Slice 2)](decisions/80-context-representations.md) — **Layered representations over the Context Graph, additive and keyed by node id (v57 pin untouched): L0 identity, L1 summary (provenance-bound when model-derived), L2 structured HOST-ONLY with a typed refusal for model-derived (decision 79 clause b mechanically enforced), L3 detailed, L4 source reference; pinned by the context-representation subject at v58/326 files, audit 321/321, expectations 87 records; HITL 2026-08-31.**
- [The Deterministic Tiered Context Scheduler (Slice 3)](decisions/81-context-scheduler.md) — **HOT/WARM/COLD working set under synchronous deterministic ticks (decision 79 clause a): integer scoring (relevance\*4 + recency + pin), thresholds 280/120, Stale demotes one tier toward Cold regardless of pin (content-staleness is a fact, not a preference), the 4096-token budget demotes lowest-scored Hot non-pinned first with node-id tie-break, Archive is explicit-only and demotion never deletes; pinned by the context-scheduler subject at v59/327 files, audit 322/322, expectations 88 records; HITL 2026-08-31.**
- [The Context Demand-Paging Tools (Slice 4)](decisions/82-context-tools.md) — **Three read-only tools over immutable snapshots of the graph, representation store, and scheduler state: context.inspect (metadata + available levels + stale flag), context.search (deterministic lexical, cap 16), context.expand (exact-level resolution with typed not-resident honesty); the tools never mutate scheduler state — the model cannot promote, pin, or archive; live session registration deferred to the benchmark slice; pinned by the context-tool subject at v60/328 files, audit 323/323, expectations 89 records; HITL 2026-08-31.**
- [The Context Benchmark Decision Gate (Slice 5)](decisions/83-context-benchmark-gate.md) — **Frozen clause (g): the deterministic gate over the real tool path — key-blind paged strategy, integer-only metrics, a broad-query scenario where paging cannot win, and a pre-committed mechanical GO rule (recall parity AND aggregate token reduction > 50%). Measured: recall parity held 14/14 but token reduction was 47.2% (1230 -> 650) — NO-GO; the audit and hygiene-metrics seam is not justified by the measured evidence; corpus v61/329 files, audit 324/324, expectations 90 records; HITL 2026-08-31.**
- [Context Management Foundations — Program Closure](decisions/84-context-foundations-closure.md) — **Pure closure record (no behavior change): five slices landed and pinned at v61, the gate measured NO-GO, the metrics seam is not built, and the deferred live-session registration is closed as precondition-gated future work (graph-reconstruction consumer, node-taxonomy entry review, profile-gated opt-in); HITL 2026-08-31.**
- [Paged Strategy V2 — Progressive Disclosure Re-Benchmark](decisions/85-paged-strategy-v2.md) — **The same pre-committed gate re-run with a smarter deterministic strategy (lexical rerank, digest dedup, progressive disclosure; fixtures, 50% bar, and recall rule unchanged): measured GO — 58.5% token reduction (1230 -> 510) at recall parity 14/14, superseding the decision 83 NO-GO and un-gating the audit and hygiene-metrics seam as a candidate next slice; savings are dedup-dominated on these fixtures (rerank neutral on single-token queries); corpus v62/329 files, audit 324/324, expectations 90 records; HITL 2026-08-31.**
- [The V3 Heterogeneous Re-Gate](decisions/86-v3-heterogeneous-regate.md) — **The v2 GO was challenged as fixture-driven and the challenge was accepted: fixtures rebuilt realistically (distinct domain excerpts, 2-token queries, distractors, one intentional duplicate) and the gate hardened (savings decomposition, dedup-share guard, 9-cell estimator sweep) BEFORE the run. Measured NO-GO — paged 3012 vs baseline 2286 (−31.7%) with recall 14/14: the decision 85 GO does not survive honest fixtures and the audit/hygiene-metrics seam returns to gated status; the run also exposed that the Identity-dump baseline is content-free and the structured-first expansion priority over-pays, which the next methodology correction must address; corpus v63/329 files, audit 324/324, expectations 90 records; HITL 2026-08-31.**
- [The Corrected-Baseline Re-Gate](decisions/87-corrected-baseline-regate.md) — **The challenger of decisions 85/86 signed off the methodology before the run: DeepAll (deepest-available dump, no dedup, zero overhead) as the sole gated baseline, SummariesAll and retired Identity as informational, strategy unchanged — one variable at a time. Measured GO: paged V2 3012 vs DeepAll 7609 (60.4% reduction, recall 14/14, dedup share 0%, 9/9 sensitivity cells pass; depth premium vs SummariesAll +138.6% as expected). The claim is deliberately weak-but-honest: selective retrieval dominates maximal dumping at recall parity — a GO does not endorse the structured-first expansion policy, which moves to decision 88 with a fresh pre-commit; corpus v64/329 files, audit 324/324, expectations 90 records; HITL 2026-08-31.**
- [The Escalation-Policy Test](decisions/88-escalation-policy-test.md) — **Answer-aware depth-aware recall scoring (the depth-blind metric cannot measure escalation), key-blind policy, top-1 gate with an informational top-k curve, externally signed off before the run. Measured Retained: top-1 escalation loses depth-aware recall 13/14 vs V2's 14/14 at lower cost (1844 vs 3012 tokens) — the ProgressiveV2 expansion policy stands and the tradeoff is recorded as the measured cost of depth; the one loss is a structured key; runtime semantics unchanged pending the scheduler-semantics slice; corpus v65/329 files, audit 324/324, expectations 90 records; HITL 2026-08-31.**
- [The Scheduler-Semantics Re-Pin](decisions/89-scheduler-semantics-repin.md) — **The four amendments (host-owned demand edge, digest-counted budget, pinned-HOT quota with stale-never-promote, tick coalescing) re-pin the decision 81 rules with the frozen clauses intact and the tools still read-only; the context-benchmark verdicts stay byte-identical; corpus v66, audit all-applicable-required, expectations updated in place; live registration remains precondition-gated; HITL 2026-08-31.**
- [The Consumer Slice](decisions/90-consumer-slice.md) — **HOT assembles L1 summaries by default (deeper levels reachable only through the read-only expand tool and the demand edge — the decision 88 curve finding rules out rank-only auto-escalation), hot nodes carry a bounded 1-hop neighbor map (ids+digests only, counted in the budget), and the end-to-end demand composition (tool round -> events -> tick) is proven as pure composition with live activation still precondition-gated; benchmark verdicts byte-identical; corpus v67/335 files, audit all-applicable-required, expectations updated in place; HITL 2026-08-31.**
- [The Metrics Counters](decisions/91-metrics-counters.md) — **Pure in-memory observability un-gated by the corrected-baseline GO: saturating u64 counters (ticks, coalesced no-ops, events applied/dropped, demand updates, promotions, per-kind demotions, assembled summary + stub tokens) and a capped 64-record tick ring, derived deterministically from tick inputs/outputs; the tick pipeline stays pure, nothing persists, benchmark verdicts byte-identical; corpus v68/337 files, audit all-applicable-required, expectations updated in place; live activation still precondition-gated; HITL 2026-08-31.**
- [The Search-Scoring Re-Rank](decisions/92-search-scoring-rerank.md) — **The deterministic re-rank (stopword-filtered terms; whole-word +10 summary / +4 id, substring +3/+1; tier +8 HOT/+4 WARM; kind +3 Knowledge/+1 Source; score desc, node_id asc) amends the pinned decision 82 ordering; the benchmark was intentionally re-measured under the UNCHANGED decision 87/88 rules and the re-measured verdicts are recorded; corpus v69/339 files, audit all-applicable-required, expectations updated in place; HITL 2026-08-31.**
- [The Score-Ordered Retention](decisions/93-score-ordered-retention.md) — **The externally-discussed resolution of the decision 92 re-gate: search score feeds only the assembly demotion order (rank-for-retention, not rank-for-depth — score-decides-depth re-measures the decision 88 rejection and is refused); the benchmark flow is frozen, carries no GO claim, and the record stays byte-identical to v69; adoption gated on the pre-committed byte-identity and determinism branches; corpus v70/340 files, audit all-applicable-required, expectations updated in place; HITL 2026-08-31.**
- [The Sensitivity-Sweep Rule](decisions/94-sensitivity-sweep-rule.md) — **Externally signed after the decision 92 re-gate: NO REVISION — the 9-cell estimator sweep stays gated exactly as pre-committed, the primary-cell-only rule is rejected, and no estimator-range change is admissible except on independent real-tokenizer evidence (e1), never on verdict grounds; e1 is admitted as informational evidence prerequisite for any future range argument; decisions 86-93 stand untouched; HITL 2026-08-31.**
- [The Context System Activation Entry Review](decisions/95-context-activation-entry-review.md) — **HITL 2026-08-31: C1–C6 approved as drafted after the externally-discussed plan — B1 bounded read-only scan, B2 taxonomy review (own HITL), B3 default-off [profile.context] session wiring, B4 the decision 91 ring as live audit; the adversarial invariants (spam-pinning resistance, churn boundedness, neighbor-stub honesty under fanout) gate every slice as pass/fail fixtures; clause (d)/(e) exceptions, persistence, and default-on stay out of scope; B1 authorized as the first slice.**
- [Activation B1: The Bounded Workspace Scan](decisions/96-b1-workspace-scan.md) — **First activation slice (decision 95 arc): bounded deterministic read-only scan over the established workspace primitives — pinned bounds (256 nodes, 65536 bytes/file), lexicographic order, protected (.siralos/**, AGENTS.md, .git/**) and oversized paths skipped and counted, node cap with deterministic truncation, digests over the existing identity primitive, typed unavailability, no persistence and no spawn; provisional graph binding until the B2 taxonomy review; staleness deferred to session state; corpus v71/344 files, audit all-applicable-required, expectations updated in place; HITL 2026-08-31.**
- [Activation B2: The Node Taxonomy](decisions/97-b2-node-taxonomy.md) — **HITL-approved classification: Source (default), Decision (docs/adr/**, docs/wayfinder/decisions/**), Knowledge reserved with no producer; classification at graph-binding time only with the B1 scan byte-unchanged; the decision 92 kind-weight table untouched (Decision +0) so no benchmark re-measurement occurs; L0-only binding with L1 summary generation proposed for B3; no model-visible surface; corpus v72/345 files, audit all-applicable-required, expectations updated in place; HITL 2026-08-31.**
- [Activation B3a: Deterministic Summary Generation](decisions/98-b3a-summary-generation.md) — **Scanned nodes gain deterministic L1 summaries via the R5 advisory summary formatter — pure functions of file content under existing bounds, composed with the B1 scan and B2 classification into a host-side WorkspaceContext over the decision 80 store seam (L0+L1 per node, nothing above L1); dedup-honest (identical content -> identical summaries), persistence-free, no session wiring and no model-visible surface — B3b consumes it behind the default-off profile key; corpus v73/347 files, audit all-applicable-required, expectations updated in place; HITL 2026-08-31.**
- [Activation B3b: Default-Off Session Wiring](decisions/99-b3b-session-wiring.md) — **The context subsystem is session-wired behind the additive [profile.context_system] opt-in (distinct from the decision 54 controls key): ON builds the WorkspaceContext, registers the three read-only context tools over the derived snapshot, and drives the demand loop after each tool round (observations -> events/scores -> tick -> working set, coalescing live); OFF is byte-transparent; build failure is a diagnostic with the subsystem off (fail-closed, never partial); the model can never inject events or scores; nothing persists and nothing renders — B4 owns the audit surface; corpus v74/351 files, audit all-applicable-required, expectations updated in place; HITL 2026-08-31.**
- [Activation B4: The Live Audit Surface](decisions/100-b4-audit-surface.md) — **The decision 91 ring and counters are the live audit trail for opted-in sessions: the /context render gains a bounded trailing audit segment (saturating counters in pinned order, the last 8 tick records oldest-first) only when [profile.context_system] is enabled and the subsystem built; OFF is byte-transparent; the segment flows through the terminal sanitizer; no new commands, no persistence; corpus v75/353 files, audit all-applicable-required, expectations updated in place; HITL 2026-08-31.**
- [The Context System Activation Roll-Up](decisions/101-context-activation-rollup.md) — **Verified complete (HITL 2026-08-31): B1-B4 as authorized by the decision 95 entry review — scan, taxonomy, summaries, default-off session wiring, live audit — with the adversarial invariants held, the OFF path byte-transparent at every layer, the benchmark record byte-identical from v69 through v75, and no persistence, spawn, or model-injectable demand; the demand loop is live behind [profile.context_system] with the bounded /context audit segment; deferred items recorded (task-utility measurement, e1 calibration, clause (d)/(e) exceptions, neighbor content prefetch); the arc is closed.**
- [The Provider-Interface Takeaways](decisions/102-provider-interface-takeaways.md) — **Measurement-only slice from the Meta Model API docs review, externally discussed: the cache-stability rule pre-committed (the stable prefix stays byte-stable, volatile-last by construction; never reorder or freeze assembly for cache hits; cache benefit measured opportunistically, never a gate), provider usage capture into the recording identity under the decision 78 hygiene, and the e1 estimator-calibration comparison implemented as informational tooling per the decision 94 rule; deferred tool loading, reasoning-effort, and structured output recorded as future additive tickets; workflows, compaction, prompt_cache_key adoption, and gate changes on verdict grounds refused; HITL 2026-08-31.**
- [The Siralos TUI Entry Review](decisions/103-siralos-tui-entry-review.md) — **HITL 2026-08-31: C1–C6 approved with ratatui + crossterm pinned in the CLI crate — a TUI frontend over the existing session seam composing the sanitizer/input-queue/command-catalog triad unchanged, pure state-to-frame rendering headless-tested via TestBackend; slices T1 shell, T2 approvals modals, T3 the decision 100 context pane, T4 render-model differential pinning; non-TTY falls back to stdio; multi-agent/workflow machinery stays out of scope; T1 authorized.**
- [TUI T1: The Shell](decisions/104-tui-t1-shell.md) — **ratatui + crossterm pinned in the CLI crate; the pure deterministic render model (TuiState + draw) headlessly tested over TestBackend; the session seam composed with the sanitizer boundary intact and no threads; --tui opt-in with non-TTY stdio fallback; blocking-round freeze documented; stdio path byte-unchanged; HITL 2026-08-31.**
- [TUI Default Entry](decisions/105-tui-default-entry.md) — **The user's amendment: the TUI is the default frontend when stdout is a TTY; --stdio forces stdio; plain non-TTY uses stdio silently; the one-commit --tui flag removed; stdio byte-unchanged for scripts and CI; HITL 2026-08-31.**
- [TUI T2: The Approval Surface](decisions/106-tui-t2-approvals.md) — **Approval prompts render as TUI modals over the unchanged host-gated evaluation — y/n/Esc route through the same decision read the stdio path uses, no keys pass while a modal is pending, and the T1 dispatch-duplication consolidation starts where the modal work touches it (ledger in the record); HITL 2026-08-31.**
- [TUI T3: The Context Pane](decisions/107-tui-t3-context-pane.md) — **The decision 100 audit (counters + last 8 tick records) and tool activity render as a live 40-column right-hand pane for opted-in sessions — single-sourced from the ContextMetrics the /context segment uses, byte-transparent when the subsystem is off, sanitizer-disciplined, deterministic over TestBackend; the consolidation ledger continues; HITL 2026-08-31.**
- [TUI T4: Render-Model Pinning](decisions/108-tui-t4-render-pinning.md) — **The render model is pinned into the differential corpus (four TestBackend frame snapshots — shell, scroll, approval modal, context pane — as candidate-authored expectations; corpus v76/357) and the T2/T3 dispatch debt is settled: the slash-command dispatch and key-edit handling are single shared definitions both loops call, with any per-frontend session-composition residual recorded with its why; the stdio path stays byte-unchanged; ticket 103 complete pending its roll-up; HITL 2026-08-31.**
- [The Siralos TUI Roll-Up](decisions/109-siralos-tui-rollup.md) — **Verified complete (HITL 2026-08-31): T1-T4 as authorized by the decision 103 entry review with the TUI-default amendment — the shell, approval modals over unchanged host gates, the live context pane, and the corpus-pinned render model; the consolidation debt settled to the permanent terminal-I/O residual (parse_slash_command, compose_session, handle_key, the approval evaluation shared; unsafe_code = forbid bars unifying the I/O handles); the TUI is the default frontend on TTYs with --stdio as the escape hatch and silent non-TTY stdio; ticket 103 closed; known limitation recorded (blocking provider rounds freeze the redraw).**
- [The TUI Usability Pass Entry Review](decisions/110-tui-usability-entry-review.md) — **HITL 2026-08-31: U1–U8 approved from the user's real-usage report — two defects root-caused (the keystroke redraw starvation: the continue after non-submit keys skipped the loop-bottom draw so characters rendered on the 100ms poll timeout; the command-vocabulary silence: unknown /-commands fell through to the prompt path with no listing), plus the palette, timestamps, status provider/model, /provider + /model, and the /evolve discovery command closing the Stage 6 wiring gap; modes not applicable (none exist); runtime switching and evolution execution out of scope.**
- [The TUI Usability Pass](decisions/111-tui-usability-pass.md) — **Implemented: drain-then-draw-once (keystrokes immediate), the / command palette over the single SlashCommand vocabulary, unknown-command honesty, UTC-stamped messages (std-only civil-date math, no new dependency), the status provider/model line, /provider + /model display commands, and /evolve surfacing the Stage 6 evolution features; corpus v77/357 re-pinned with fixed fixture timestamps; stdio unchanged except the additive shared commands; HITL 2026-08-31.**
- [The Audit Remediation Pass Entry Review](decisions/112-audit-remediation-entry-review.md) — **HITL 2026-08-31: the mechanical batch from the external-model project audit authorized (the context.inspect nested-path defect and the double-scroll defect as priority); six HITL questions presented OPEN (approval wiring, snapshot staleness, unknown-command canonical behavior, the fourth atomic writer, provider-identity-in-lock, stderr scoping) and explicitly out of scope.**
- [The Audit Remediation Pass](decisions/113-audit-remediation-pass.md) — **Implemented: inspect accepts nested-path node ids (the HIGH defect — real workspace paths were all rejected and their demand events never fired, hidden by slash-free test fixtures; tripwire tests added to all three tools), single +-10 scroll, one shared catalog, sanitized echo and status, compose before the alternate screen, the harness gate mirroring the live seam, and the docs staleness sweep; corpus v77/357 unchanged (no records changed; count 357); HITL questions await direction; HITL 2026-08-31.**
- [The Audit HITL Rulings](decisions/114-audit-hitl-rulings.md) — **All six open questions ruled by the user (HITL 2026-08-31): approvals stay dormant as future-seam, the tool-snapshot staleness accepted and documented, unknown-command honesty now canonical in BOTH frontends through one shared definition (the stdio prompt fallthrough for unknown /-commands is gone — a compatibility change the user accepted), the fourth atomic writer documented alongside the approved three, the session lock documented as authority-only, and the sanitizer boundary documented as model/tool-output sinks with stderr as host-side diagnostics; ticket 105 closed.**
- [The Neighbor-Expansion Experiment](decisions/115-neighbor-expansion-experiment.md) — **The externally-endorsed paraphrase-gap measurement, pre-committed: 1-hop neighbors of matched nodes as decayed-score inspect candidates (never deeper, never escalated, capped at 8) in the ProgressiveV2 flow under the unchanged gate rules. Measured NULL: the fixture corpus contains no hit→key graph edges, so the mechanism admitted zero candidates and the clause-(d) boundary remains unmeasured — not failed, unpowered; the gate re-measured NO-GO byte-identical to the decision 92 record; the honest next step is a pre-committed fixture amendment planting one hit→key edge; corpus v78/357 re-pinned; HITL 2026-08-31.**
- [The Paraphrase-Gap Fixture Amendment](decisions/116-paraphrase-fixture-amendment.md) — **The pre-committed mechanism test completing the decision 115 experiment: one genuine referrer node (a real 2-token match) planted with an edge to the paraphrase key — the decision 115 experiment was unpowered because the scenario had zero hits and no edges; the gate scenarios byte-unchanged (the aggregate re-measured byte-identical); the reachability outcome reached at 65 candidate tokens (+1 candidate inspect); corpus v79/357 re-pinned; HITL 2026-08-31.**
- [The TUI Polish Pass Entry Review](decisions/117-tui-polish-entry-review.md) — **HITL 2026-08-31: the user reported input latency persisting (root-caused: the drain's inner 15ms poll gated every redraw) and requested a nicer TUI referencing the MiMo-Code aesthetic; adopted: the zero-timeout drain fix, rounded borders, role colors, a session header, palette styling, and a context-usage readout; skipped per ADR 0036: agents/modes/memory/workflows/voice.**
- [The TUI Polish Pass](decisions/118-tui-polish-pass.md) — **Implemented: the zero-timeout drain (keystrokes draw immediately — no drain-wait gate), the header bar with provider/model, role-colored transcript with dim stamps, rounded borders on the pane/palette/modal, a styled command palette, and the ctx assembled/4096 status readout; the render frames re-pinned at corpus v78/357; the stdio frontend byte-unchanged; HITL 2026-08-31.**
- [The TUI UX Pass 2 Entry Review](decisions/119-tui-ux2-entry-review.md) — **HITL 2026-08-31: the second live-usage round — header dedup, the ASCII banner and greeting, the palette filter verified against typing, local-timezone stamps via the time crate (a recorded CLI-only dependency), the working indicator styled within the synchronous architecture (a live spinner recorded as an open architecture question — it needs a UI thread), and the /provider picker scoped read-only with the add-flow split to ticket 108 pending the config-write authority decision.**
- [The TUI UX Pass 2](decisions/120-tui-ux-pass-2.md) — **Implemented: the header deduplicated (provider/model only when configured), the ASCII banner and greeting at session start, the palette filter verified, local-timezone stamps (YYYY-MM-DD HH:MM:SS +offset via the time crate, UTC fallback), the styled working marker, and the read-only /provider picker echoing the selection; the add-flow and the live spinner await their decisions; corpus v81/357 re-pinned; HITL 2026-08-31.**
- [The TUI Interaction Pass](decisions/124-tui-interaction-pass.md)
- [The Provider Add-Form UX Fixes](decisions/126-add-form-ux-fixes.md) — **Three add-form UX defects from live usage fixed: URLs accepted (all printable chars in the Char handler), a visible cursor in each field, and Up/Down field navigation with per-field validation; HITL 2026-08-31.** — **The third live-usage round: the palette retention wiring fixed (the root cause was handle_key never calling update_palette — the unit test tested the helper directly), arrow-key palette navigation with Enter/Tab selection, full command display, command history via Up/Down, the /models fetch from the provider (a new bounded network surface with the freeze documented), and the right-edge layout gap fixed; corpus v82/359; HITL 2026-08-31.**
- [The Provider Add-Form Clarity Fixes](decisions/128-add-form-clarity-fixes.md) — **The add-form is now self-explanatory: descriptive field labels with examples, human-readable error messages (no cryptic regex), and per-field descriptions; the validation rules are unchanged; HITL 2026-08-31.**
- [The Add-Form URL-First Reorder](decisions/130-url-first-reorder.md) — **The form asks for the endpoint URL first (matching the user's mental model: URL + key), and the provider name auto-derives from the URL host (api.example-vendor.com -> example-vendor) with an editable prefill; both URL-first and name-only flows produce valid configurations; HITL 2026-08-31.**
- [The Six-Field Provider Form](decisions/132-six-field-provider-form.md) — **The user's six-field structure — url, api key (the env-var NAME; the env-only policy holds), display name, api protocol, model, model display name — with no examples in the labels, the model fetch wired into the form (a picker on success, an honest fallback on failure), and two additive profile keys (protocol, model display name) stored and displayed with absent-transparency; the protocol's request-shaping use is future work; HITL 2026-08-31.**
- [The Form Order and Fetch-Timeout Fixes](decisions/134-order-timeout-fixes.md) — **display name moved to the first field (empty-advance with url-derived prefill), the model fetch fails fast at 5s instead of freezing the form for 60s on an unreachable endpoint, and the api key error detects a pasted key and teaches the env-var-name pattern; HITL 2026-08-31.**
- [The Optional API Key](decisions/136-optional-api-key.md) — **the api key field is optional: empty = no credential for public endpoints (the profile schema already treated credential as absent-able), the written config omits the credential key, and the model fetch proceeds without authentication; a provided credential remains the env-var NAME; the new test also exposed and fixed a latent fresh-workspace write panic; HITL 2026-08-31.**
- [The Verbatim Credential and the Protocol Picker](decisions/138-marker-protocol-picker.md) — **the api key field stores what the user types verbatim (env: prefix for the env-var form, otherwise the literal key — the form is an interface, not a validator; the config is gitignored; display surfaces redact key: values), the model picker gained a sliding viewport so the selection is always visible, provider errors render honestly and bounded (URL + status + a truncated body), and the api protocol is a selectable picker over openai-completions, openai-responses, and anthropic-messages with the legacy aliases accepted in the parse; the request-shaping use remains future work; HITL 2026-08-31.**
- [The Provider Config Flow & Input Fixes](decisions/122-provider-config-flow.md) — **The config-write authority granted: /provider gains an add-flow (provider, model, credential env-var NAME — never values, endpoint) writing the [profile] section into siralos.toml atomically with format-preserving merge and re-parse validation (the fifth documented atomic writer; live re-composition deferred — a restart applies it); the palette retains while typing and Tab completes the prefix; a blank line separates the banner from the greeting; the UI-thread architecture explained and recorded OPEN; ticket 108 closed; HITL 2026-08-31.**
- [The Provider Model-Id Charset and Bound Entry Review (retroactive)](decisions/139-model-id-charset-entry-review.md) — **RETROACTIVE record: the model-id charset/bound change was implemented, verified, and committed (11d5eb5) before this review was written at the owner's explicit request — no prior ticket, entry review, or approval existed and none is invented or backdated; the normal authorize-then-implement order is inverted and named as such; retroactive PASS over the committed diff (single is_model_id_char predicate, 128 -> 256 bound, honest rejections, round-trip coverage); 2026-09-10.**
- [The Provider Model-Id Charset and Bound](decisions/140-model-id-charset.md) — **the [profile] model-id rule now accepts provider-issued ids (ASCII alphanumeric plus . _ - / : @ via the single siralos_core::composition::is_model_id_char used by the core validator, the write boundary, and the TUI form; MAX_PROFILE_MODEL_BYTES 128 -> 256 matching the bound the add-form already stated; every rejection states its rule); example/model-a and example/model-b:free save and apply; full gate cited green (614 core / 306 adapters / 25 conformance / 213 cli, differential 352/352); ticket 117; retroactive record of commit 11d5eb5; 2026-09-10.**
- [The Credential Bound Split by Form Entry Review (retroactive)](decisions/141-credential-bound-entry-review.md) — **RETROACTIVE record: the by-form credential bound was implemented, verified, and committed (68f834f) before this review was written — no prior ticket, entry review, or approval existed and none is invented or backdated; the provenance is an owner bug report (a 73-char real OpenRouter key refused by the 70-byte env-sized bound), not a design approval; the normal authorize-then-implement order is inverted and named as such; retroactive PASS over the committed diff (env:/legacy keep the 70-byte whole-value bound, key: gets 4096 after the prefix, every message names its bound, load path applies what save writes); 2026-09-10.**
- [The Credential Bound Split by Form](decisions/142-credential-bound-by-form.md) — **the [profile] credential validator now branches on form before measuring (env: and bare legacy keep MAX_PROFILE_CREDENTIAL_BYTES = 70 whole-value; key: values measured after the prefix against MAX_PROFILE_CREDENTIAL_KEY_BYTES = 4096; every rejection states its bound); a 73-char real key (77 bytes with prefix) saves and applies via ProfileRecord::validate; the new test failed pre-fix with Err("The credential exceeds the 70-byte bound."); full gate cited green (614 core / 306 adapters / 25 conformance / 214 cli, differential 352/352); ticket 118; retroactive record of commit 68f834f; the decision 68 70-byte documentation is immutable — supersession noted in this record only; 2026-09-10.**
- [Secret Hygiene Skips Git-Ignored Paths Entry Review (retroactive)](decisions/143-secret-hygiene-entry-review.md) — **RETROACTIVE record: the git-ignored skip was implemented, verified, and committed (c384764, plus bbee359) before this review was written — no prior ticket or entry review existed; the approval that exists is an in-chat design approval from the human owner on 2026-09-10, nothing more claimed, nothing backdated; the normal authorize-then-implement order is inverted and named as such; retroactive PASS over the committed diff (one batched git check-ignore -z --stdin resolving the ignored subset with git owning ignore matching, publishable files keep full coverage, fail-closed to scan-everything without git, bbee359 repairs the 139/140 formatting); orchestrator-verified (owner siralos.toml passes, non-ignored probe still fails named, format:check + lint pass); 2026-09-10.**
- [Secret Hygiene Skips Git-Ignored Paths](decisions/144-secret-hygiene-gitignored-paths.md) — **check:secrets no longer fails on files git itself will not publish (collectIgnoredPaths() via one batched git check-ignore -z --stdin; tracked and untracked-but-NOT-ignored files still scanned; patterns, wording, and output format unchanged; fail closed on git unlaunchable or non-0/1 exit); this closes the 139/140 open item (the gitignored workspace siralos.toml storing a literal key); ticket 119; retroactive record of commits c384764 + bbee359; 2026-09-10.**
- [Remove the Configured Provider Entry Review (retroactive)](decisions/145-remove-provider-entry-review.md) — **RETROACTIVE record: provider removal was implemented, verified, and committed (8855be8) before this review was written — no prior ticket or entry review existed; the approval that exists is an in-chat design approval from the human owner on 2026-09-10, nothing more claimed, nothing backdated; the normal authorize-then-implement order is inverted and named as such; retroactive PASS over the committed diff (remove_profile_config() on the atomic-writer pattern, truthful no-op when absent, one implementation behind y/N confirmation in both frontends, dormant Approved./Denied. path preserved); full gate cited green (614/306/25/227, 13 new, differential 352/352); 2026-09-10.**
- [Remove the Configured Provider](decisions/146-remove-configured-provider.md) — **a provider can now be deleted (remove_profile_config() beside write_profile_config: byte-preserving read, only the [profile] table removed, write refused unless every other top-level item reserializes identically, temp re-parsed as Absent before rename, symlinks/non-regular refused, temp deleted on failure; missing file or absent [profile] is a truthful no-op; "- Remove provider" picker row only when a provider exists plus /provider remove in the shared catalog through one implementation, both y/N-confirmed; stdio wires the input-queue approval helper for this command only, dormant path unchanged); ticket 120; retroactive record of commit 8855be8; 2026-09-10.**
- [Scope the Modal Borrow So Provider Removal Does Not Panic Entry Review (retroactive)](decisions/147-provider-remove-modal-borrow-entry-review.md) — **RETROACTIVE record: the modal-borrow fix was implemented, verified, and committed (8306de8) before this review was written — no prior ticket, entry review, or approval existed and none is invented or backdated; the provenance is an owner bug report in-session about the just-shipped removal feature ("unable to remove providers, crashes app"); the normal authorize-then-implement order is inverted and named as such; retroactive PASS over the committed diff (the modal-branch decision taken inside a block ending the scrutinee RefMut before the body re-borrows, extracted as handle_pending_approval_key so the loop path is unit-tested, dormant Approved./Denied. path preserved, class sweep over all 55 borrow sites clean); full gate cited green (614/306/25/230, 3 new, differential 352/352); 2026-09-10.**
- [Scope the Modal Borrow So Provider Removal Does Not Panic](decisions/148-provider-remove-modal-borrow.md) — **answering the "- Remove provider" confirmation (y, n, or Esc) no longer panics with "RefCell already mutably borrowed" (edition-2024 if-let scrutinee lifetime, probed via standalone rustc: compile-fail under 2015/2021, panic under 2024); the broken shape predates the feature (dormant per decision 114) and the 8855be8 deletion feature exposed it without introducing it; ticket 121; retroactive record of commit 8306de8; 2026-09-10.**
- [The Profile Endpoint as Base URL and the Protocol Chat Path Entry Review (retroactive)](decisions/149-profile-endpoint-protocol-entry-review.md) — **RETROACTIVE record: the base-vs-verbatim endpoint split was implemented, verified, and committed (1ba70f3 + 853684e) before this review was written — no prior ticket, entry review, or approval existed; the provenance is an owner bug report (the endpoint 404 reported live, orchestrator-confirmed: bare base 404, base + /chat/completions 200); the normal authorize-then-implement order is inverted and named as such; retroactive PASS over the committed diff (pure chat_url/models_url helpers, verbatim full-path passthrough, protocol carried by GenericProvider, C7 expectation updated with the frozen v32 oracle untouched); 2026-09-11.**
- [The Profile Endpoint as Base URL and the Protocol Chat Path](decisions/150-profile-endpoint-protocol.md) — **the [profile] endpoint is now a base URL with the protocol selecting the chat POST path (openai-completions -> /chat/completions, openai-responses -> /responses, anthropic-messages -> /messages); ticket 122; retroactive record of commits 1ba70f3 + 853684e; full gate cited green (differential 352/352); 2026-09-11.**
- [The Transcript Wrap and the Legible 429 Entry Review (retroactive)](decisions/151-transcript-wrap-429-entry-review.md) — **RETROACTIVE record: the presentational wrap plus the actionable 429 line was implemented, verified, and committed (6ef93d4) before this review was written — no prior ticket, entry review, or approval existed; the provenance is an owner bug report (the unreadable 429 and the clipping reported live); the normal authorize-then-implement order is inverted and named as such; retroactive PASS over the committed diff (wrap_line_to_width + wrapped_transcript_rows shared by both render paths, stored text never mutated, 429 line on chat and /models paths, deliberately no retries or backoff, one post-freeze frame re-pinned); 2026-09-11.**
- [The Transcript Wrap and the Legible 429](decisions/152-transcript-wrap-429.md) — **transcript rows now wrap within the pane and HTTP 429 tells the user what to do (rate limiting — wait and retry, or switch model) while every other status keeps the decisions-137/138 shape byte-identical; ticket 123; retroactive record of commit 6ef93d4; full gate cited green (differential 352/352); 2026-09-11.**
- [The Session Model Switch (/model) Entry Review (retroactive)](decisions/153-model-switch-entry-review.md) — **RETROACTIVE record: the /model session switch was implemented, verified, and committed (4e9f4db) before this review was written — no prior ticket or entry review existed; the approval that exists is an in-chat design approval from the human owner on 2026-09-10/11, nothing more claimed, nothing backdated; the normal authorize-then-implement order is inverted and named as such; retroactive PASS over the committed diff (shared live cell read at call time, persist-before-live ordering, reused write_profile_config with the core rule, honest Fake/replay semantics, new catalog entry 13); 2026-09-11.**
- [The Session Model Switch (/model)](decisions/154-model-switch.md) — **/model <id> now switches the model the NEXT request uses and persists it (bare /model opens the TUI picker); ticket 124; retroactive record of commit 4e9f4db; full gate cited green (614 core / 316 adapters / 25 conformance / 243 cli, differential 352/352); 2026-09-11.**
- [Mouse-Wheel Scrolling and the /mouse Toggle Entry Review (retroactive)](decisions/155-mouse-wheel-toggle-entry-review.md) — **RETROACTIVE record: the wheel was implemented, verified and committed (`8214729`) before this review was written — no prior ticket or entry review existed; the approval that exists is an in-chat design approval from the human owner on 2026-09-11, who chose option (b), wheel scrolling with a toggle that returns the mouse to the terminal, after being told the tradeoff (capture stops native click-drag selection); nothing more claimed, nothing backdated; retroactive PASS over the committed diff; 2026-09-11.**
- [Mouse-Wheel Scrolling and the /mouse Toggle](decisions/156-mouse-wheel-toggle.md) — **the transcript now scrolls with the wheel: capture is paired around the alternate screen (enabled after raw mode, released before leaving, unwind paths restored, flag and escape in lockstep), the wheel step rides the EXISTING scroll_offset/max_scroll clamp, a live-loop seam (`handle_tui_mouse`) makes the WIRING unit-testable — the gap that let two earlier attempts ship helpers with zero call sites — `/mouse` joins the shared catalog in both frontends so native selection is recoverable (stdio reports truthfully instead of pretending), and wheel events are ignored while a modal, picker or add-form is open, pinned by a whole-state equality assertion; four focused tests; `npm run check` exit 0 (316/25/247/614, differential 352/352); the real terminal escape sequences and capture release remain unverified headlessly and are the owner's to confirm; 2026-09-11.**
- [Session Profile Reload (/reload) Entry Review (retroactive)](decisions/157-reload-entry-review.md) — **RETROACTIVE record: `/reload` was implemented, verified and committed (`ba66019`, `1bf4f55`, `453e31d`, `54128d6`) before this review was written — no prior ticket or entry review existed; the approval that exists is an in-chat design approval from the human owner on 2026-09-11 for a reload that re-reads declarative configuration only and never widens authority; nothing more claimed, nothing backdated; retroactive PASS over the committed diff, with the scope limit recorded (only the model applies live); 2026-09-11.**
- [Session Profile Reload (/reload)](decisions/158-session-profile-reload.md) — **a config change no longer has to mean a restart: `/reload` re-reads `siralos.toml` through the SAME loader and composition path startup uses and reports what would change as a PURE function (it cannot mutate), a changed model moves the live provider cell the NEXT request reads and adopts the file's display name (the file owns it), the profile file is never written and an unchanged model is a no-op so the pure path stays byte-identical, an invalid profile reports its diagnostic verbatim and changes nothing, an absent profile follows startup semantics, and the owner's acceptance criterion is pinned by test: a profile asking for more than the Host grants is REFUSED with the effective rules left exactly the Host's own and never reaches the apply step; `/reload` joins the shared catalog (15) in both frontends; six reload tests; `npm run check` exit 0 (316/25/253/614, differential 352/352); SCOPE LIMIT recorded rather than implied — only the model applies live, while provider/endpoint/protocol/credential changes still report that a restart is required to converge; 2026-09-11.**
- [Session Profile Reload: Live Endpoint and Protocol Entry Review (retroactive)](decisions/159-reload-live-apply-entry-review.md) — **RETROACTIVE record: the endpoint/protocol live apply was implemented, verified and committed (`6d74e80`, `283d022`, `f00e4ec`) before this review was written — no prior ticket or entry review existed; the provenance is the owner's in-chat direction on 2026-09-12 to continue the work directly plus the deferred item decision 158 §3 had already recorded, not a new design approval; nothing more claimed, nothing backdated; retroactive PASS over the committed diff (endpoint and protocol as adapter live cells read at request time, only CHANGED values applied and each named on the report, endpoint values never echoed, unchanged fields byte-identical no-ops, and the false `; live session unchanged` tail corrected and pinned; provider name and credential still `(restart to converge)`); 2026-09-12.**
- [Session Profile Reload: Live Endpoint and Protocol](decisions/160-reload-live-endpoint-protocol.md) — **a reload's endpoint and protocol changes now apply live like the model: the adapters hold the endpoint base and the protocol as cells read at request time, the apply step writes only what CHANGED (`applied: endpoint changed (live, no restart)`, `applied: protocol a -> b (live, no restart)`), endpoint values are never echoed, and an unchanged field stays a byte-identical no-op; the report tail stopped claiming `; live session unchanged` while that apply runs, and a changed provider name or credential now says `(restart to converge)` instead; tickets 126/127; retroactive record of commits `6d74e80` + `283d022` + `f00e4ec`; full gate cited green (317 adapters / 25 conformance / 253 cli (1 ignored) / 614 core, differential 352/352); 2026-09-12.**
- **Real Model/Provider is ticketed** — planning complete: [66 research](tickets/66-real-model-provider-research.md) (PASS) → [67 entry review](tickets/67-real-model-provider-entry-review.md) (HITL C1–C6 PASS) → [68 credentials & registry](tickets/68-real-provider-credentials-and-registry.md) (PASS); implementation proceeded by direct user direction and is recorded in [decision 69](decisions/69-real-provider-implementation-progress.md) — Verified 2026-08-31 per [decision 71](decisions/71-real-provider-verified-roll-up.md) — no open frontier in this range; any next work starts with a new ticket + entry review per ADR 0036.

## Not yet specified

<!-- In-scope fog — you can see it's coming but can't ticket sharply yet; graduates as frontier moves -->

- ~~Exact shape of the R7.5 independent-review **PASS/FAIL** rubric~~ — **decided** in [R7.5 Review Rubric](decisions/01-r7-5-review-rubric.md) (strings §1, authority §2, sanitizer advisory §3, flag scope §4, checklist §5).
- ~~Granularity of **R8 vs R9** slice boundaries~~ — **decided** in [R8 vs R9 Cut](decisions/04-r8-r9-cut.md) (6+3 surfaces, per-row `unavailable` posture, Not-in-R8/R9 list); corpus subject names remain advisory until the R8 entry review freezes them.
- ~~**R10 (H1/H2/ICM + H3 runtime-readiness) parity** subject breakdown~~ — **decided** in [R10 Scope](decisions/05-r10-scope.md) (one Verified + 3 sub-slices `H1→H2→ICM→H3`; 14 subjects with core seams; R11 exclusion list); advisory until the R10 entry review freezes corpus names.
- ~~**R11 full differential/effect-boundary/security/recovery/cross-platform parity** closure criteria~~ — **decided** in [R11 Gate](decisions/06-r11-gate.md) (entry on R7+R8/R9+R10a-c; 4 pillars + measurement discipline; 7-surface PASS artefacts).
- ~~**R12 retirement/retention disposition** evidence template~~ — **decided** in [R12 Disposition](decisions/07-r12-disposition.md) (8 shared evidences + 5-field retention vs retirement retained-audits, 8 guardrails); disposition itself waits on R11 Verified.
- ~~Stage 4 **Controlled Runtime Execution** generic host boundary vs. Godot runtime adapter layering~~ — **decided** in [Stage 4 Entry Sequence](decisions/08-stage4-entry-sequence.md) (generic 4.1-4.2 before Godot 4.3+ specialization, four-thing ordered gate, stages 4-6 not guaranteed).
- **siralos-godot externalization** — 60 research decided, 61 C1–C6 frozen **PASS 2026-08-31**, 62 cutover plan frozen **PASS 2026-08-31**, 63 bootstrap Verified at `d098926` 77 tests, 64 pin **Option A** **PASS 2026-08-31**, 65 standalone **plugin fully self-contained** **PASS 2026-08-31** — Stage 7 is **complete**: `../siralos-godot` at `1bf2ca3` (domain 41 + adapters 10 entries + workspace/config/paths helpers, 234 tests, self-contained, pushed to GitHub — the plugin repo is managed independently in its own repository), monorepo at `87bfd35` (3 members, adapters → core only, `siralos-godot` external path dep, harness consumes plugin adapters, core 505 + adapters 155 + cli 70, **differential 315/315 parity held**); was fog until Stage 5 `siralos.lock` + Stage 6 Verified — no open frontier, any next work needs a new ticket per ADR 0036.
- ~~**Real Model/Provider** — 66 research decided, 67 C1–C6 frozen **PASS 2026-08-31**, 68 credentials & registry **PASS 2026-08-31**; implementation proceeded by direct user direction and is recorded in [decision 69](decisions/69-real-provider-implementation-progress.md): `ProfileRecord` fields + `siralos.toml` parsing, env-only `HostCredential`, registry + all-purpose `GenericProvider` with provider-neutral placeholder defaults, bounded HTTP adapters, and the hermetic `provider-generic` subject at `v53` (audit 316/316 applicable required, pinned v32 oracle untouched) are **landed and full-gate green**, together with the replay-recording slice and the 68 §4 secret-hygiene audit per [decision 70](decisions/70-real-provider-replay-recording.md) — remaining: the Real Provider Verified roll-up only.~~ — **graduated**: Real Provider is **Verified** per [decision 71](decisions/71-real-provider-verified-roll-up.md).

## Out of scope

<!-- Conscious scope boundary — never graduates; returns only if Destination is redrawn -->

- General Hooks — explicit `before_model`/`after_tool` injection points that create hidden control flow; kept **FUTURE / NOT DUE** — use an explicit Skill or Tool instead (ADR 0036).
- TaskGraph / generic workflow engines — a build-graph system where tasks declare dependencies (“B needs A”) and the engine schedules them in parallel/sequence; Siralos is a single bounded turn + tools harness, not a general DAG runner; kept **FUTURE / NOT DUE**.
- Plugin dependency graphs — letting plugin A declare “I need plugin B”; creates a package manager (A→B→C). Lean rule: a plugin depends only on the Host version — **FUTURE / NOT DUE**.
- Automatic Skill/Plugin acquisition — the model silently downloading a Skill/Plugin it thinks it needs; kept **FUTURE / NOT DUE** — `Add Plugin` stays explicit and Host-gated via `siralos.toml` digest + `DomainHost::install`.
- Generic Memory subsystem — an ambient, hidden store that auto-remembers across runs; kept **FUTURE / NOT DUE** — memory, if wanted, is an explicit `Context` item you can `show/explain/pin` (not a core Host feature).
- Re-enabling pathname-based filesystem/process approximations that the fail-closed posture intentionally reports as `unavailable` — permanent security boundary.
